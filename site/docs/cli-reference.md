# CLI Reference

> Auto-generated by `specgraph docs cli`. Do not edit manually.

---

- [Spec Management](#spec-management)
- [Authoring Funnel](#authoring-funnel)
- [Slices](#slices)
- [Graph Queries](#graph-queries)
- [Drift & Linting](#drift-linting)
- [Execution Lifecycle](#execution-lifecycle)
- [Lifecycle Transitions](#lifecycle-transitions)
- [Decisions](#decisions)
- [Constitution](#constitution)
- [Conversations](#conversations)
- [Findings](#findings)
- [Sync](#sync)
- [Export & Backup](#export-backup)
- [Server & Config](#server-config)

---

## Spec Management

### specgraph create

Create a new spec

```
specgraph create <slug> [flags]
```

**Flags:**

```
      --intent string     intent for the spec (required)
      --priority string   priority (p0-p3) (default "p2")
```

### specgraph show

Show spec details

```
specgraph show <slug> [flags]
```

**Flags:**

```
      --json   output as JSON
```

### specgraph list

List specs

```
specgraph list [flags]
```

**Flags:**

```
      --json              output as JSON
      --priority string   filter by priority
      --stage string      filter by stage
```

### specgraph update

Update an existing spec

```
specgraph update <slug> [flags]
```

**Flags:**

```
      --complexity string   new complexity
      --intent string       new intent
      --notes string        free-text notes
      --priority string     new priority
      --stage string        new stage
```

### specgraph changes

List changelog entries for a spec

```
specgraph changes <slug> [flags]
```

**Flags:**

```
      --checkpoints           show only checkpoint entries
      --diff                  show inline word-level diffs
      --from int32            compare from this version (requires --diff)
      --json                  output as JSON
      --limit int32           maximum number of entries (0 = all)
      --since-version int32   show entries after this version
      --to int32              compare to this version (requires --diff)
```

## Authoring Funnel

### specgraph spark

Capture an idea and enter the authoring funnel

```
specgraph spark <slug> [flags]
```

**Flags:**

```
      --seed string   seed idea (one sentence)
```

### specgraph shape

Bound scope, explore solutions, and surface risks (use --json-file to supply output)

```
specgraph shape <slug> [flags]
```

**Flags:**

```
      --json-file string   path to JSON file containing ShapeOutput
```

### specgraph specify

Define interface contract, verification criteria, and invariants (use --json-file to supply output)

```
specgraph specify <slug> [flags]
```

**Flags:**

```
      --json-file string   path to JSON file containing SpecifyOutput
```

### specgraph decompose

Break a spec into work units (use --json-file to supply output)

```
specgraph decompose <slug> [flags]
```

**Flags:**

```
      --json-file string   path to JSON file containing DecomposeOutput
```

### specgraph approve

Mark a spec as approved and ready for execution

```
specgraph approve <slug>
```

## Slices

### specgraph slice

Manage decomposition slices

```
specgraph slice
```

#### specgraph slice claim

Claim a slice for work

```
specgraph slice claim <slug> [flags]
```

**Flags:**

```
      --assignee string   who is claiming (required)
```

#### specgraph slice complete

Mark a slice as done

```
specgraph slice complete <slug>
```

#### specgraph slice get

Show slice details

```
specgraph slice get <slug> [flags]
```

**Flags:**

```
      --json   output as JSON
```

#### specgraph slice list

List slices for a spec

```
specgraph slice list <parent-slug> [flags]
```

**Flags:**

```
      --json   output as JSON
```

## Graph Queries

### specgraph deps

Show dependencies of a spec

```
specgraph deps <slug> [flags]
```

**Flags:**

```
      --json         output as JSON
      --transitive   show transitive dependencies
```

### specgraph impact

Show specs impacted by changes to a spec

```
specgraph impact <slug> [flags]
```

**Flags:**

```
      --json   output as JSON
```

### specgraph critical-path

Show the critical path for a spec

```
specgraph critical-path <slug> [flags]
```

**Flags:**

```
      --json   output as JSON
```

### specgraph ready

Show specs ready to work on

```
specgraph ready [flags]
```

**Flags:**

```
      --json   output as JSON
```

### specgraph edge

Manage graph edges

```
specgraph edge
```

#### specgraph edge add

Add an edge between two nodes

```
specgraph edge add <from-slug> <to-slug> [flags]
```

**Flags:**

```
      --type string   edge type (depends_on, blocks, composes, relates_to, informs) (default "depends_on")
```

#### specgraph edge list

List edges for a node

```
specgraph edge list <slug> [flags]
```

**Flags:**

```
      --json          output as JSON
      --type string   filter by edge type
```

#### specgraph edge remove

Remove an edge between two nodes

```
specgraph edge remove <from-slug> <to-slug> [flags]
```

**Flags:**

```
      --type string   edge type (default "depends_on")
```

## Drift & Linting

### specgraph drift

Check specs for drift

```
specgraph drift [slug] [flags]
```

**Flags:**

```
      --json           output as JSON
      --scope string   drift check scope (deps|interfaces|verify); omit for all
```

#### specgraph drift acknowledge

Acknowledge drift for a spec

```
specgraph drift acknowledge <slug> [flags]
```

**Flags:**

```
      --all               acknowledge all upstream dependencies
      --note string       acknowledgement note (required)
      --upstream string   specific upstream slug to acknowledge
```

### specgraph lint

Lint specs for violations

```
specgraph lint [slug]
```

## Execution Lifecycle

### specgraph claim

Claim a spec for an agent

```
specgraph claim <slug> [flags]
```

**Flags:**

```
      --agent string        agent identifier (required)
      --duration duration   lease duration (default 15m0s)
```

### specgraph unclaim

Release a claim on a spec

```
specgraph unclaim <slug> [flags]
```

**Flags:**

```
      --agent string   agent identifier (required)
```

### specgraph report-progress

Report execution progress for a claimed spec

```
specgraph report-progress <slug> [flags]
```

**Flags:**

```
      --agent string     agent identifier (required)
      --message string   progress message (required)
```

### specgraph report-blocker

Report a blocker on a claimed spec

```
specgraph report-blocker <slug> [flags]
```

**Flags:**

```
      --agent string         agent identifier (required)
      --description string   blocker description (required)
```

### specgraph report-completion

Report completion of a claimed spec (transitions to done)

```
specgraph report-completion <slug> [flags]
```

**Flags:**

```
      --agent string   agent identifier (required)
```

### specgraph bundle

Generate an execution bundle for a spec

```
specgraph bundle <slug> [flags]
```

**Flags:**

```
      --endpoint string   override callback endpoint
      --output string     write YAML to file instead of stdout
```

### specgraph progress

Show execution events for a spec

```
specgraph progress <slug> [flags]
```

**Flags:**

```
      --limit uint32   max events to show (default 20)
```

## Lifecycle Transitions

### specgraph amend

Amend a spec, returning it to an earlier authoring stage

```
specgraph amend <slug> [flags]
```

**Flags:**

```
      --re-entry string   authoring stage to re-enter (spark|shape|specify|decompose|approved|in_progress|review)
      --reason string     reason for amendment (required)
```

### specgraph supersede

Supersede a spec with a new one

```
specgraph supersede <slug> [flags]
```

**Flags:**

```
      --with string   slug for the replacement spec (required)
```

### specgraph abandon

Abandon a spec

```
specgraph abandon <slug> [flags]
```

**Flags:**

```
      --reason string   reason for abandonment (required)
```

## Decisions

### specgraph decision

Manage decisions

```
specgraph decision
```

#### specgraph decision create

Create a new decision

```
specgraph decision create <slug> [flags]
```

**Flags:**

```
      --confidence string      confidence level (high|medium|low)
      --decision string        decision text
      --origin-spec string     slug of originating spec
      --origin-stage string    authoring stage
      --question string        the question being decided
      --rationale string       rationale
      --rejected stringArray   rejected alternative "Option:Reason" (repeatable)
      --scope string           decision scope (project|team|org)
      --tags string            comma-separated tags
      --title string           decision title (required)
```

#### specgraph decision list

List decisions

```
specgraph decision list [flags]
```

**Flags:**

```
      --json            output as JSON
      --status string   filter by status
```

#### specgraph decision show

Show decision details

```
specgraph decision show <slug> [flags]
```

**Flags:**

```
      --json   output as JSON
```

## Constitution

### specgraph constitution

Manage the project constitution

```
specgraph constitution
```

#### specgraph constitution emit

Emit constitution as tool files

```
specgraph constitution emit [flags]
```

**Flags:**

```
      --format string   output format (e.g. claude-md) (default "claude-md")
  -o, --output string   write output to file instead of stdout
```

#### specgraph constitution import

Import a constitution from a YAML file (or stdin)

```
specgraph constitution import [file] [flags]
```

**Flags:**

```
      --layer string     constitution layer (user|org|project|domain; default: project)
      --project string   project slug (defaults to slug from .specgraph.yaml)
```

#### specgraph constitution show

Show the project constitution

```
specgraph constitution show [flags]
```

**Flags:**

```
      --json           output as JSON
      --layer string   show specific layer (user|org|project|domain; default: merged)
```

## Conversations

### specgraph conversation

Manage authoring conversation logs

```
specgraph conversation
```

#### specgraph conversation list

List authoring conversation logs for a spec

```
specgraph conversation list <slug> [flags]
```

**Flags:**

```
      --json           output as JSON
      --stage string   filter by authoring stage
```

#### specgraph conversation record

Record authoring conversation exchanges for a spec stage

```
specgraph conversation record <slug> [flags]
```

**Flags:**

```
      --amend              mark as amend re-entry
      --json               output as JSON
      --json-file string   path to JSON file containing conversation exchanges
      --stage string       authoring stage (spark, shape, specify, decompose, approve)
```

## Findings

### specgraph findings

Manage analytical findings

```
specgraph findings
```

#### specgraph findings list

List findings for a spec

```
specgraph findings list <slug> [flags]
```

**Flags:**

```
      --json               output as JSON
      --pass-type string   filter by pass type (constitution-check, red-team, peripheral-vision, consistency, simplicity)
```

#### specgraph findings store

Reads findings from a JSON file (proto3 format) and stores them via the StoreFindings RPC.

```
specgraph findings store <slug> [flags]
```

**Flags:**

```
      --json               output as JSON
      --json-file string   path to findings JSON file (required)
      --pass-type string   pass type (constitution-check, red-team, peripheral-vision, consistency, simplicity) (required)
```

### specgraph pass

Manage analytical passes

```
specgraph pass
```

#### specgraph pass run

Calls RunAnalyticalPass RPC and returns the prompt template, tool manifest, and instructions. Default output is markdown optimized for LLM consumption.

```
specgraph pass run <slug> [flags]
```

**Flags:**

```
      --json               output as JSON
      --pass-type string   pass type (constitution-check, red-team, peripheral-vision, consistency, simplicity) (required)
```

## Sync

### specgraph sync

Sync specs with external systems

```
specgraph sync
```

#### specgraph sync beads

Push approved specs to Beads as issues

```
specgraph sync beads [flags]
```

**Flags:**

```
      --dry-run           show what would be synced without syncing
      --priority string   only sync specs at this priority
      --stage string      only sync specs at this stage
```

#### specgraph sync github

Push specs as GitHub Issues

```
specgraph sync github [flags]
```

**Flags:**

```
      --dry-run           show what would be synced without syncing
      --priority string   only sync specs at this priority
      --stage string      only sync specs at this stage
```

#### specgraph sync status

Show sync state for all specs

```
specgraph sync status [flags]
```

**Flags:**

```
      --adapter string   filter by adapter (beads, github)
      --spec string      filter by spec slug
```

## Export & Backup

### specgraph export

Export a project to a JSON backup file

```
specgraph export <project-slug> [flags]
```

**Flags:**

```
  -o, --output string   output file path (default: stdout)
```

### specgraph import

Import a project from a JSON backup file

```
specgraph import <file> [flags]
```

**Flags:**

```
      --force               overwrite existing project data
      --require-signature   require a valid HMAC signature
```

### specgraph verify

Verify an export matches current server state

```
specgraph verify <file>
```

## Server & Config

### specgraph up

Start the SpecGraph server (daemon, service, or manual)

```
specgraph up
```

### specgraph down

Stop the SpecGraph server

```
specgraph down [flags]
```

**Flags:**

```
      --rm   uninstall the service definition after stopping
```

### specgraph serve

Start the SpecGraph server

```
specgraph serve [flags]
```

**Flags:**

```
      --cors-origin string   Enable CORS for this origin (dev mode only)
      --pg-url string        PostgreSQL connection URL (overrides config; env: SPECGRAPH_PG_URL)
```

### specgraph status

Show server and service health

```
specgraph status [flags]
```

**Flags:**

```
      --json   output as JSON
```

### specgraph health

Check server health

```
specgraph health
```

### specgraph init

Initialize a SpecGraph project in the current directory

```
specgraph init [project-slug] [flags]
```

**Flags:**

```
      --yes   non-interactive (accepted for backward compat; init is always non-interactive)
```

### specgraph prime

Ensure the server is running, then print project context and active specs for use by Claude Code's SessionStart hook.

```
specgraph prime
```

### specgraph inject

Inject spec execution context (bundle + constitution subset) into tool-specific workspace files.

```
specgraph inject <slug> [flags]
```

**Flags:**

```
  -o, --output string   output directory (default: current directory)
      --tool string     target tool (claude-code, cursor, agents-md) (default "claude-code")
```

