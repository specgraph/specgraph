---
phase: 04-verification-integration-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/api/lifecycle_test.go
  - internal/storage/postgres/lifecycle_test.go
autonomous: true
requirements: [DRFT-01]
must_haves:
  truths:
    - "Editing a spec that is NOT an upstream dependency produces NO drift on a downstream done-spec (headline SC#2 no-false-positive), proven against a real DB through LifecycleService.CheckDrift"
    - "A full-graph CheckDrift (empty slug) over a mixed seed (drifted-done + clean-done + non-done) reports SkippedCount >= 1 and surfaces exactly the drifted spec, proven against a real DB"
    - "A per-upstream (UpstreamSlug) AcknowledgeDrift re-baselines content_hash_at_link and a subsequent CheckDrift on the downstream reports no drift items, proven end-to-end through the interface"
  artifacts:
    - "e2e/api/lifecycle_test.go — new Describe(\"Drift detection (no false-positive on unrelated edit)\") block"
    - "e2e/api/lifecycle_test.go — new It(\"acknowledges a single upstream and re-checks clean\") spec"
    - "internal/storage/postgres/lifecycle_test.go — new t.Run(\"CheckAllSpecs_MixedState_SkippedCount\") subtest under TestLifecycle"
  key_links:
    - "DEPENDS_ON edge content_hash_at_link vs upstream live ContentHash comparison in Engine.checkSpec"
    - "done-stage eligibility gate: non-done specs counted as SkippedCount in all-specs mode"
    - "X-Specgraph-Project header project scoping preserved by the shared e2e clients"
---

<objective>
Close the three SC#2 verification gaps for DRFT-01 (`spgr-vch`) by adding targeted
real-DB tests against the already-shipped dependency-drift interface. No drift engine,
converter, scope-table, proto, or migration change — this is verification only (D-01, D-02, D-03).

Purpose: Prove the drift interface flags true drift and does NOT false-positive on
unrelated edits (roadmap SC#2), against real content-hash and DEPENDS_ON-edge scenarios.
Output: One new e2e no-false-positive block, one new e2e per-upstream ack round-trip spec,
and one new real-DB full-graph SkippedCount integration subtest.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verification-integration-reliability/04-CONTEXT.md
@.planning/phases/04-verification-integration-reliability/04-RESEARCH.md
@.planning/phases/04-verification-integration-reliability/04-PATTERNS.md
@.planning/phases/04-verification-integration-reliability/04-VALIDATION.md

# Analog sources to copy from (read these before writing tests):
@e2e/api/lifecycle_test.go
@e2e/api/helpers_test.go
@internal/storage/postgres/lifecycle_test.go
@internal/drift/drift.go
@internal/drift/drift_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add e2e no-false-positive-on-unrelated-edit drift spec (headline SC#2)</name>
  <files>e2e/api/lifecycle_test.go</files>
  <read_first>
    - e2e/api/lifecycle_test.go lines 230-310 — the existing `Describe("Drift detection", ...)` block: seed → advanceStage(ctx, slug, "done") → graphClient.AddEdge(DEPENDS_ON) → timestampSkew sleep → UpdateSpec → CheckDrift. Copy this shape.
    - e2e/api/lifecycle_test.go lines 21-25 — `const timestampSkew = 1200 * time.Millisecond`.
    - e2e/api/helpers_test.go lines 88-201 (`advanceStage`), 19-41/71-73 (project-scoped shared clients).
    - 04-PATTERNS.md "add no-false-positive spec" section and RESEARCH Pitfall 2 (the edit must be truly unrelated AND change a hash).
  </read_first>
  <action>
    Add a new `Describe("Drift detection (no false-positive on unrelated edit)", func() {...})` block INSIDE the outer `Describe("Lifecycle", Ordered, ...)` container, placed BEFORE the existing `Describe("Drift detection (all specs)")` block. Reuse the package-level `lifecycleClient`, `specClient`, `graphClient`, and `ctx` — do NOT recreate clients or imports (all needed imports already exist in the file). Use THREE uniquely-named done specs to avoid collision with the existing "Drift detection" block's slugs: e.g. `nfp-downstream`, `nfp-upstream`, `nfp-unrelated`. Create all three via specClient.CreateSpec, advance each to "done" with `advanceStage(ctx, slug, "done")`. Wire ONLY `nfp-downstream` DEPENDS_ON `nfp-upstream` via graphClient.AddEdge with EdgeType_EDGE_TYPE_DEPENDS_ON (baselines content_hash_at_link); leave `nfp-unrelated` with NO edge to anything. Then `time.Sleep(timestampSkew)` and UpdateSpec ONLY `nfp-unrelated` with a real Intent change (proto.String(...)) so its ContentHash genuinely changes. Assert `lifecycleClient.CheckDrift(ctx, DriftCheckRequest{Slug: "nfp-downstream"})` returns no drift items: `if len(resp.Msg.Reports) > 0 { Expect(resp.Msg.Reports[0].Items).To(BeEmpty(), "editing an unrelated spec must NOT drift the downstream") }`. Sanity guard per Pitfall 2: keep the nfp-downstream→nfp-upstream edge in the seed so the drift path is genuinely exercised (the test must be able to fail if the edge is absent). Assert `err` via `Expect(err).NotTo(HaveOccurred())`, never on message strings. Do NOT leave any un-acked drift — nfp-downstream never drifts and nfp-unrelated has no downstream, so nothing to ack. Per D-02, verify (do not rebuild) the deps-scope interface.
  </action>
  <verify>
    <automated>go vet -tags e2e ./e2e/api/... && rg -q "no false-positive on unrelated edit" e2e/api/lifecycle_test.go</automated>
  </verify>
  <acceptance_criteria>
    - New Describe block compiles under the `e2e` build tag (`go vet -tags e2e ./e2e/api/...` passes).
    - Under Docker (`task test:e2e:api`), the new spec passes: CheckDrift on the downstream returns zero drift items after an unrelated spec is edited.
    - The test seeds three distinct done specs, wires only downstream→upstream, and mutates only the unrelated spec with a hash-changing Intent update.
    - No message-string error assertions; project-scoped shared clients reused; no residual un-acked drift left for the later "(all specs)" block.
  </acceptance_criteria>
  <done>New e2e no-false-positive Describe block compiles under `e2e` and (under Docker) proves an unrelated-spec edit yields zero drift on the downstream done-spec.</done>
</task>

<task type="auto">
  <name>Task 2: Add real-DB full-graph mixed-state SkippedCount integration test</name>
  <files>internal/storage/postgres/lifecycle_test.go</files>
  <read_first>
    - internal/storage/postgres/lifecycle_test.go lines 391-472 — `t.Run("AcknowledgeDrift_Basic"/"AcknowledgeDrift_AllUpstreams")`: per-subtest `newStore(t)` + `clearDatabase(t, store)` + `context.Background()`, then CreateSpec/AddEdge/UpdateSpec(done)/UpdateSpec(upstream) seed shape. Copy this.
    - internal/drift/drift.go lines 23-121 — `Backend` interface (GetSpec, ListSpecs, GetDependenciesWithEdgeData — the postgres store satisfies it), `Engine.Check`, and SkippedCount semantics (all-specs mode skips non-done specs; zero-item reports omitted).
    - internal/drift/drift_test.go lines 166-195 — `TestCheckAllSpecs` engine-level shape and `result.SkippedCount` assertion.
    - e2e/testutil/server.go — confirms `drift.NewEngine(store, nil)` is the established construction (no import cycle from `package postgres_test`).
    - 04-PATTERNS.md "full-graph SkippedCount integration test" section and RESEARCH Pitfall 4.
  </read_first>
  <action>
    Add a new subtest `t.Run("CheckAllSpecs_MixedState_SkippedCount", func(t *testing.T) {...})` inside the existing `TestLifecycle` function in internal/storage/postgres/lifecycle_test.go (place it adjacent to the AcknowledgeDrift subtests). Start with `store := newStore(t); clearDatabase(t, store); ctx := context.Background()`. Seed a mixed graph: (a) a drifted-done pair — CreateSpec `mix-drift-up` + `mix-drift-down`, AddEdge(mix-drift-down, mix-drift-up, EdgeTypeDependsOn), UpdateSpec mix-drift-down to done, then UpdateSpec mix-drift-up with a changed Intent so the upstream hash diverges (drift); (b) a clean-done pair — CreateSpec `mix-clean-up` + `mix-clean-down`, AddEdge, UpdateSpec mix-clean-down to done, leave upstream untouched (no drift); (c) at least one NON-done spec — CreateSpec `mix-skipped` and leave it at spark (do NOT advance to done) so it is counted as skipped. Construct the engine with `drift.NewEngine(store, nil)` (import `github.com/specgraph/specgraph/internal/drift`) and call `result, err := engine.Check(ctx, "", "deps")` (empty slug = all-specs). Assert `require.NoError(t, err)`, `require.GreaterOrEqual(t, result.SkippedCount, int32(1))` for the non-done spec, and that exactly the drifted downstream (`mix-drift-down`) appears in `result.Reports` with non-empty Items while the clean-done spec is absent or has zero items (zero-item reports are filtered out). No timestamp-skew needed here — storage ops are ordered by call and ContentHash changes deterministically per UpdateSpec. Do NOT touch the scope tables (D-03). Use `require.ErrorIs` with `storage.Err*` sentinels for any negative assertions (never message strings).
  </action>
  <verify>
    <automated>go vet -tags integration ./internal/storage/postgres/... && rg -q "CheckAllSpecs_MixedState_SkippedCount" internal/storage/postgres/lifecycle_test.go</automated>
  </verify>
  <acceptance_criteria>
    - New subtest compiles under the `integration` build tag (`go vet -tags integration ./internal/storage/postgres/...` passes) with no import cycle.
    - Under Docker (`task test:integration`), the subtest passes: `engine.Check(ctx, "", "deps")` returns `SkippedCount >= 1` and exactly the drifted downstream appears in Reports with non-empty Items.
    - Seed contains a drifted-done pair, a clean-done pair, and at least one non-done spec.
    - No scope-table modification; negative assertions use `storage.Err*` sentinels via `require.ErrorIs`.
  </acceptance_criteria>
  <done>New `CheckAllSpecs_MixedState_SkippedCount` integration subtest compiles under `integration` and (under Docker) asserts `SkippedCount >= 1` with exactly the drifted downstream in Reports.</done>
</task>

<task type="auto">
  <name>Task 3: Add e2e per-upstream acknowledge round-trip spec</name>
  <files>e2e/api/lifecycle_test.go</files>
  <read_first>
    - e2e/api/lifecycle_test.go lines 256-309 — the mutate-then-check (timestampSkew + 3-attempt retry) and the `AcknowledgeDrift{All: true}` + re-check pair. Copy the retry/skew idiom; swap `All: true` for `UpstreamSlug`.
    - proto: `DriftAcknowledgeRequest.upstream_slug` (per-upstream, mutually exclusive with `all`).
    - 04-PATTERNS.md "(OPTIONAL) per-upstream acknowledge round-trip" section.
  </read_first>
  <action>
    Add a new `Describe("Drift detection (per-upstream acknowledge)", func() {...})` block inside the outer Ordered container, BEFORE the existing `Describe("Drift detection (all specs)")` block. Seed two uniquely-named done specs (e.g. `pua-downstream`, `pua-upstream`), wire `pua-downstream` DEPENDS_ON `pua-upstream`, then `time.Sleep(timestampSkew)` and UpdateSpec `pua-upstream` with a hash-changing Intent to create drift. Assert drift appears on pua-downstream using the SAME 3-attempt retry loop as lines 269-286 (guards second-precision timestamp races). Then call `lifecycleClient.AcknowledgeDrift(ctx, DriftAcknowledgeRequest{Slug: "pua-downstream", Note: "Reviewed single upstream change", UpstreamSlug: "pua-upstream"})` (per-upstream, NOT All) and `Expect(resp.Msg.Report.SpecSlug).To(Equal("pua-downstream"))`. Finally re-check: `CheckDrift(ctx, DriftCheckRequest{Slug: "pua-downstream"})` and assert `if len(check.Msg.Reports) > 0 { Expect(check.Msg.Reports[0].Items).To(BeEmpty()) }` — proving the per-upstream ack re-baselined content_hash_at_link end-to-end through the interface. Because the ack fully resolves the drift, no residual drift remains for the later "(all specs)" block. All error checks via `Expect(err).NotTo(HaveOccurred())`.
  </action>
  <verify>
    <automated>go vet -tags e2e ./e2e/api/... && rg -q "per-upstream acknowledge" e2e/api/lifecycle_test.go</automated>
  </verify>
  <acceptance_criteria>
    - New Describe block compiles under the `e2e` build tag.
    - Under Docker (`task test:e2e:api`), the spec passes: drift is detected, per-upstream AcknowledgeDrift succeeds, and the subsequent CheckDrift reports no drift items.
    - Uses `UpstreamSlug` (not `All`), the timestampSkew + 3-attempt retry idiom, and uniquely-named specs; leaves no residual drift.
  </acceptance_criteria>
  <done>New per-upstream ack Describe block compiles under `e2e` and (under Docker) proves per-upstream AcknowledgeDrift re-baselines and a re-check reports no drift items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→LifecycleService | Drift check/ack RPCs cross here; requests must stay project-scoped |

## STRIDE Threat Register

This is a verification-only phase over an existing read-mostly feature — NO new attack
surface and NO new security controls. The threats below are existing guarantees that must
not regress while adding tests.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information disclosure / Tampering | Cross-project drift read/ack | low | accept | Existing `scopeStore(ctx, h.scoper)` project scoping is unchanged; new e2e tests keep using the shared clients that inject the `X-Specgraph-Project` header — do not weaken |
| T-04-02 | Information disclosure | Drift error responses | low | accept | Existing `sanitizeDriftError` + code-based mapping unchanged; any negative assertions use `connect.CodeOf(err)` / `storage.Err*` sentinels, never message strings |
| T-04-03 | Tampering | Slug / upstream_slug inputs | low | accept | Existing `validateSlug` + parameterized pgx queries unchanged; tests add no new input path |
| T-04-SC | Tampering | npm/pip/cargo/go get installs | n/a | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) |

No high-severity threats introduced.
</threat_model>

<verification>
- `go vet -tags e2e ./e2e/api/...` and `go vet -tags integration ./internal/storage/postgres/...` compile the new tests without Docker.
- `task test` stays green (new tests are behind e2e/integration build tags; unit gate unaffected).
- Wave/pre-PR gate (Docker): `task test:e2e:api` and `task test:integration` run the new SC#2 real-DB proofs.
- Scope-sync tests (`TestDriftScope*Map_Sync*`, `_Completeness`) remain green — no scope table touched (D-03).
</verification>

<success_criteria>
- SC#2 no-false-positive: an unrelated-spec edit produces zero drift on a downstream done-spec (e2e, real DB).
- SC#2 full-graph: empty-slug CheckDrift over a mixed seed reports `SkippedCount >= 1` and surfaces exactly the drifted spec (integration, real DB).
- SC#2 ack interface: per-upstream AcknowledgeDrift re-baselines and a re-check reports clean (e2e, real DB).
- No change to drift engine, converters, scope tables, proto, or migrations.
</success_criteria>

<artifacts_this_phase_produces>
New test specs (no new source symbols):
- `e2e/api/lifecycle_test.go` — `Describe("Drift detection (no false-positive on unrelated edit)")` (specs seed `nfp-downstream`/`nfp-upstream`/`nfp-unrelated`).
- `e2e/api/lifecycle_test.go` — `Describe("Drift detection (per-upstream acknowledge)")` (specs seed `pua-downstream`/`pua-upstream`).
- `internal/storage/postgres/lifecycle_test.go` — `t.Run("CheckAllSpecs_MixedState_SkippedCount")` under `TestLifecycle` (seeds `mix-drift-*`, `mix-clean-*`, `mix-skipped`).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-verification-integration-reliability/04-01-SUMMARY.md` when done
</output>
