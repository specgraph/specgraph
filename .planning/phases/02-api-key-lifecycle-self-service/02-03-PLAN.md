---
phase: 02-api-key-lifecycle-self-service
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/global.go
  - internal/server/csrf.go
  - internal/server/csrf_test.go
  - internal/server/auth_handler.go
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "A new self-service key-policy config struct exists with defaults: expiry default 90d, max 180d, quota 10, per-identity rate limit — all server-configurable"
    - "A double-submit CSRF middleware rejects cookie-authenticated POST self-key mutations whose header token does not match the non-HttpOnly cookie token, and PASSES bearer-authenticated (CLI/MCP) requests untouched"
    - "The specgraph_csrf cookie is issued (when absent) on the safe GET /api/auth/whoami the dashboard already calls on load, so the double-submit token exists before the first mutation"
    - "The deprecated legacy APIKeyConfig is NOT extended for self-service policy"
  artifacts:
    - internal/config/global.go
    - internal/server/csrf.go
    - internal/server/csrf_test.go
  key_links:
    - "config struct read by the self-mint handler (Plan 05) for expiry cap / quota / rate-limit"
    - "CSRF validate middleware is MOUNTED on the Connect IdentityService handler in Plan 05 (RegisterIdentityService / serve.go:203); the Connect session path reads specgraph_session via internal/auth/interceptor.go:57-81, NOT cookieToAuthHeader (which only wraps the REST whoami)"
    - "CSRF cookie issued on the whoami GET here; echoed as X-CSRF-Token by the web client (Plan 08)"
---

<objective>
Build the two foundational, handler-independent server-side pieces AUTH-03 needs before the self-mint handlers land: (1) a NEW self-service key-policy config struct (expiry default 90d / max 180d per D-08, per-user active-key quota 10, per-identity rate-limit thresholds) defaulted in `globalDefaults()` — NOT an extension of the deprecated `APIKeyConfig`; and (2) a double-submit CSRF middleware (`internal/server/csrf.go`) per D-09 that protects the POST-only cookie-authenticated self-mint/rotate/revoke mutations, on top of the existing `SameSite=Lax` + JSON-content-type preflight. This plan ALSO wires the CSRF cookie issuance (Addresses review concern cursor #1) onto the safe GET `/api/auth/whoami` the dashboard already calls on load, so the double-submit token exists before the first mutation. The VALIDATE middleware's mount onto the Connect IdentityService handler happens in Plan 05 (where `RegisterIdentityService` / `serve.go:203` are already modified) — see the key_links.

Purpose: Decouple config + CSRF (no handler dependency) so they can land in Wave 1 and be consumed by the handlers (Plan 05, which mounts the validator) and web client (Plan 08, which echoes the token).
Output: New config struct + defaults; CSRF middleware (validate + issue) + unit test; issuance wired onto the whoami GET.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-key-lifecycle-self-service/02-CONTEXT.md
@.planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md
@.planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md
@internal/config/global.go
@internal/server/auth_handler.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add self-service key-policy config struct + defaults</name>
  <files>internal/config/global.go</files>
  <read_first>
    - internal/config/global.go (JITCreateConfig struct at :226-233 as the yaml/koanf-tag analog; globalDefaults() at :418; the deprecated APIKeyConfig at :254 which MUST NOT be extended per RESEARCH "Deprecated/outdated")
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md (section "internal/config/global.go (config) — MODIFY")
    - .planning/phases/02-api-key-lifecycle-self-service/02-CONTEXT.md (D-08: 90d default / 180d max, quota 10, all server-configurable)
  </read_first>
  <action>
    Add a new struct (e.g. `SelfServiceKeysConfig`) to `internal/config/global.go` with yaml+koanf tags mirroring `JITCreateConfig`'s tag style: `DefaultTTLDays int`, `MaxTTLDays int`, `Quota int`, `RateLimitPerHour int`, `RateLimitBurst int`. Add it as a field on the parent config (e.g. `SelfServiceKeys SelfServiceKeysConfig` with tag `self_service_keys`) next to the existing OIDC/JITCreate fields. In `globalDefaults()` set defaults per D-08: DefaultTTLDays 90, MaxTTLDays 180, Quota 10, and conservative rate-limit defaults (burst 5, refill ~30/hr per RESEARCH Open Question 2). Do NOT modify or extend the deprecated `APIKeyConfig` (it is "ignored after Authn plan, storage owns"). Package doc comment already exists; ensure the new struct has a doc comment (revive).
  </action>
  <verify>
    <automated>go build ./internal/config/ && go test ./internal/config/ -run TestGlobalDefaults</automated>
  </verify>
  <acceptance_criteria>
    - internal/config/global.go declares SelfServiceKeysConfig with DefaultTTLDays/MaxTTLDays/Quota/RateLimit fields and koanf tags
    - globalDefaults() sets DefaultTTLDays=90, MaxTTLDays=180, Quota=10
    - No edit touches the APIKeyConfig struct body
    - `go build ./internal/config/` exits 0
  </acceptance_criteria>
  <done>A dedicated, server-configurable self-service key-policy config struct exists with the D-08 defaults; legacy APIKeyConfig untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Double-submit CSRF middleware (validate + issue) + whoami issuance + test</name>
  <files>internal/server/csrf.go, internal/server/csrf_test.go, internal/server/auth_handler.go</files>
  <read_first>
    - internal/server/auth_handler.go (whoami is a GET registered at :46 as `authMW(cookieToAuthHeader(...))`; the web calls `fetch('/api/auth/whoami')` on load — the safe-GET issuance point; sessionCookie config at :173-182 — SameSite=Lax, dynamic Secure, HttpOnly; cookieToAuthHeader at :136-148 promotes cookie→Bearer for THIS REST route only)
    - internal/auth/interceptor.go (authenticate at :57-68 → sessionCookieValue at :70-82 reads specgraph_session — this is the Connect session path the VALIDATE middleware guards when Plan 05 mounts it; extractBearerToken at :84-95 — bearer-authed requests are NOT cookie-CSRF-able and must be exempt)
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md (section "internal/server/csrf.go (middleware) — NEW"; No-Analog note: double-submit token logic is net-new)
    - .planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md (Open Question 1 recommendation: double-submit crypto/rand token, non-HttpOnly cookie + echoed header, small middleware around mutating routes)
  </read_first>
  <action>
    Create `internal/server/csrf.go` (SPDX header, package doc if needed) with double-submit CSRF pieces modeled on `cookieToAuthHeader`'s `http.Handler` wrapper:
    (a) an **issuer** — a helper `issueCSRFCookie(w, r)` (and a thin middleware wrapper `csrfIssue(next http.Handler)`) that, when the `specgraph_csrf` cookie is ABSENT, generates a `crypto/rand` token and sets it as a NON-HttpOnly cookie (name `specgraph_csrf`) mirroring `sessionCookie`'s SameSite=Lax + dynamic Secure attributes (but NOT HttpOnly, so the web client can echo it);
    (b) a **validator** — `csrfValidate(next http.Handler)` that, for a request, ENFORCES only when ALL of: method is mutating (POST), the request is COOKIE-authenticated (no `Authorization: Bearer` header — bearer/CLI/MCP requests are exempt because they are not CSRF-able, per interceptor.go:84-95), and the request path is one of the four self-key Connect procedures (`/specgraph.v1.IdentityService/CreateMyAPIKey`, `/ListMyAPIKeys`, `/RotateMyAPIKey`, `/RevokeMyAPIKey`). When enforced, require an `X-CSRF-Token` header whose value equals the `specgraph_csrf` cookie via `crypto/subtle.ConstantTimeCompare` — mismatch/absent → HTTP 403. All other requests pass through untouched. (Addresses review concern cursor #6 — ListMyAPIKeys, also a POST in Connect, is INCLUDED in the enforced set for defense-in-depth; it is metadata-only per identity.proto:54-56 but cheap to protect.)
    Wire issuance: in `RegisterAuthHandlers` (`internal/server/auth_handler.go`), wrap the existing whoami GET handler so a successful safe GET issues the `specgraph_csrf` cookie when absent (e.g. `csrfIssue(authMW(cookieToAuthHeader(...)))` or an inline `issueCSRFCookie` call before writing the whoami response). Do NOT reintroduce or weaken the existing SameSite=Lax session cookie.
    Create `internal/server/csrf_test.go`: assert (a) a valid matching token on a self-key POST passes; (b) missing header on a cookie-authed self-key POST → 403; (c) mismatched header → 403; (d) a Bearer-authenticated request to the same path is EXEMPT (passes without a token); (e) the issuer sets a non-HttpOnly `specgraph_csrf` cookie when absent and does not overwrite an existing one.
  </action>
  <verify>
    <automated>go test ./internal/server/ -run TestCSRF</automated>
  </verify>
  <acceptance_criteria>
    - internal/server/csrf.go exports a validator (csrfValidate) and an issuer (issueCSRFCookie / csrfIssue), using crypto/rand for generation and crypto/subtle.ConstantTimeCompare for validation
    - The validator ENFORCES only on cookie-authenticated mutating POSTs to the four self-key procedures and EXEMPTS Bearer-authenticated requests (cursor #2/#6)
    - The whoami GET in RegisterAuthHandlers issues the specgraph_csrf cookie when absent (cursor #1 — token bootstrap)
    - `go test ./internal/server/ -run TestCSRF` exits 0 covering pass / missing-header-403 / mismatch-403 / bearer-exempt / non-HttpOnly-issuance
    - The CSRF cookie is set with SameSite=Lax and is non-HttpOnly (readable by the web client for echo)
  </acceptance_criteria>
  <done>A tested double-submit CSRF middleware (validate + issue) guards cookie-authed POST self-key mutations with constant-time comparison, exempts bearer callers, and bootstraps the token on the whoami GET; Plan 05 mounts the validator on the Connect handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cookie-authed mutation | CSRF-relevant boundary for POST self-key mutations |
| operator config → server policy | expiry/quota/rate-limit ceilings set here bound stale-privilege windows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering (CSRF) | POST self-mint/rotate/revoke/list via session cookie | high | mitigate | Double-submit CSRF token (crypto/rand non-HttpOnly cookie issued on the whoami GET + echoed X-CSRF-Token header, constant-time compare) on top of SameSite=Lax + JSON preflight (D-09). Enforced ONLY on cookie-authenticated POSTs to the four self-key procedures — ListMyAPIKeys included for defense-in-depth (cursor #6); Bearer-authenticated CLI/MCP requests are exempt (not CSRF-able, cursor #2). Validator MOUNTED on the Connect IdentityService handler in Plan 05; without that mount the mitigation is aspirational, so the mount is a Plan 05 acceptance criterion (cursor #1). |
| T-02-09 | Elevation (latency) | Stale privilege on long-lived key | medium | mitigate | Mandatory expiry with tight 180d max cap (D-08) enforced from this config in the handler |
| T-02-10 | Information Disclosure | CSRF token timing side-channel | low | accept | Constant-time compare used; token is not itself a secret credential |
| T-02-31 | Spoofing (CSRF bypass) | Missing token bootstrap → first POST has no cookie to echo | high | mitigate | Cookie issued on the safe GET /api/auth/whoami the dashboard calls on load, so the token exists before any mutation (cursor #1) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Phase-wide inventory (this plan delivers the **bolded** items):

- Proto/RPCs: CreateMyAPIKey, ListMyAPIKeys, RotateMyAPIKey, RevokeMyAPIKey, ResyncUserRole + messages
- Auth: exported `auth.RoleMin`; Cedar verb `apikey.self`; base.cedar permit; procedure→action map; drift test
- Storage: `GetAPIKeyForUser`, `RevokeAPIKeyForUser`, `RotateAPIKeyForUser`, quota-safe `CreateAPIKeyForUser`; sentinel `ErrQuotaExceeded`
- Server: four self handlers; `ResyncUserRole` handler; `selfMintLimiter`; `clampExpiry`; **CSRF double-submit middleware — validator + issuer (`internal/server/csrf.go`) with issuance wired onto the whoami GET (`auth_handler.go`); validator mounted on the Connect IdentityService handler in Plan 05**
- Config: **self-service key-policy struct (default 90d / max 180d / quota 10 / rate-limit) in `internal/config/global.go`**
- CLI: `auth api-key create/list/rotate/revoke` self-variants; session-preferring resolver (Finding D); `auth user resync <user-id> --role <r> [--revoke-keys]`
- Web: `web/src/routes/keys/+page.svelte`, `web/src/lib/components/RevealKeyModal.svelte`, `web/src/lib/keys.svelte.ts`, `identityClient` + CSRF interceptor
</artifacts_produced>

<verification>
- Config struct + defaults present; legacy APIKeyConfig untouched.
- CSRF middleware unit test green (pass / 403 missing / 403 mismatch / bearer-exempt / non-HttpOnly issuance).
- CSRF cookie issued on the whoami GET; validator mount is a Plan 05 acceptance criterion.
</verification>

<success_criteria>
Server-side policy (expiry/quota/rate-limit) and CSRF protection exist independently of the handlers, ready to be consumed by Plan 05 and Plan 08.
</success_criteria>

<output>
Create `.planning/phases/02-api-key-lifecycle-self-service/02-03-SUMMARY.md` when done.
</output>
