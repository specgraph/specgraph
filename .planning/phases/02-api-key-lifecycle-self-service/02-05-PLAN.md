---
phase: 02-api-key-lifecycle-self-service
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - internal/server/identity_handler.go
  - internal/server/identity_selfkeys_test.go
  - cmd/specgraph/serve.go
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "CreateMyAPIKey/RotateMyAPIKey derive the owner from auth.IdentityFromContext (no target field), reject Source==\"apikey\" with CodePermissionDenied, and floor role_downgrade at RoleMin(requestedOrInherit, caller.EffectiveRole) on BOTH create and rotate"
    - "ListMyAPIKeys hard-sets the storage filter UserID from context and never returns another user's keys"
    - "Self-mint enforces expiry cap (90d default / 180d max) → CodeInvalidArgument over cap, per-identity rate limit → CodeResourceExhausted, and quota → CodeResourceExhausted (storage.ErrQuotaExceeded mapped in identityError)"
    - "The Plan 03 CSRF validator is MOUNTED on the Connect IdentityService handler in RegisterIdentityService, so cookie-authed self-key POSTs without a valid X-CSRF-Token are rejected (Bearer/CLI exempt)"
    - "Self create/rotate/revoke each emit a structured audit log line — actor = the self identity from auth.IdentityFromContext, plus the key id and the action — and the line NEVER contains the plaintext secret or an unescaped user-supplied label (log-injection / PII guard, RESEARCH V7 / §6)"
  artifacts:
    - internal/server/identity_handler.go
    - internal/server/identity_selfkeys_test.go
  key_links:
    - "handler → auth.RoleMin (Plan 04) → floored role_downgrade"
    - "handler → storage.CreateAPIKeyForUser/RotateAPIKeyForUser/RevokeAPIKeyForUser/GetAPIKeyForUser (Plan 02) — handler-owned secret generation, storage returns *APIKey only"
    - "handler → SelfServiceKeysConfig (Plan 03) for expiry cap / quota / rate limit"
    - "RegisterIdentityService → csrfValidate (Plan 03) mounted on the Connect handler; identityError → CodeResourceExhausted for storage.ErrQuotaExceeded"
---

<objective>
Implement the four AUTH-03 self-service handlers on `IdentityHandler` (CreateMyAPIKey, ListMyAPIKeys, RotateMyAPIKey, RevokeMyAPIKey), wiring together every gate the canonical g7st design mandates: owner-from-context, `Source=="apikey"` anti-key-chaining rejection, the `RoleMin` floor on create AND rotate (SC#2), owner-scoped storage calls, the per-identity rate limiter, expiry clamp to the 90d/180d config caps (D-08), quota-safe mint, and emit-once delivery. This is the core AUTH-03 deliverable satisfying SC#1 and SC#2.

Purpose: A non-admin OIDC user can safely self-provision a role-capped, expiring key that can never launder a higher role.
Output: Four self handlers + wiring (config, rate limiter, clampExpiry) + adversarial unit tests.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-key-lifecycle-self-service/02-CONTEXT.md
@.planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md
@.planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md
@docs/superpowers/specs/2026-06-16-spgr-g7st-self-service-api-keys-design.md
@internal/server/identity_handler.go
@internal/auth/identitystore.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreateMyAPIKey + RotateMyAPIKey (mint pair with all gates)</name>
  <files>internal/server/identity_handler.go, internal/server/identity_selfkeys_test.go</files>
  <read_first>
    - internal/server/identity_handler.go (CreateAPIKey at :223 emit-once model; RotateAPIKey at :280; identityError code-mapping at :55-76; IsBuiltinRole validation at :231-234). NOTE the REAL constructor shape: `IdentityHandler struct { users storage.UsersBackend; logger *slog.Logger }` at :21 and `RegisterIdentityService(mux *http.ServeMux, users storage.UsersBackend, opts ...connect.HandlerOption)` at :29 — there is NO config field today. Threading SelfServiceKeysConfig means adding a struct field + a constructor parameter, which changes the call site.
    - cmd/specgraph/serve.go (the SOLE production call site `server.RegisterIdentityService(mux, res.authStore, opts, maxBytes)` at :203. NOTE the real signature is `RegisterIdentityService(mux *http.ServeMux, users storage.UsersBackend, opts ...connect.HandlerOption)` at identity_handler.go:29 — both `opts` (`connect.WithInterceptors(...)`, serve.go:191) and `maxBytes` (`connect.WithReadMaxBytes(4<<20)`, serve.go:181) are `connect.HandlerOption` values BUNDLED into the single variadic `opts ...` param, NOT separate positional parameters. Threading SelfServiceKeysConfig therefore means adding a NON-variadic parameter (or a struct field), which changes this call site; adding it as another variadic HandlerOption would not thread config. The two `_test.go` call sites in internal/server are covered by `go test ./internal/server/`, but serve.go is only caught by `go build ./...`)
    - internal/auth/identitystore.go (rateLimiterFor sync.Map pattern at :643 to reuse for selfMintLimiter; IdentityFromContext usage)
    - internal/server/auth_handler.go (auth.IdentityFromContext at :124 — owner source)
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md ("internal/server/identity_handler.go — MODIFY"; the CreateMyAPIKey deltas + RotateMyAPIKey non-inherit rule)
    - docs/superpowers/specs/2026-06-16-spgr-g7st-self-service-api-keys-design.md (§1 four gates, §3 rotate re-cap, §4 delivery)
  </read_first>
  <behavior>
    - Source=="apikey" caller → CodePermissionDenied (anti key-chaining, both create and rotate)
    - Empty/inherit role_downgrade resolves to caller.EffectiveRole BEFORE RoleMin; floored = RoleMin(that, caller.EffectiveRole) so a reader-effective caller can only mint a reader-ceiling key
    - Expiry over MaxTTL → CodeInvalidArgument; absent expiry → DefaultTTL (90d)
    - Rotate re-floors role_downgrade and re-caps expiry via explicit args; never inherits the old key's ceiling or window
    - Rate limit exceeded → CodeResourceExhausted; quota exceeded (storage) → CodeResourceExhausted
    - On a successful create AND rotate, a structured audit line is emitted (actor = self identity, key id, action) and it contains neither the plaintext secret nor the raw user-supplied label
  </behavior>
  <action>
    Add `CreateMyAPIKey` and `RotateMyAPIKey` to `internal/server/identity_handler.go`. Both: derive `id, ok := auth.IdentityFromContext(ctx)` (no `msg.GetUserId()`), return `CodeUnauthenticated` if absent; reject `id.Source == "apikey"` with `connect.NewError(connect.CodePermissionDenied, …)` (Finding F5); gate on a per-identity limiter (add a `selfMintLimiter(userID)` helper reusing the `rateLimiterFor` sync.Map pattern, thresholds from `SelfServiceKeysConfig`) returning `CodeResourceExhausted`. For create: validate a non-empty `role_downgrade` is a built-in role (`auth.IsBuiltinRole`) else `CodeInvalidArgument`; compute `downgrade := msg.GetRoleDowngrade(); if downgrade == "" { downgrade = string(id.EffectiveRole) }`; `floored := auth.RoleMin(auth.Role(downgrade), id.EffectiveRole)`; add a `clampExpiry(reqTs, cfg.DefaultTTLDays, cfg.MaxTTLDays)` helper returning the resolved expiry or a `CodeInvalidArgument` over-cap error; generate the new secret in the handler via `secret, phc, err := auth.GenerateAPIKeySecret()` (handler-owned secret, mirroring admin CreateAPIKey at :236 — storage never generates it, cursor #3); call `h.users.CreateAPIKeyForUser(ctx, &storage.APIKey{UserID: id.UserID, RoleDowngrade: string(floored), Label: msg.GetLabel(), ExpiresAt: expiry, PHCHash: phc}, cfg.Quota)` (returns `(*APIKey, error)` — no secret return); return the plaintext via `auth.FormatAPIKeyToken(created.Prefix, secret)` exactly once. (Addresses review concern cursor #2 / RESEARCH V7 — structured audit emission.) After a successful create AND after a successful rotate, emit a structured audit log line via `h.logger.InfoContext(ctx, "apikey.self.create"|"apikey.self.rotate", slog.String("actor", id.UserID), slog.String("key_id", created.ID), slog.String("action", "create"|"rotate"))`. The audit line MUST NOT include the plaintext secret and MUST NOT include the raw user-supplied `label` verbatim (treat `label` as untrusted — a log-injection / PII vector per RESEARCH §6): omit the label from the audit line entirely (log only server-derived fields — actor, key id, action). For rotate: read the old key via `h.users.GetAPIKeyForUser(ctx, id.UserID, msg.GetKeyId())`, compute `floored := auth.RoleMin(auth.Role(old.RoleDowngrade), id.EffectiveRole)` and a defaulted ≤-cap expiry (a ttl-less rotate defaults to 90d, does NOT inherit the old window), generate a fresh `secret, phc, err := auth.GenerateAPIKeySecret()`, build an explicit `newKey := &storage.APIKey{PHCHash: phc, RoleDowngrade: string(floored), ExpiresAt: expiry}`, then `h.users.RotateAPIKeyForUser(ctx, id.UserID, msg.GetKeyId(), newKey)` (returns `(*APIKey, error)`; return the plaintext via `auth.FormatAPIKeyToken(rotated.Prefix, secret)` once); map storage sentinels via `h.identityError`. Wire the `SelfServiceKeysConfig` into the handler: add a `selfKeys SelfServiceKeysConfig` field (plus the `selfMintLimiter` sync.Map) to the `IdentityHandler` struct (:21) and grow the `RegisterIdentityService` constructor (:29) to accept the config — then update its SOLE production call site `cmd/specgraph/serve.go:203` (`server.RegisterIdentityService(mux, res.authStore, opts, maxBytes)`) to pass the resolved config from `cfg`, and the two internal/server `_test.go` call sites. Because the serve.go break is NOT caught by `go test ./internal/server/`, Task verify includes `go build ./...`. Do NOT place any literal role-name examples in comments that a negative gate would match. Create `internal/server/identity_selfkeys_test.go` covering: source-gate rejection, laundering floor on create (admin Role + reader EffectiveRole → reader ceiling), rotate re-floor (no stale ceiling), expiry over-cap → InvalidArgument, rate-limit → ResourceExhausted. Use the fake UsersBackend and assert on connect codes, not message strings.
  </action>
  <verify>
    <automated>go test ./internal/server/ -run 'TestCreateMyAPIKey|TestRotateMyAPIKey|TestSelfMint_RejectsApikeySource|TestSelfMint_ExpiryCap|TestSelfMint_AuditLogged' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - CreateMyAPIKey and RotateMyAPIKey derive owner from auth.IdentityFromContext and reject Source=="apikey" with CodePermissionDenied
    - Both apply auth.RoleMin flooring; test proves Identity{Role:admin, EffectiveRole:reader} yields a reader-ceiling key on create and rotate
    - Expiry over MaxTTL returns CodeInvalidArgument; rate-limit returns CodeResourceExhausted
    - Create and rotate emit a structured audit line (actor = self identity, key id, action); `TestSelfMint_AuditLogged` (constructs the handler with a buffer-backed slog.Logger) asserts the actor/key_id/action fields are present AND the plaintext secret and the raw label are absent from the log output
    - The grown RegisterIdentityService constructor is threaded through cmd/specgraph/serve.go:203 (and the internal/server test call sites), so `go build ./...` stays green
    - `go test ./internal/server/ -run 'TestCreateMyAPIKey|TestRotateMyAPIKey|TestSelfMint_RejectsApikeySource|TestSelfMint_ExpiryCap|TestSelfMint_AuditLogged' && go build ./...` exits 0
  </acceptance_criteria>
  <done>The mint pair enforces owner-from-context, anti-key-chaining, the RoleMin floor on create+rotate, expiry cap, rate limit, and quota — proven by adversarial tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ListMyAPIKeys + RevokeMyAPIKey (owner-scoped read/revoke)</name>
  <files>internal/server/identity_handler.go, internal/server/identity_selfkeys_test.go</files>
  <read_first>
    - internal/server/identity_handler.go (ListAPIKeys at :313-324 — msg.GetUserId() passed straight through returns ALL users' keys when empty; RevokeAPIKey at :266)
    - .planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md (Pitfall 3 ListMyAPIKeys leaks all keys; Finding F4 revoke idempotency)
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md ("Owner from context" pattern; RevokeMyAPIKey/ListMyAPIKeys note)
  </read_first>
  <behavior>
    - ListMyAPIKeys returns only the caller's keys — hard-set storage.ListAPIKeysFilter.UserID from context, never from the request
    - RevokeMyAPIKey revokes only the caller's key; foreign/missing → CodeNotFound; re-revoking own key is an idempotent success
    - A successful RevokeMyAPIKey emits a structured audit line (actor = self identity, key id, action=revoke) with no secret or raw label
  </behavior>
  <action>
    Add `ListMyAPIKeys` and `RevokeMyAPIKey` to `internal/server/identity_handler.go`. `ListMyAPIKeys`: derive owner from `auth.IdentityFromContext`; call `h.users.ListAPIKeys(ctx, storage.ListAPIKeysFilter{UserID: id.UserID})` with the UserID HARD-SET from context (never `msg.GetUserId()` — there is no such field on the self request); map results to proto. `RevokeMyAPIKey`: derive owner from context; call `h.users.RevokeAPIKeyForUser(ctx, id.UserID, msg.GetKeyId())`; map `storage.ErrAPIKeyNotFound` → `CodeNotFound` via `h.identityError`. On a successful revoke, emit a structured audit line via `h.logger.InfoContext(ctx, "apikey.self.revoke", slog.String("actor", id.UserID), slog.String("key_id", msg.GetKeyId()), slog.String("action", "revoke"))` (server-derived fields only — never the secret or a raw label; RESEARCH §6). Both reject missing identity with `CodeUnauthenticated`. Add invariant tests to `internal/server/identity_selfkeys_test.go`: `TestListMyAPIKeys_ScopedToCaller` (seed keys for two users in the fake, assert only the caller's are returned), `TestRevokeMyAPIKey_ForeignKeyNotFound` (revoking another user's key → CodeNotFound), and `TestRevokeMyAPIKey_AuditLogged` (buffer-backed slog.Logger; a successful revoke emits an audit line carrying the actor + key id + action and no secret/label).
  </action>
  <verify>
    <automated>go test ./internal/server/ -run 'TestListMyAPIKeys_ScopedToCaller|TestRevokeMyAPIKey'</automated>
  </verify>
  <acceptance_criteria>
    - ListMyAPIKeys sets ListAPIKeysFilter.UserID from auth.IdentityFromContext and never reads a user id from the request message
    - TestListMyAPIKeys_ScopedToCaller proves no cross-user leak
    - RevokeMyAPIKey returns CodeNotFound for a foreign/missing key
    - A successful revoke emits a structured audit line (actor/key_id/action=revoke) with no secret or raw label — proven by TestRevokeMyAPIKey_AuditLogged
    - `go test ./internal/server/ -run 'TestListMyAPIKeys_ScopedToCaller|TestRevokeMyAPIKey'` exits 0
  </acceptance_criteria>
  <done>List and revoke are strictly owner-scoped; the cross-user-leak invariant is enforced and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount CSRF validator on the Connect handler + map ErrQuotaExceeded</name>
  <files>internal/server/identity_handler.go, cmd/specgraph/serve.go, internal/server/identity_selfkeys_test.go</files>
  <read_first>
    - internal/server/identity_handler.go (RegisterIdentityService at :29-36 — `path, h := specgraphv1connect.NewIdentityServiceHandler(...); mux.Handle(path, h)`; this task wraps `h` with the CSRF validator BEFORE mux.Handle. identityError switch at :61-75 — add the quota case here)
    - internal/server/csrf.go (Plan 03 — the `csrfValidate(next http.Handler) http.Handler` middleware to mount; it self-scopes to cookie-authed POSTs on the four self-key procedures and exempts Bearer callers)
    - internal/auth/interceptor.go (:57-81 — the Connect session path the mounted validator sits in front of; specgraph_session read here, NOT via cookieToAuthHeader)
    - cmd/specgraph/serve.go (:203 RegisterIdentityService call site — already updated in Task 1 for the config param; no additional change needed here unless the mount is threaded through serve.go)
    - internal/storage/errors.go (storage.ErrQuotaExceeded sentinel from Plan 02)
  </read_first>
  <behavior>
    - A cookie-authenticated POST to CreateMyAPIKey/ListMyAPIKeys/RotateMyAPIKey/RevokeMyAPIKey without a matching X-CSRF-Token → HTTP 403 (validator is actually mounted, not just defined)
    - A Bearer-authenticated request to the same procedures is unaffected (CLI path stays green)
    - storage.ErrQuotaExceeded surfaces as connect.CodeResourceExhausted (not CodeInternal)
  </behavior>
  <action>
    (Addresses review concern cursor #1 — CSRF was defined but never wired; cursor #4 — quota code mapping.) In `RegisterIdentityService` (`internal/server/identity_handler.go:29`), wrap the Connect handler with the Plan 03 CSRF validator before mounting: `path, h := specgraphv1connect.NewIdentityServiceHandler(handler, opts...); mux.Handle(path, csrfValidate(h))`. This mounts the double-submit CSRF check in front of the Connect IdentityService session path (the interceptor reads `specgraph_session` at `interceptor.go:57-81`; do NOT route CSRF through the REST `cookieToAuthHeader` whoami path — cursor #2). The validator itself exempts Bearer-authenticated (CLI/MCP) requests and only enforces on cookie-authed POSTs to the four self-key procedures, so admin RPCs and CLI usage are unaffected. Extend the `identityError` switch (`:61-75`) with `case errors.Is(err, storage.ErrQuotaExceeded): return connect.NewError(connect.CodeResourceExhausted, errors.New("api key quota exceeded"))` so the quota-safe mint's storage error maps to `CodeResourceExhausted` rather than falling through to `CodeInternal`. (The per-identity rate-limit path already returns a `connect.Error` directly, which `identityError` passes through via `errors.As`.) Add tests to `internal/server/identity_selfkeys_test.go`: `TestSelfMint_CSRFMountRejectsMissingToken` (a cookie-authed self-key POST through the mounted handler without X-CSRF-Token → 403; a Bearer request is exempt) and `TestCreateMyAPIKey_QuotaExceededMapsResourceExhausted` (fake CreateAPIKeyForUser returns storage.ErrQuotaExceeded → handler returns connect.CodeResourceExhausted).
  </action>
  <verify>
    <automated>go test ./internal/server/ -run 'TestSelfMint_CSRFMount|TestCreateMyAPIKey_QuotaExceeded' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - RegisterIdentityService wraps the Connect handler with csrfValidate before mux.Handle (grep: `csrfValidate` appears in internal/server/identity_handler.go)
    - A cookie-authed self-key POST without a valid X-CSRF-Token returns 403; a Bearer-authed request to the same path is exempt
    - identityError maps storage.ErrQuotaExceeded → connect.CodeResourceExhausted
    - `go test ./internal/server/ -run 'TestSelfMint_CSRFMount|TestCreateMyAPIKey_QuotaExceeded' && go build ./...` exits 0
  </acceptance_criteria>
  <done>The CSRF validator is actually mounted on the Connect IdentityService handler (the threat_model's CSRF mitigation is real, not aspirational), and quota exhaustion maps to CodeResourceExhausted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → self RPC | Owner, role floor, and source are all enforced server-side; nothing about the target is client-trusted |
| caller identity → minted key ceiling | The role floor bounds the minted key at the caller's live effective role |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Elevation of Privilege | role_downgrade laundering (downgraded apikey mints owner-role key) | critical | mitigate | RoleMin(requestedOrInherit, caller.EffectiveRole) floor on create AND rotate; inherit resolves to EffectiveRole before RoleMin |
| T-02-15 | Elevation / Persistence | Key-chaining (leaked non-admin key self-perpetuates) | high | mitigate | Reject Source=="apikey" on CreateMyAPIKey/RotateMyAPIKey with CodePermissionDenied |
| T-02-16 | Information Disclosure | ListMyAPIKeys leaks all users' keys via empty filter | high | mitigate | Hard-set ListAPIKeysFilter.UserID from context; TestListMyAPIKeys_ScopedToCaller invariant |
| T-02-17 | Denial of Service | argon2id compute DoS via mint/rotate spam | high | mitigate | Per-identity golang.org/x/time/rate limiter → CodeResourceExhausted |
| T-02-18 | Elevation (latency) | Over-long expiry extends stale privilege | high | mitigate | clampExpiry enforces 90d default / 180d max (D-08) → CodeInvalidArgument over cap |
| T-02-19 | Information Disclosure | Secret leakage / re-serialization | medium | mitigate | Emit-once plaintext via FormatAPIKeyToken; never re-returned; label treated as untrusted in logs |
| T-02-31b | Tampering (CSRF) | Connect self-key POSTs unprotected because middleware defined but not mounted | high | mitigate | csrfValidate mounted on the Connect IdentityService handler in RegisterIdentityService (cursor #1); cookie-authed self-key POSTs require a matching X-CSRF-Token, Bearer/CLI exempt; makes Plan 03's double-submit mitigation real |
| T-02-32 | Denial of Service (error opacity) | Quota exhaustion masked as CodeInternal | medium | mitigate | identityError maps storage.ErrQuotaExceeded → CodeResourceExhausted (cursor #4), giving the client an actionable, retryable-after-revoke signal |
| T-02-33 | Repudiation / Log Injection | Missing or unsafe audit trail on self create/rotate/revoke | medium | mitigate | Structured audit line on every successful self create/rotate/revoke (actor = self identity from auth.IdentityFromContext, key id, action); server-derived fields only — the plaintext secret and the raw user-supplied label are NEVER logged, closing both the no-trail (repudiation) and the label-as-log-injection/PII vector (RESEARCH §6 / V7, cursor #2) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Phase-wide inventory (this plan delivers the **bolded** items):

- Proto/RPCs: CreateMyAPIKey, ListMyAPIKeys, RotateMyAPIKey, RevokeMyAPIKey, ResyncUserRole + messages
- Auth: exported `auth.RoleMin`; Cedar verb `apikey.self`; base.cedar permit; procedure→action map; drift test
- Storage: `GetAPIKeyForUser`, `RevokeAPIKeyForUser`, `RotateAPIKeyForUser`, quota-safe `CreateAPIKeyForUser`; sentinel `ErrQuotaExceeded`
- Server: **four self handlers (CreateMyAPIKey/ListMyAPIKeys/RotateMyAPIKey/RevokeMyAPIKey); `selfMintLimiter`; `clampExpiry`**; `ResyncUserRole` handler; CSRF double-submit middleware
- Config: self-service key-policy struct (default 90d / max 180d / quota 10 / rate-limit)
- CLI: `auth api-key create/list/rotate/revoke` self-variants; session-preferring resolver (Finding D); `auth user resync <user-id> --role <r> [--revoke-keys]`
- Web: `web/src/routes/keys/+page.svelte`, `web/src/lib/components/RevealKeyModal.svelte`, `web/src/lib/keys.svelte.ts`, `identityClient` + CSRF interceptor
</artifacts_produced>

<verification>
- `go test ./internal/server/ -run 'MyAPIKey|SelfMint'` green (source gate, floor create+rotate, list scoping, expiry cap, rate limit, CSRF mount 403, quota→ResourceExhausted).
- Handler uses auth.RoleMin, owner-scoped storage methods (handler-owned secret), and SelfServiceKeysConfig.
- csrfValidate is mounted on the Connect IdentityService handler; identityError maps ErrQuotaExceeded → CodeResourceExhausted.
</verification>

<success_criteria>
SC#1 (self-provision without borrowing admin key) and SC#2 (role capped at caller's current role, no laundering) are met at the server boundary with adversarial test coverage.
</success_criteria>

<output>
Create `.planning/phases/02-api-key-lifecycle-self-service/02-05-SUMMARY.md` when done.
</output>
