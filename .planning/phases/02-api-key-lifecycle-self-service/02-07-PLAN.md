---
phase: 02-api-key-lifecycle-self-service
plan: 07
type: execute
wave: 4
depends_on: ["02-01", "02-05"]
files_modified:
  - cmd/specgraph/auth_apikey.go
  - cmd/specgraph/client.go
  - cmd/specgraph/auth_apikey_test.go
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "`auth api-key create/list/rotate/revoke` with no --user calls the self RPCs; with --user <other> keeps the existing admin path"
    - "Self-key commands authenticate with the stored spgr_ws_ session and ignore/warn on a set SPECGRAPH_API_KEY (Finding D)"
    - "Self create/rotate print the plaintext exactly once with a secret-manager instruction and never write a credential file"
  artifacts:
    - cmd/specgraph/auth_apikey.go
    - cmd/specgraph/client.go
    - cmd/specgraph/auth_apikey_test.go
  key_links:
    - "self commands → CreateMyAPIKey/ListMyAPIKeys/RotateMyAPIKey/RevokeMyAPIKey (Plan 05 handlers)"
    - "session-preferring resolver → avoids the Source==apikey hard-fail on a dev box (Finding D)"
---

<objective>
Ship the AUTH-03 CLI surface: self-variants of `auth api-key create/list/rotate/revoke` that route to the new self RPCs when no `--user` is given (and keep the existing admin path when `--user <other>` is passed), plus the mandatory CLI auth-precedence fix (Finding D) so self-mint authenticates with the stored OIDC session rather than `SPECGRAPH_API_KEY` — which would otherwise hard-fail `PermissionDenied` under the `Source=="apikey"` gate on a normal dev box.

Purpose: Deliver SC#1's CLI half — a user self-provisions without borrowing the bootstrap admin key — and remove the Finding-D footgun.
Output: Self CLI variants + session-preferring resolver + tests.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-key-lifecycle-self-service/02-CONTEXT.md
@.planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md
@.planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md
@cmd/specgraph/auth_apikey.go
@cmd/specgraph/client.go
@cmd/specgraph/login.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session-preferring resolver (Finding D) in client.go</name>
  <files>cmd/specgraph/client.go</files>
  <read_first>
    - cmd/specgraph/client.go (resolveAPIKey at :102-111 — env-key-before-session precedence; newAuthenticatedHTTPClient at :123 where the token is injected)
    - cmd/specgraph/login.go (stored spgr_ws_ session token at :257; resolveSession)
    - .planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md (Finding D / Pitfall 6 — self-mint must use the session, warn if SPECGRAPH_API_KEY is set)
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md ("cmd/specgraph/client.go — MODIFY (Finding D)")
  </read_first>
  <behavior>
    - A session-preferring resolver returns the stored spgr_ws_ session credential, ignoring SPECGRAPH_API_KEY
    - When SPECGRAPH_API_KEY is set and a self command uses the session resolver, a warning is emitted to stderr
    - The existing default resolver (env-first) is unchanged for admin/other commands
  </behavior>
  <action>
    Add a session-preferring credential path used only by the self-key commands: either a new resolver (e.g. `resolveSessionCredential(serverURL) string`) that loads the credentials file session token (the `spgr_ws_` token, `login.go:257`) directly and does NOT consult `SPECGRAPH_API_KEY`, plus a constructor variant of `newAuthenticatedHTTPClient` that uses it — or a flag on the existing client builder selecting session-preference. When `SPECGRAPH_API_KEY` is set while a self command runs, emit a warning to stderr that the env key is being ignored in favor of the session (so the `Source=="apikey"` gate does not surprise the user). Leave the existing env-first `resolveAPIKey` untouched for admin/other commands.
  </action>
  <verify>
    <automated>go build ./cmd/specgraph/ && go test ./cmd/specgraph/ -run 'TestSelfMint_SessionPrecedence'</automated>
  </verify>
  <acceptance_criteria>
    - A session-preferring resolver exists that ignores SPECGRAPH_API_KEY and returns the stored session credential
    - Setting SPECGRAPH_API_KEY during a self command produces a stderr warning
    - The default env-first resolveAPIKey remains unchanged for non-self commands
    - `go test ./cmd/specgraph/ -run 'TestSelfMint_SessionPrecedence'` exits 0
  </acceptance_criteria>
  <done>Self-key commands authenticate via the stored session and warn on a set env key, closing the Finding-D footgun.</done>
</task>

<task type="auto">
  <name>Task 2: Self-variants of auth api-key create/list/rotate/revoke + tests</name>
  <files>cmd/specgraph/auth_apikey.go, cmd/specgraph/auth_apikey_test.go</files>
  <read_first>
    - cmd/specgraph/auth_apikey.go (authAPIKeyCreateCmd at :66-101 emit-once "store this token now" line at :98; Rotate at :120; Revoke at :103; List at :44; parseExpiresAt at :31; flag wiring in init() at :157-171)
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md ("cmd/specgraph/auth_apikey.go — MODIFY (self variants)")
    - docs/superpowers/specs/2026-06-16-spgr-g7st-self-service-api-keys-design.md (§4 delivery: emit once, secret-manager instruction, never write a credential file)
  </read_first>
  <behavior>
    - No --user flag → the command calls the corresponding CreateMyAPIKey/ListMyAPIKeys/RotateMyAPIKey/RevokeMyAPIKey RPC via the session-preferring client
    - --user <other> → the existing admin RPC path (CreateAPIKey/ListAPIKeys/RotateAPIKey/RevokeAPIKey) is used unchanged
    - Self create/rotate print the plaintext once with a secret-manager instruction; nothing is written to disk
  </behavior>
  <action>
    In `cmd/specgraph/auth_apikey.go`, branch each of create/list/rotate/revoke on whether `--user` is empty. When empty (self path): build the client via the session-preferring path from Task 1 and call `CreateMyAPIKey` (fields label/role_downgrade/expires_at, NO user_id), `ListMyAPIKeys`, `RotateMyAPIKey` (key_id + optional expires_at), or `RevokeMyAPIKey` (key_id). When `--user <other>` is set, keep the existing admin RPC calls unchanged. Reuse `parseExpiresAt`, the `printJSON`/render output split, and the emit-once "store this token now / it will not be shown again" line for self create+rotate, with a note to store it in a secret manager or `${SPECGRAPH_API_KEY}` shell profile — the command MUST NOT run `export` or write a credential file. Preserve the existing flags (`--label`, `--role-downgrade`, `--expires-at`) for the self path. In the self-path command help/usage text, note the behavior change from admin mint (cursor #7 LOW): a self-minted key's expiry is MANDATORY with a server default of 90d and a hard max of 180d (D-08) — unlike admin `CreateAPIKey` where `expires_at` is optional (`--expires-at` omitted → 90d default, values over the server max are rejected by the handler). Add `cmd/specgraph/auth_apikey_test.go` cases asserting: no-`--user` create invokes CreateMyAPIKey (fake client records it); `--user other` create invokes the admin CreateAPIKey; self create prints the plaintext exactly once.
  </action>
  <verify>
    <automated>go test ./cmd/specgraph/ -run 'TestAuthAPIKey' && go build ./cmd/specgraph/</automated>
  </verify>
  <acceptance_criteria>
    - create/list/rotate/revoke route to the self RPCs when --user is empty and to admin RPCs when --user is set
    - Self create/rotate emit the plaintext once with a secret-manager instruction and write no file
    - Self-path help text documents the mandatory 90d-default / 180d-max expiry (behavior change from admin optional expiry, cursor #7)
    - `go test ./cmd/specgraph/ -run 'TestAuthAPIKey'` exits 0
  </acceptance_criteria>
  <done>The four self CLI variants work via the session-preferring client, preserving the admin path under --user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI credential selection → server | Which stored credential authenticates the self-mint (session vs env key) |
| terminal → secret delivery | Emit-once plaintext must not persist to disk/history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-24 | Elevation of Privilege | Self-mint accidentally authenticating as bootstrap admin key | high | mitigate | Session-preferring resolver ignores SPECGRAPH_API_KEY for self commands; Source==apikey gate would else hard-fail (Finding D) |
| T-02-25 | Information Disclosure | Secret leaking into shell history / disk | medium | mitigate | Emit-once print; secret-manager instruction; never runs export or writes a credential file |
| T-02-26 | Spoofing | --user path silently used for self intent | low | accept | Explicit --user selects the admin path by design; documented in help text |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Phase-wide inventory (this plan delivers the **bolded** items):

- Proto/RPCs: CreateMyAPIKey, ListMyAPIKeys, RotateMyAPIKey, RevokeMyAPIKey, ResyncUserRole + messages
- Auth: exported `auth.RoleMin`; Cedar verb `apikey.self`; base.cedar permit; procedure→action map; drift test
- Storage: `GetAPIKeyForUser`, `RevokeAPIKeyForUser`, `RotateAPIKeyForUser`, quota-safe `CreateAPIKeyForUser`; sentinel `ErrQuotaExceeded`
- Server: four self handlers; `ResyncUserRole` handler; `selfMintLimiter`; `clampExpiry`; CSRF double-submit middleware
- Config: self-service key-policy struct (default 90d / max 180d / quota 10 / rate-limit)
- CLI: **`auth api-key create/list/rotate/revoke` self-variants; session-preferring resolver (Finding D)**; `auth user resync <user-id> --role <r> [--revoke-keys]`
- Web: `web/src/routes/keys/+page.svelte`, `web/src/lib/components/RevealKeyModal.svelte`, `web/src/lib/keys.svelte.ts`, `identityClient` + CSRF interceptor
</artifacts_produced>

<verification>
- `go test ./cmd/specgraph/ -run 'TestAuthAPIKey|TestSelfMint_SessionPrecedence'` green.
- Self path uses the session-preferring client; admin path preserved under --user.
</verification>

<success_criteria>
SC#1 CLI half: a user self-provisions/lists/rotates/revokes their own key from the CLI using their session, never the bootstrap admin key.
</success_criteria>

<output>
Create `.planning/phases/02-api-key-lifecycle-self-service/02-07-SUMMARY.md` when done.
</output>
