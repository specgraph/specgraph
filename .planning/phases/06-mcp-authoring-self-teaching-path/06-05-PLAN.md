---
phase: 06-mcp-authoring-self-teaching-path
plan: 05
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - e2e/api/mcp_only_authoring_test.go
  - e2e/api/skills_test.go
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "An MCP-client-only run (no CLI/ConnectRPC service client) can read `specgraph://prime`, author the constitution to persistence, and drive a spec spark→shape→specify→decompose→approve via friendly YAML `output` plus JSON `exchanges` to approved."
    - "`specgraph://prime` returns the empty-state constitution hint (MCP routing text) on a fresh project."
    - "The post-spark stages are exercised with canonical minimal `exchanges` JSON fixtures (role/content/stage/sequence), proving the exchanges self-teaching path end-to-end through the real handler."
  artifacts:
    - e2e/api/mcp_only_authoring_test.go
  key_links:
    - "The spec calls ONLY mcp-go client methods (ReadResource/CallTool) — never constructs a specgraphv1connect service client (the 'no CLI' simulation, D-08)."
    - "Constitution + funnel tool inputs are friendly YAML matching the plan-04 handlers and plan-02 skills."
---

<objective>
Add the automated MCP-only e2e that is the phase gate (D-08): it drives the full authoring path through the MCP surface only — `specgraph://prime` → `specgraph_skills_get` → `constitution` tool (friendly YAML) → `author` spark→shape→specify→decompose→approve (friendly YAML `output` + explicit JSON `exchanges` on post-spark stages) — with NO CLI/ConnectRPC service client constructed, and asserts the constitution persists and the spec reaches approved. It also smoke-asserts `specgraph://prime` returns the empty-state MCP routing hint on a fresh project (the research prime-reliability confirmation), and fixes the `skills_test.go` skill-list assertion to include `specgraph-constitution` (review finding #4). This proves criteria #2, #3, and prime-reliability are regression-proof with no CLI fallback possible.

Purpose: Lock the MCP-only self-teaching path behind an automated gate that cannot pass via the CLI.
Output: `e2e/api/mcp_only_authoring_test.go` (Ginkgo, `//go:build e2e`).
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-RESEARCH.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-PATTERNS.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-VALIDATION.md
@e2e/api/skills_test.go
@e2e/api/prime_cross_surface_test.go
</context>

<artifacts_this_phase_produces>
NEW symbols created by THIS plan (exclude from source-grounding drift checks):
- New file `e2e/api/mcp_only_authoring_test.go` and its Ginkgo `Describe`/`It` blocks (label `MCPOnly`), including canonical `exchanges` JSON-array string constants for shape/specify/decompose.
- Reuses (does NOT redefine) existing helpers `skillsMCPClient`, `toolText` (skills_test.go), `mcpResourceText` (prime_cross_surface_test.go).
- Modified `e2e/api/skills_test.go`: skill-list assertion + `It` description updated to include `specgraph-constitution` (review finding #4).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: MCP-only authoring e2e (prime smoke + constitution + full funnel)</name>
  <read_first>
    - e2e/api/skills_test.go (skillsMCPClient harness lines 33-58; toolText lines 63-73 — reuse verbatim, do not redefine; ALSO the "lists six skills" assertion at lines 106-121 that omits `specgraph-constitution` — fix per review finding #4)
    - e2e/api/prime_cross_surface_test.go (mcpResourceText helper lines ~203-211; the ConnectRPC service-client usage that this spec must AVOID)
    - e2e/api/helpers_test.go (canonical ConversationExchange fixtures at lines 117-119, 137-139, 156-158 — copy the role/content/stage/sequence shape for the MCP exchanges JSON args)
    - internal/server/authoring_handler.go (approve ACCEPT path lines ~480-485 does NOT validate exchanges; only the REJECT path line ~488 requires them — so the e2e approve-accept call passes slug only)
    - internal/mcp/tools_core.go (constitution tool actions get/update — post plan 04 friendly YAML)
    - internal/mcp/tools_authoring.go (author tool actions + friendly `output` — post plan 04)
  </read_first>
  <files>e2e/api/mcp_only_authoring_test.go, e2e/api/skills_test.go</files>
  <action>
    Create `e2e/api/mcp_only_authoring_test.go` with `//go:build e2e`, `package api_test`, Apache-2.0 SPDX header, and a Ginkgo `Describe` labelled so `go test -tags e2e ./e2e/api/ -run MCPOnly` selects it. Use the existing `skillsMCPClient(ctx)` harness (in-process real MCP server) and call ONLY `mcpCli.ReadResource` / `mcpCli.CallTool` — never construct a `specgraphv1connect.*ServiceClient` (that is the "no CLI" simulation, D-08). Steps: (1) `ReadResource specgraph://prime` → assert non-error and, for a fresh/empty-constitution project, the empty-state MCP routing hint substring from plan 03 (prime-reliability smoke). (2) `CallTool specgraph_skills_get name=specgraph-constitution` → assert non-error via `toolText`. (3) `CallTool constitution action:update` with a friendly-YAML `constitution` value (`layer: project`, name, constraints, at least one principle) → assert persists; follow with `constitution action:get` and assert the written name/layer round-trips. (4) `CallTool author` for a unique slug through spark→shape→specify→decompose→approve. spark passes friendly snake_case YAML `output` (seed/signal/scope_sniff) with NO exchanges. shape/specify/decompose each pass friendly snake_case YAML `output` (shape scope_in/chosen_approach/success_must; specify invariants/verify_criteria; decompose strategy/slices) AND an explicit `exchanges` JSON-array arg — define these as canonical string constants in the test file matching `helpers_test.go:117-119` shape: a two-element array `[{"role":"probe","content":"…","stage":"shape","sequence":1},{"role":"response","content":"…","stage":"shape","sequence":2}]` (and `stage:"specify"` / `stage:"decompose"` for the respective stages). approve passes the slug ONLY (the ACCEPT path does not require exchanges — confirmed against authoring_handler.go). Assert the final approve result reflects the approved stage. Use `toolText` (asserts `!IsError`) for every tool call. Reuse `mcpResourceText` for resource bodies. Add a pre-flight `It` that calls `author action:shape` with valid friendly `output` but NO `exchanges` and asserts it FAILS (IsError) — documenting the contract the skill must teach (server requires ≥1 exchange; this also exercises the real ValidateExchanges rejection path that the 06-04 mock-backend unit tests cannot). Derive a per-run unique slug to avoid Postgres unique-slug collisions.
    ALSO fix review finding #4: in `e2e/api/skills_test.go`, add `"specgraph-constitution"` to the skill-list assertion (lines ~112-119) and rename/update the "lists six skills" `It` description to reflect seven skills — this is verified by the same e2e run.
  </action>
  <verify>
    <automated>go test -tags e2e ./e2e/api/ -run MCPOnly</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/api/mcp_only_authoring_test.go` exists with `//go:build e2e`, `package api_test`, SPDX header.
    - The spec constructs NO `specgraphv1connect` service client — only `mcpCli` ReadResource/CallTool calls.
    - `specgraph://prime` read asserts non-error + the empty-state MCP routing hint on a fresh project.
    - Constitution friendly-YAML `update` persists and round-trips via `get`.
    - The funnel drives spark→shape→specify→decompose→approve via friendly YAML `output`; shape/specify/decompose pass explicit canonical `exchanges` JSON fixtures (role/content/stage/sequence); approve passes slug only; the spec reaches approved.
    - A pre-flight `It` asserts `author action:shape` with valid `output` but no `exchanges` fails (IsError), exercising the real server ValidateExchanges rejection.
    - `e2e/api/skills_test.go` skill-list assertion includes `specgraph-constitution` (seven skills) and its `It` description is updated.
    - `go test -tags e2e ./e2e/api/ -run MCPOnly` exits 0, and a full `go test -tags e2e ./e2e/api/` run stays green after the plan-03 golden/layout changes (Docker/testcontainers).
  </acceptance_criteria>
  <done>The MCP-only authoring e2e gate passes end-to-end using only the MCP client surface — no CLI/ConnectRPC fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → in-process MCP server | The e2e exercises the real MCP write boundary; it is the verification that friendly-YAML parsing + enum rejection behave correctly end-to-end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | friendly-YAML write boundary (verified) | high | mitigate | This e2e is the end-to-end verification that valid friendly YAML persists and the funnel reaches approved; the invalid-enum rejection is unit-verified in plans 01/04. The pre-flight `It` (valid `output`, no `exchanges`) asserts the real server `ValidateExchanges` rejects a missing-exchanges post-spark call, closing the V5 input-validation gap the mock-backend unit tests cannot exercise. |
| T-06-03 | Information Disclosure | prime resource | low | accept | Prime smoke asserts a sanitized empty-state hint (no raw internal error) on a fresh project, confirming the research prime-reliability finding. |
</threat_model>

<verification>
- `go test -tags e2e ./e2e/api/ -run MCPOnly` exits 0.
- Phase gate: `task pr-prep` (full check → integration → e2e) green before `/gsd-verify-work` (D-08 — no CLI-path fallback permitted).
</verification>

<success_criteria>
A single automated MCP-client-only e2e proves an agent can author the constitution to persistence and walk a spec to approved from `specgraph://prime` alone, with the CLI unavailable — the phase gate for MCP-01.
</success_criteria>

<output>
Create `.planning/phases/06-mcp-authoring-self-teaching-path/06-05-SUMMARY.md` when done.
</output>
