---
phase: 06-mcp-authoring-self-teaching-path
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/render/prime.go
  - internal/render/prime_test.go
  - internal/mcp/resources.go
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "`specgraph://prime` routes an MCP-only agent to the authoring skills (`specgraph_skills_get`/`list`/`search`) as the explicit next step."
    - "The fresh-init empty-state constitution hint points to the MCP skill/tool path, not `specgraph constitution set`."
  artifacts:
    - internal/render/prime.go
    - internal/mcp/resources.go
  key_links:
    - "writeSkills adds a 'start here' authoring routing line; writeProjectConstitution empty-state + constitutionEmptyResource route to MCP, not CLI."
    - "The prime golden-layout test (TestRenderProjectMarkdown_NoProvenance_MatchesExistingLayout) is updated to the new wording."
---

<objective>
Make `specgraph://prime` a reliable ENTRY POINT that routes an MCP-only agent to the authoring skills as the next step, and fix the fresh-init empty-state hints so they point to the MCP path instead of the CLI (D-10). This is a minimal render/resource-layer change — prime does NOT duplicate the interface teaching (the skills carry that depth). The research prime-load-failure finding confirmed the discuss-session failure was environmental (no DB/server), not a prime code bug; no prime handler fix is required here — plan 05 adds the empty-state smoke assertion.

Purpose: Close the routing gap so a fresh-init agent starting from only `specgraph://prime` discovers the authoring skills (criterion #2) and is never sent to a CLI it does not have.
Output: Updated `internal/render/prime.go`, its golden test, and `internal/mcp/resources.go`.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-RESEARCH.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-PATTERNS.md
@internal/render/prime.go
@internal/mcp/resources.go
</context>

<artifacts_this_phase_produces>
NEW symbols created by THIS plan (exclude from source-grounding drift checks):
- No new functions. Modified content: `writeSkills` routing line, `writeProjectConstitution` empty-state string (internal/render/prime.go), `constitutionEmptyResource` string (internal/mcp/resources.go), and the updated golden expectation in internal/render/prime_test.go.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Prime render routing + empty-state MCP hint (+ golden test)</name>
  <read_first>
    - internal/render/prime.go (writeSkills lines ~306-315; writeProjectConstitution empty-state lines ~206-212)
    - internal/render/prime_test.go (TestRenderProjectMarkdown_NoProvenance_MatchesExistingLayout — byte-for-byte golden; Pitfall 3)
  </read_first>
  <files>internal/render/prime.go, internal/render/prime_test.go</files>
  <action>
    In `writeSkills`, after the existing catalog sentence, add one explicit "start here" routing sentence directing an MCP-only agent to fetch `specgraph_skills_get name=specgraph-constitution` / `specgraph-authoring` first to author the constitution or a spec (D-10). In `writeProjectConstitution` empty-state branch, replace the `Run \`specgraph constitution set\`` hint with an MCP-first hint routing to the `constitution` MCP tool (`action: update`) and the `specgraph-constitution` skill. Then update the golden expectation in `internal/render/prime_test.go` (`TestRenderProjectMarkdown_NoProvenance_MatchesExistingLayout`) to match the new byte-for-byte output — the test asserts exact layout, so it MUST be updated in lockstep. Do not add DB-backed logic; empty-state stays soft (composer already treats absent constitution as soft-empty).
  </action>
  <verify>
    <automated>go test ./internal/render/</automated>
  </verify>
  <acceptance_criteria>
    - `writeSkills` output contains an authoring routing sentence naming `specgraph_skills_get` and the `specgraph-constitution`/`specgraph-authoring` skills.
    - `writeProjectConstitution` empty-state no longer contains `specgraph constitution set`; it references the `constitution` MCP tool / `specgraph-constitution` skill.
    - `go test ./internal/render/` passes (golden updated).
  </acceptance_criteria>
  <done>Prime routing line + empty-state MCP hint are in place and the golden layout test passes.</done>
</task>

<task type="auto">
  <name>Task 2: MCP constitution empty-resource routing</name>
  <read_first>
    - internal/mcp/resources.go (constitutionEmptyResource lines ~36-42 — identical CLI hint)
    - internal/render/prime.go (post-Task-1 empty-state wording — keep the two consistent)
  </read_first>
  <files>internal/mcp/resources.go</files>
  <action>
    Update `constitutionEmptyResource` so the empty-state markdown replaces `Run \`specgraph constitution set\`` with the same MCP-first routing used in Task 1 (point to the `constitution` MCP tool `action: update` and the `specgraph-constitution` skill), keeping the two surfaces consistent for a fresh-init agent (D-10).
  </action>
  <verify>
    <automated>go test ./internal/mcp/ && task check</automated>
  </verify>
  <acceptance_criteria>
    - `constitutionEmptyResource` text no longer contains `specgraph constitution set`; it routes to the MCP tool/skill.
    - `go test ./internal/mcp/` passes.
    - `task check` passes.
  </acceptance_criteria>
  <done>Prime + constitution empty-resource route MCP-only agents to the authoring skills/tool; golden test updated; `task check` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prime/render output → agent | Prime markdown is composed server-side from DB reads and rendered to the agent; this plan only changes static routing text. No new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Information Disclosure | prime resource handler | low | accept | Prime error path already returns sanitized `internal error` (execution_handler) and empty-state stays soft; this plan adds no new error surface. Verified reliable by plan 05 prime smoke. |
</threat_model>

<verification>
- `go test ./internal/render/ ./internal/mcp/` green.
- `task check` green.
</verification>

<success_criteria>
`specgraph://prime` and the constitution empty-resource route a fresh-init MCP-only agent to the authoring skills/tool; no CLI hint remains in the fresh-init path.
</success_criteria>

<output>
Create `.planning/phases/06-mcp-authoring-self-teaching-path/06-03-SUMMARY.md` when done.
</output>
