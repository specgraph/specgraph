---
phase: 06-mcp-authoring-self-teaching-path
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/render/prime.go
  - internal/render/prime_test.go
  - internal/mcp/resources.go
  - internal/mcp/resources_test.go
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "`specgraph://prime` routes an MCP-only agent to the authoring skills (`specgraph_skills_get`/`list`/`search`) as the explicit next step."
    - "The fresh-init empty-state constitution hint points to the MCP skill/tool path, not `specgraph constitution set` — on BOTH the ProjectView and SpecView prime surfaces (D-10)."
  artifacts:
    - internal/render/prime.go
    - internal/mcp/resources.go
  key_links:
    - "writeSkills adds a 'start here' authoring routing line; writeProjectConstitution AND writeSpecConstitution empty-states + constitutionEmptyResource route to MCP, not CLI."
    - "The project empty-state test (TestRenderProjectMarkdown_EmptyConstitution) is updated to the new wording, the populated-layout golden (`expectedProjectMatchLegacy`) is updated for the new writeSkills routing line, and a new SpecView empty-state test asserts the SpecView prime no longer emits `specgraph constitution` CLI text."
    - "`internal/mcp/resources_test.go` is in scope: the two `specgraph constitution set` assertions (`:170`, `:187`) are updated to the MCP-first routing text in lockstep with `constitutionEmptyResource`, so `task check` does not break (pass-3 review finding #2)."
---

<objective>
Make `specgraph://prime` a reliable ENTRY POINT that routes an MCP-only agent to the authoring skills as the next step, and fix the fresh-init empty-state hints so they point to the MCP path instead of the CLI (D-10). This is a minimal render/resource-layer change — prime does NOT duplicate the interface teaching (the skills carry that depth). The research prime-load-failure finding confirmed the discuss-session failure was environmental (no DB/server), not a prime code bug; no prime handler fix is required here — plan 05 adds the empty-state smoke assertion.

Purpose: Close the routing gap so a fresh-init agent starting from only `specgraph://prime` discovers the authoring skills (criterion #2) and is never sent to a CLI it does not have — including when it sparks a spec first and hits the SpecView constitution empty-state (D-10).
Output: Updated `internal/render/prime.go` (project + spec empty-states), its tests, and `internal/mcp/resources.go`.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-RESEARCH.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-PATTERNS.md
@internal/render/prime.go
@internal/mcp/resources.go
</context>

<artifacts_this_phase_produces>
NEW symbols created by THIS plan (exclude from source-grounding drift checks):
- No new functions. Modified content: `writeSkills` routing line, `writeProjectConstitution` empty-state string, and `writeSpecConstitution` empty-state string (internal/render/prime.go); `constitutionEmptyResource` string (internal/mcp/resources.go); the updated `expectedProjectMatchLegacy` golden const (routing line + 6→7 skill-count correction), the corrected `fixtureProjectView().SkillsCount` (6→7), the updated `TestRenderProjectMarkdown_EmptyConstitution` expectation, and a NEW `TestRenderSpecMarkdown_EmptyConstitution` test in internal/render/prime_test.go; the updated `specgraph constitution set` assertions in `TestConstitutionResource_NotFoundRendersHint` (resources_test.go:170) and `TestConstitutionResource_SlugRequiredRendersHint` (resources_test.go:187) in internal/mcp/resources_test.go (pass-3 review finding #2).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Prime render routing + empty-state MCP hint (project + spec) (+ tests)</name>
  <read_first>
    - internal/render/prime.go (writeSkills lines ~306-315; writeProjectConstitution empty-state lines ~206-212; writeSpecConstitution empty-state line ~326 — the SAME `specgraph constitution set` CLI hint on the SpecView surface)
    - internal/render/prime_test.go (TestRenderProjectMarkdown_EmptyConstitution ~lines 59-65 asserts the project empty-state CLI string; TestRenderProjectMarkdown_NoProvenance_MatchesExistingLayout ~line 67 is a byte-for-byte golden `require.Equal(expectedProjectMatchLegacy, got)` — CORRECTION (addresses review finding #2): `expectedProjectMatchLegacy` EMBEDS the `## Skills` section verbatim at lines 53-55, so adding a "start here" routing line to `writeSkills` WILL break this golden. The golden const MUST be updated in lockstep with the writeSkills change — see Task 1 action + acceptance criteria.)
    - internal/render/prime_test.go STALE SKILL COUNT (addresses pass-2 review finding #3): `fixtureProjectView()` sets `SkillsCount: 6` (~line 243) and `expectedProjectMatchLegacy` says "6 skills exposed via MCP" (~line 55), but the embedded catalog serves SEVEN skills (`internal/mcp/skills/embedded_test.go` `wantNames` lines 14-22; live composer sets `view.SkillsCount = len(metas)` at `internal/prime/composer.go:108`). Since this task already rewrites the golden for the routing line, correct the count 6→7 in BOTH `fixtureProjectView()` and the `expectedProjectMatchLegacy` literal in the same change.
  </read_first>
  <files>internal/render/prime.go, internal/render/prime_test.go</files>
  <action>
    In `writeSkills`, after the existing catalog sentence, add one explicit "start here" routing sentence directing an MCP-only agent to fetch `specgraph_skills_get name=specgraph-constitution` / `specgraph-authoring` first to author the constitution or a spec (D-10). Because the byte-for-byte golden `expectedProjectMatchLegacy` (internal/render/prime_test.go lines ~21-57) embeds the `## Skills` section verbatim, the new routing sentence WILL change that golden output — UPDATE `expectedProjectMatchLegacy` in the SAME change so `TestRenderProjectMarkdown_NoProvenance_MatchesExistingLayout` reflects the new populated-layout Skills text (addresses review finding #2; do not skip this or `task check` surprise-fails). In BOTH `writeProjectConstitution` (line ~210) AND `writeSpecConstitution` (line ~326) empty-state branches, replace the identical `Run \`specgraph constitution set\`` hint with the SAME MCP-first hint routing to the `constitution` MCP tool (`action: update`) and the `specgraph-constitution` skill — an MCP-only agent that sparks a spec before authoring the constitution hits the SpecView empty-state, so it must NOT be routed to a CLI it does not have (D-10). Then update `TestRenderProjectMarkdown_EmptyConstitution` in `internal/render/prime_test.go` to assert the new MCP-first project empty-state wording (it currently asserts the old `specgraph constitution set` string), and ADD a new `TestRenderSpecMarkdown_EmptyConstitution` (a `SpecView` with a nil `Constitution`) asserting the SpecView prime output routes to the `constitution` MCP tool / `specgraph-constitution` skill and `require.NotContains` any `specgraph constitution` CLI text. Do not add DB-backed logic; empty-state stays soft (composer already treats absent constitution as soft-empty). ALSO (addresses pass-2 review finding #3): correct the stale served-skill count from 6→7 in BOTH `fixtureProjectView()` (`SkillsCount: 6` → `SkillsCount: 7`, ~line 243) and the `expectedProjectMatchLegacy` `## Skills` literal ("6 skills exposed via MCP" → "7 skills exposed via MCP", ~line 55) so the golden matches the live 7-skill embedded catalog (`internal/mcp/skills/embedded_test.go` `wantNames`). Make this fix in the same golden edit as the routing line so `task check` does not surprise-fail on a pre-existing drift.
  </action>
  <verify>
    <automated>go test ./internal/render/</automated>
  </verify>
  <acceptance_criteria>
    - `writeSkills` output contains an authoring routing sentence naming `specgraph_skills_get` and the `specgraph-constitution`/`specgraph-authoring` skills.
    - `expectedProjectMatchLegacy` in `internal/render/prime_test.go` is updated to include the new `writeSkills` routing text, and `TestRenderProjectMarkdown_NoProvenance_MatchesExistingLayout` (the byte-for-byte golden) passes against it.
    - `writeProjectConstitution` AND `writeSpecConstitution` empty-states no longer contain `specgraph constitution set`; both reference the `constitution` MCP tool / `specgraph-constitution` skill.
    - `TestRenderProjectMarkdown_EmptyConstitution` asserts the new MCP-first project empty-state wording.
    - A new `TestRenderSpecMarkdown_EmptyConstitution` asserts the SpecView empty-state routes to the MCP tool/skill and contains no `specgraph constitution` CLI text.
    - The served-skill count in the golden matches the live embedded catalog: `fixtureProjectView().SkillsCount == 7` and `expectedProjectMatchLegacy` reads "7 skills exposed via MCP" (matching `len(embedded skills)` == 7 in `internal/mcp/skills/embedded_test.go`), not 6 (pass-2 review finding #3).
    - `go test ./internal/render/` passes (populated-layout golden + both empty-state tests updated/added).
  </acceptance_criteria>
  <done>Prime routing line + MCP-first empty-state hints are in place on BOTH the project and spec prime surfaces, and the project + new spec empty-state tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: MCP constitution empty-resource routing</name>
  <read_first>
    - internal/mcp/resources.go (constitutionEmptyResource lines ~36-42 — identical CLI hint)
    - internal/mcp/resources_test.go (pass-3 review finding #2: `TestConstitutionResource_NotFoundRendersHint` asserts `require.Contains(..., "specgraph constitution set")` at line 170, and `TestConstitutionResource_SlugRequiredRendersHint` asserts the same string at line 187 — BOTH break the moment `constitutionEmptyResource` drops that CLI string, so `go test ./internal/mcp/` and `task check` fail unless these two assertions are updated in lockstep)
    - internal/render/prime.go (post-Task-1 empty-state wording — keep the two consistent)
  </read_first>
  <files>internal/mcp/resources.go, internal/mcp/resources_test.go</files>
  <action>
    Update `constitutionEmptyResource` so the empty-state markdown replaces `Run \`specgraph constitution set\`` with the same MCP-first routing used in Task 1 (point to the `constitution` MCP tool `action: update` and the `specgraph-constitution` skill), keeping the two surfaces consistent for a fresh-init agent (D-10). THEN (addresses pass-3 review finding #2) update `internal/mcp/resources_test.go` in the same change: in `TestConstitutionResource_NotFoundRendersHint` (line 170) and `TestConstitutionResource_SlugRequiredRendersHint` (line 187), replace the `require.Contains(t, contents[0].Text, "specgraph constitution set")` assertions with assertions on the NEW MCP-first routing text (assert the empty-state routes to the `constitution` MCP tool `action: update` / `specgraph-constitution` skill and `require.NotContains(t, contents[0].Text, "specgraph constitution set")`). Without this, `constitutionEmptyResource` and its tests drift and `task check` surprise-fails.
  </action>
  <verify>
    <automated>go test ./internal/mcp/ && task check</automated>
  </verify>
  <acceptance_criteria>
    - `constitutionEmptyResource` text no longer contains `specgraph constitution set`; it routes to the MCP tool/skill.
    - `internal/mcp/resources_test.go` `TestConstitutionResource_NotFoundRendersHint` (was line 170) and `TestConstitutionResource_SlugRequiredRendersHint` (was line 187) no longer assert `specgraph constitution set`; they assert the MCP-first routing text and `require.NotContains` the old CLI string (pass-3 review finding #2).
    - `go test ./internal/mcp/` passes.
    - `task check` passes.
  </acceptance_criteria>
  <done>Prime + constitution empty-resource route MCP-only agents to the authoring skills/tool; the populated-layout golden was updated in Task 1 for the writeSkills routing line; `task check` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prime/render output → agent | Prime markdown is composed server-side from DB reads and rendered to the agent; this plan only changes static routing text. No new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Information Disclosure | prime resource handler | low | accept | Prime error path already returns sanitized `internal error` (execution_handler) and empty-state stays soft; this plan adds no new error surface. Verified reliable by plan 05 prime smoke. |
</threat_model>

<verification>
- `go test ./internal/render/ ./internal/mcp/` green.
- `task check` green.
</verification>

<success_criteria>
`specgraph://prime` and the constitution empty-resource route a fresh-init MCP-only agent to the authoring skills/tool; no CLI hint remains in the fresh-init path.
</success_criteria>

<output>
Create `.planning/phases/06-mcp-authoring-self-teaching-path/06-03-SUMMARY.md` when done.
</output>
