---
phase: 06-mcp-authoring-self-teaching-path
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - internal/mcp/tools_core.go
  - internal/mcp/tools_core_test.go
  - internal/mcp/tools_authoring.go
  - internal/mcp/tools_authoring_test.go
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "`constitution` tool `update` accepts friendly YAML (`layer: project`, `type: adr`) and persists via UpdateConstitution — no protojson enum/field names required."
    - "`author` tool spark/shape/specify/decompose accept friendly snake_case YAML `output` and persist via the funnel RPCs — no protojson required."
    - "Invalid enum/layer values return a sanitized error result; the tool descriptions document the friendly-YAML shape (agent teaching surface)."
    - "The `exchanges` param doc self-teaches the required post-spark payload (proto field names + a minimal JSON example), and malformed `exchanges` JSON returns a sanitized errResult at the MCP boundary."
  artifacts:
    - internal/mcp/tools_core.go
    - internal/mcp/tools_authoring.go
  key_links:
    - "handleUpdate routes raw → constitution/load.FromYAML → load.ToProto → UpdateConstitution (replaces protojson.Unmarshal)."
    - "handleSpark/Shape/Specify/Decompose route raw → authoring/load.*FromYAML → stage proto → funnel RPC (replaces protojson.Unmarshal ×4)."
---

<objective>
Replace the raw-protojson write boundary in both MCP authoring surfaces with the friendly-YAML pipeline (D-01, D-02, D-03) — the #1002 defect fix. The `constitution` tool reuses the existing `internal/constitution/load` pipeline; the four `author` funnel stages call the `internal/authoring/load` package (plan 01). Rewrite each tool's `Description` and param docs to document the friendly-YAML shape, since tool descriptions are an agent-facing teaching surface (criterion #4). Migrate the existing MCP tool tests off protojson to friendly YAML (Pitfall 1 — the RPC service-client tests stay proto-typed and are untouched).

Purpose: An MCP-only agent submits exactly the friendly YAML the skills teach and it persists — no CLI, no protojson knowledge.
Output: Updated `tools_core.go` + `tools_authoring.go` handlers/descriptions and their migrated tests.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-RESEARCH.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-PATTERNS.md
@internal/mcp/tools_core.go
@internal/mcp/tools_authoring.go
@internal/constitution/load/load.go
@internal/authoring/load/load.go
</context>

<artifacts_this_phase_produces>
NEW symbols created by THIS plan (exclude from source-grounding drift checks):
- No new exported symbols. Modified: `constitutionTool.handleUpdate` + `constitutionTool.def()` (internal/mcp/tools_core.go); `authorTool.handleSpark/handleShape/handleSpecify/handleDecompose` + `authorTool.def()` including the rewritten `output` and `exchanges` param docs (internal/mcp/tools_authoring.go).
- New imports: `internal/constitution/load` (tools_core.go), `internal/authoring/load` (tools_authoring.go).
- Rewritten test cases in tools_core_test.go / tools_authoring_test.go using friendly YAML, plus a new malformed-`exchanges` negative test.
- Consumes NEW symbols from plan 01: `SparkFromYAML`, `ShapeFromYAML`, `SpecifyFromYAML`, `DecomposeFromYAML`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Constitution update — friendly YAML pipeline + description + tests</name>
  <read_first>
    - internal/mcp/tools_core.go (handleUpdate lines 105-121 — the defect; def() lines 54-73; handleGet already-friendly analog lines 87-103)
    - internal/constitution/load/load.go (FromYAML + ToProto — reuse verbatim)
    - internal/mcp/tools_core_test.go (existing update tests at lines ~79-113 feed protojson `{"layer":"CONSTITUTION_LAYER_PROJECT",...}` — migrate to friendly YAML)
  </read_first>
  <files>internal/mcp/tools_core.go, internal/mcp/tools_core_test.go</files>
  <action>
    In `handleUpdate`, replace `protojson.Unmarshal([]byte(raw), &c)` with `load.FromYAML([]byte(raw))` (import `internal/constitution/load`); on error return a sanitized `errResult` (house style — never surface raw yaml/protojson internals, T-06-03); pass `load.ToProto(con)` as the `UpdateConstitution` request Constitution. Keep the `constitution` param key (back-compat) but rewrite its `stringProp` doc and the tool `Description` to document the friendly-YAML shape inline (`layer: project`, `type: adr`, tech/principles/constraints/antipatterns) — criterion #4. Remove the now-unused `protojson` import from tools_core.go if nothing else uses it. Migrate the update tests in tools_core_test.go to submit friendly YAML (e.g. `layer: project`, `name: my-project`, `constraints: [...]`) and assert the resulting `UpdateConstitutionRequest.Constitution.Layer == CONSTITUTION_LAYER_PROJECT`; keep an invalid-input case asserting an `errResult`.
  </action>
  <verify>
    <automated>go test ./internal/mcp/ -run Constitution</automated>
  </verify>
  <acceptance_criteria>
    - `internal/mcp/tools_core.go` `handleUpdate` no longer calls `protojson.Unmarshal`; it uses `load.FromYAML` + `load.ToProto`.
    - The `constitution` tool `Description`/param doc documents the friendly-YAML shape (no "Full constitution JSON" wording).
    - Friendly YAML `layer: project` persists as `CONSTITUTION_LAYER_PROJECT`; invalid layer returns an `errResult`.
    - `go test ./internal/mcp/ -run Constitution` passes.
  </acceptance_criteria>
  <done>The `constitution` tool `update` accepts friendly YAML via the constitution/load pipeline, documents the shape, and its tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Funnel stages — friendly YAML pipeline + description + tests</name>
  <read_first>
    - internal/mcp/tools_authoring.go (handleSpark/Shape/Specify/Decompose lines 169-294 — protojson.Unmarshal ×4; def() lines 123-145; keep validateOptionalPosture/parseOptionalExchanges as-is)
    - internal/authoring/load/load.go (SparkFromYAML/ShapeFromYAML/SpecifyFromYAML/DecomposeFromYAML from plan 01)
    - internal/mcp/tools_authoring_test.go (existing tests feed protojson `output` e.g. `{"scopeIn":["auth"],"chosenApproach":"oauth2"}`, `{"strategy":"DECOMPOSITION_STRATEGY_VERTICAL_SLICE"}` — migrate to friendly YAML)
  </read_first>
  <files>internal/mcp/tools_authoring.go, internal/mcp/tools_authoring_test.go</files>
  <action>
    In each of `handleSpark`, `handleShape`, `handleSpecify`, `handleDecompose`, replace `protojson.Unmarshal([]byte(raw), &out)` with the matching `authload.<Stage>FromYAML([]byte(raw))` (import `internal/authoring/load` aliased as `authload`); on error return a sanitized `errResult`. Leave `validateOptionalPosture`, `parseOptionalExchanges`, `handleApprove`, `handleAmend`, `handleSupersede`, and the conversation/analytical-pass helpers UNCHANGED behaviorally (posture/exchanges still use their existing protojson-array isolation — SCOPE DECISION: `exchanges` stays a JSON array for this milestone; the fix is documenting + testing it, not converting it to YAML, matching 06-02/06-05). Rewrite the `output` param doc and the `author` tool `Description` to document the per-stage friendly snake_case YAML shape (criterion #4). EXCHANGES PARAM DOC (addresses review finding #1b): rewrite the `exchanges` `stringProp` doc in `def()` (currently "JSON array of ConversationExchange objects...") to a self-teaching description that names the proto field names and shows a minimal worked example inline — a two-element JSON array: a `role: "probe"` with `sequence: 1` and a `role: "response"` with `sequence: 2`, each with `content` and `stage`, and state exchanges are required for shape/specify/decompose (mirroring `e2e/api/helpers_test.go:117-119`). Keep `protojson` import only if still used by the untouched exchanges/findings paths. Migrate the spark/shape/specify/decompose tests in tools_authoring_test.go to friendly snake_case YAML (`scope_in`, `chosen_approach`, `strategy: vertical_slice`, `scope_sniff: medium`) and keep an invalid-enum case per enum asserting an `errResult` (the T-06-01 tampering assertion). ADD a malformed-`exchanges` negative test at the MCP boundary: pass a syntactically invalid `exchanges` JSON string to a post-spark stage and assert `parseOptionalExchanges` returns a sanitized `errResult` (no raw protojson internals leaked, T-06-03). NOTE for the executor: the semantic "exchanges missing `sequence` → server rejection" case from the review CANNOT be asserted here because `tools_authoring_test.go` uses `mockAuthoringService`, which does not run server-side `authoring.ValidateExchanges` (that runs in the real ConnectRPC handler); that missing-`sequence` rejection is exercised through the real handler path in the 06-05 e2e.
  </action>
  <verify>
    <automated>go test ./internal/mcp/ && task check</automated>
  </verify>
  <acceptance_criteria>
    - No `protojson.Unmarshal` remains in `handleSpark`/`handleShape`/`handleSpecify`/`handleDecompose`.
    - Each stage handler calls the corresponding `internal/authoring/load` parser.
    - `author` tool `Description`/`output` param doc documents the friendly snake_case YAML per stage (no "JSON string" wording for `output`).
    - The `exchanges` param doc names the proto fields (role/content/stage/sequence) and shows a minimal two-element JSON example (sequence 1 probe, sequence 2 response), stating exchanges are required for shape/specify/decompose.
    - A malformed-`exchanges` JSON input returns a sanitized `errResult` via `parseOptionalExchanges` (T-06-03), verified by a new negative test.
    - Friendly YAML `strategy: vertical_slice` persists as `DECOMPOSITION_STRATEGY_VERTICAL_SLICE`; invalid enum returns an `errResult`.
    - `go test ./internal/mcp/` and `task check` pass.
  </acceptance_criteria>
  <done>Both MCP write surfaces accept friendly YAML, reject invalid enums with sanitized errors, document the shape for agents, and all MCP tests pass under `task check`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool input (agent) → RPC | Agent-authored friendly YAML enters at the `constitution`/`author` tool handlers and is mapped to typed proto before the ConnectRPC call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | handleUpdate / handleSpark/Shape/Specify/Decompose | high | mitigate | Friendly parsers (load.FromYAML + authoring/load) reject unknown layer/enum via UNSPECIFIED→error; handlers return `errResult` rather than persisting a defaulted value. Verified by migrated invalid-input tests. |
| T-06-02 | Denial of Service | funnel + constitution parsers | medium | mitigate | Parsers unmarshal into fixed typed structs (plan 01 / constitution load); existing MCP request-size limits bound payload. |
| T-06-03 | Information Disclosure | tool error results | medium | mitigate | Keep `errResult`/`connectErrResult` house style; error messages are concise, no raw yaml/protojson internals leaked. Malformed `exchanges` JSON returns a sanitized `errResult` via `parseOptionalExchanges` (verified by a new negative test); the server-side `ValidateExchanges` (role/content/sequence invariants) remains the authoritative V5 gate for structurally-valid-but-invalid payloads, exercised in the 06-05 e2e. |
</threat_model>

<verification>
- `go test ./internal/mcp/` green (constitution + funnel handlers on friendly YAML).
- `task check` green.
</verification>

<success_criteria>
Both the `constitution` and `author` MCP tools accept the friendly-YAML the skills teach, reject invalid enums/layers with sanitized errors, and no longer require protojson. No proto regen performed.
</success_criteria>

<output>
Create `.planning/phases/06-mcp-authoring-self-teaching-path/06-04-SUMMARY.md` when done.
</output>
