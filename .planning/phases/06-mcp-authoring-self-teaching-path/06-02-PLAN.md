---
phase: 06-mcp-authoring-self-teaching-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/mcp/skills/skill_mcp_reference_test.go
  - internal/mcp/skills/embedded/specgraph-constitution/SKILL.md
  - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md
  - internal/mcp/skills/embedded/specgraph-graph-query/SKILL.md
  - internal/mcp/skills/embedded/specgraph-analytical-passes/SKILL.md
  - internal/mcp/skills/embedded/specgraph-drift/SKILL.md
  - internal/mcp/skills/embedded/specgraph-conventions/SKILL.md
  - internal/mcp/skills/embedded/specgraph-troubleshooting/SKILL.md
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "Every served SKILL.md leads with the MCP tool/resource path; the CLI is a clearly-gated appendix an MCP-only agent skips."
    - "specgraph-constitution and specgraph-authoring teach the friendly-YAML write payload as THE authoring pattern (constitution `update`, `author` per-stage `output`)."
    - "A content-level test verifies each SKILL.md references the MCP tool path and gates the CLI appendix (verified against embedded canonicals)."
    - "The friendly-YAML the SKILL.md files teach is bound to the load parsers: the specgraph-constitution write block parses through `internal/constitution/load.FromYAML` without error, and the specgraph-authoring stage `output` block uses only the snake_case funnel keys the parser accepts (no camelCase field-name typos) — closing the #1002 'skill teaches something the tool cannot accept' class."
    - "specgraph-authoring teaches the mandatory `exchanges` payload (JSON array; role/content/stage/sequence) for shape/specify/decompose, so an MCP-only agent that fixes `output` does not then hit an opaque protojson error on the first post-spark stage (criterion #2)."
    - "ATOMIC-RELEASE INVARIANT (pass-3 review finding #1, HIGH): no merge or deploy exposes these rewritten skills (which teach friendly-YAML `output`/`exchanges`) without the matching friendly-YAML handlers from plan 06-04 — 06-02 and 06-04 land in the SAME merge window (single PR or stacked merge, no intermediate deploy), or a live MCP-only agent between the two merges reproduces #1002 against the still-protojson handlers (`tools_authoring.go:178-179`)."
  artifacts:
    - internal/mcp/skills/skill_mcp_reference_test.go
    - internal/mcp/skills/embedded/specgraph-constitution/SKILL.md
  key_links:
    - "The constitution YAML schema block is preserved and becomes the `constitution update` write payload, not a file to `import`."
    - "Front-matter `name:` and `summary:` are preserved (skills_test.go asserts them)."
---

<objective>
Rewrite all 7 embedded skill canonicals to be MCP-first (D-05): every skill leads with MCP tools/resources and teaches the chosen friendly-YAML write pattern as THE authoring pattern (D-06), with the CLI demoted to a single uniform gated "Requires local CLI" appendix at the end (D-07). Add a content-level assertion (D-09) that guards this posture against regression — the closest precedent is `TestContentProtoDrift`. This closes the #1002 root cause where the served skills teach the CLI import path an MCP-only agent cannot use.

Purpose: Make the authoring format impossible for an MCP-only agent to get wrong — the skill shows exactly the friendly-YAML block the fixed MCP handlers (plan 04) parse, and a parser-binding assertion proves the SKILL.md examples actually parse (constitution) / use the parser's accepted snake_case keys (authoring), so a field-name typo fails a gate instead of reproducing #1002.
Output: 7 rewritten SKILL.md canonicals + a new content-reference & parser-binding test (Wave 0 test for criteria #1/#4 from 06-VALIDATION.md).

Review finding #4 (LOW — `e2e/api/skills_test.go:106-121` "lists six skills" omits `specgraph-constitution`) is ROUTED to plan 06-05, not fixed here: `skills_test.go` is a `//go:build e2e` file whose assertion is only exercised by the e2e run, which 06-02's `task check` (unit-only) does not execute. Fixing it in a wave-1 unit plan would leave it unverified. 06-05 owns e2e and runs the full `e2e/api` suite, so the six→seven skill-list correction is incorporated there where it is actually run. This is a routing decision, not a deferral of the finding.

MERGE-WINDOW / ATOMIC-RELEASE CONSTRAINT (pass-3 review finding #1 — escalated to HIGH; supersedes the pass-2 documentation-only note; waves UNCHANGED): 06-02 (this plan, wave 1) rewrites the skills to teach friendly-YAML `output`/`exchanges`, while 06-04 (wave 2) wires the handlers that actually accept that friendly YAML. An agent hitting the live MCP `author`/`constitution` tools after 06-02 lands but before 06-04 lands would still get protojson failures (`tools_authoring.go:178-179`) — i.e. the #1002 defect is reproduced in the gap. Therefore 06-02 and 06-04 MUST land in the SAME merge window (single PR or stacked merge, no intermediate deploy) — this is now a hard release constraint (the ATOMIC-RELEASE INVARIANT must_have), not merely advisory. Per review suggestion #2, this atomic-release constraint is also recorded in the ROADMAP Phase 6 entry so it is visible outside the plan file. This is a sequencing/merge constraint; it does NOT change the wave graph (06-02 stays wave 1, 06-04 stays wave 2 → [06-01]).

AUTHORING PARSER-BINDING SCOPE DECISION (pass-3 review finding #4 / suggestion #3 — explicit decision, not silently dropped): the reviewer suggests importing `internal/authoring/load` into `skill_mcp_reference_test.go` to round-trip the specgraph-authoring `output` example once 06-01 lands. We deliberately do NOT add that round-trip to 06-02, because importing `internal/authoring/load` (created by 06-01) would introduce a compile-time dependency that forces 06-02 into wave 2, collapsing the wave-1 parallelism the plan set relies on. Instead: (a) 06-02 keeps its unit-level camelCase-vs-snake_case key guard (Task 1(b)), which catches the specific `chosenApproach` vs `chosen_approach` field-name typo class that caused #1002; and (b) the AUTHORITATIVE structural round-trip of the taught authoring YAML through `internal/authoring/load` is exercised end-to-end by the 06-05 MCP-only e2e (wave 3), where both the parser (06-01) and the rewritten skills (06-02) exist. Net effect: the field-name typo class is caught at unit time (wave 1); the deeper nested-structure round-trip is caught at e2e time (wave 3). The wave-1 "blind spot" the reviewer notes is the window between wave 1 and wave 3, which is acceptable because no merge ships to a live agent until the full phase (including 06-05) passes `task pr-prep` (D-08 gate).
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-CONTEXT.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-RESEARCH.md
@.planning/phases/06-mcp-authoring-self-teaching-path/06-PATTERNS.md
@internal/mcp/skills/embedded/specgraph-constitution/SKILL.md
@internal/mcp/skills/embedded/specgraph-authoring/SKILL.md
@internal/authoring/drift_test.go
</context>

<artifacts_this_phase_produces>
NEW symbols created by THIS plan (exclude from source-grounding drift checks):
- Test file `internal/mcp/skills/skill_mcp_reference_test.go` and its test function(s), including a new parser-binding assertion that imports `internal/constitution/load` and feeds the specgraph-constitution write block through `load.FromYAML`.
- New "## Requires local CLI (source/CLI users only — MCP-only agents skip this)" appendix section marker in each of the 7 SKILL.md files.
- Rewritten MCP-first prose in all 7 SKILL.md bodies.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Content-reference + parser-binding assertion test (Wave 0, criteria #1/#4)</name>
  <read_first>
    - internal/authoring/drift_test.go (TestContentProtoDrift — fenced-block stripper + table-driven scan + allowlist pattern to copy)
    - internal/mcp/skills/embedded_test.go (how the embedded package loads SKILL.md content; package `skills`)
    - internal/mcp/skills/skills.go (Source/serving API for reading embedded skill bodies)
    - internal/constitution/load/load.go (FromYAML — the constitution parser this test binds the specgraph-constitution SKILL.md write block against; already exists in-repo, NOT created this phase, so importing it adds no cross-plan dependency and 06-02 stays wave 1)
  </read_first>
  <files>internal/mcp/skills/skill_mcp_reference_test.go</files>
  <action>
    Add `internal/mcp/skills/skill_mcp_reference_test.go` (package matching the skills package, Apache-2.0 SPDX header). Name the top-level test function `TestSkillMCPReference` (Cursor review 06-02 suggestion #1) so the Task 2 `-run MCPReference` filter matches reliably; any subtests must also carry `MCPReference` in their name. Table-driven test over all 7 skill names asserting each embedded SKILL.md body: (1) references the MCP tool path — for specgraph-constitution contains the `constitution` tool `update` usage and for specgraph-authoring contains the `author` tool per-stage `output` usage; the other 5 contain at least one MCP tool/resource reference (`specgraph_` or a tool name or `specgraph://`); (2) contains the gated appendix header string "Requires local CLI"; (2b) CLI-APPENDIX ORDERING GUARD (Cursor review 06-02 suggestion #3): assert that any CLI-primary invocation (`specgraph ` command-line usage) appears ONLY at a byte offset AFTER the "Requires local CLI" header — i.e. the index of the first `specgraph ` CLI command string is greater than the index of the "Requires local CLI" header (skills with no CLI commands trivially pass) — so a regression that reintroduces a CLI-first step before the gated appendix fails the gate, not just the header-presence check; (3) preserves front-matter `name: specgraph-<x>` and `summary:`. Keep those posture assertions lenient (substring/regex), not exact-wording, so future prose edits don't break the gate.
    THEN add a parser-binding assertion (the cross-plan data-contract gate that closes the #1002 class — a field-name typo in a skill YAML example must fail a gate, not pass silently):
    (a) Extract the first fenced ```yaml block from the specgraph-constitution SKILL.md that represents a `constitution` write payload (the `layer: "project"` / `type: "adr"` schema block preserved by Task 2), feed it through `internal/constitution/load.FromYAML`, assert no error AND that the parsed `*storage.Constitution` maps to the taught layer (`storage.ConstitutionLayerProject`) — binding the constitution teaching surface directly to the parser that plan 04 wires into the `constitution` handler.
    (b) Extract the first fenced ```yaml stage `output` block from the specgraph-authoring SKILL.md and assert it uses the snake_case funnel keys the `internal/authoring/load` parser accepts (`scope_in`, `chosen_approach`, `success_must`, `verify_criteria`, `strategy`) and contains NONE of their camelCase equivalents (`scopeIn`, `chosenApproach`, `verifyCriteria`) — catching the exact `chosenApproach` vs `chosen_approach` typo class at unit level (`task check`) without importing `internal/authoring/load` (which plan 01 creates), so 06-02 stays wave 1 with no dependency on 06-01. (The full authoring-YAML round-trip through `internal/authoring/load` is exercised end-to-end by the plan 05 e2e once both parser and skills exist.)
    (c) exchanges self-teaching gate (addresses review finding #1a — closes the remaining #1002 failure class on post-spark stages): assert the specgraph-authoring SKILL.md body mentions `exchanges` AND `sequence` (substring checks), proving the skill teaches the mandatory `ConversationExchanges` payload that shape/specify/decompose require server-side (`internal/authoring/validate.go:43-44`). Keep this lenient (substring), not exact-wording. SCOPE DECISION (make explicit in this plan): the `exchanges` wire format stays a JSON array for this milestone (it is NOT converted to friendly YAML); the fix here is TEACHING + TESTING it, consistent with 06-04 (param docs) and 06-05 (e2e fixtures). Converting exchanges to YAML is out of scope for MCP-01.
    Assertions (1)-(3) plus the constitution parse-binding are RED until Tasks 2-3 rewrite the skills; the authoring snake_case guard and the exchanges/sequence gate are also RED until the specgraph-authoring rewrite (Task 2) emits the friendly `output` block and the minimal `exchanges` example.
  </action>
  <verify>
    <automated>go test ./internal/mcp/skills/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - internal/mcp/skills/skill_mcp_reference_test.go exists with SPDX header.
    - The top-level test is named `TestSkillMCPReference` (and any subtests contain `MCPReference`) so `-run MCPReference` matches (Cursor review 06-02 suggestion #1).
    - Test enumerates all 7 skill names and asserts the MCP-reference + "Requires local CLI" appendix + preserved front-matter conditions.
    - A CLI-appendix ordering guard asserts any `specgraph ` CLI command string appears only AFTER the "Requires local CLI" header (first-CLI-command index > header index), not merely that the header exists (Cursor review 06-02 suggestion #3).
    - The specgraph-constitution write block is fed through `internal/constitution/load.FromYAML` and asserted to parse without error and map to `ConstitutionLayerProject`.
    - The specgraph-authoring stage `output` block is asserted to use snake_case funnel keys and contain no camelCase field-name equivalents.
    - The specgraph-authoring body is asserted to mention `exchanges` and `sequence` (teaching the mandatory post-spark ConversationExchanges payload).
    - Test currently FAILS (RED) because the CLI-first skills do not yet satisfy it.
  </acceptance_criteria>
  <done>Failing content-reference + parser-binding test enumerates all 7 skills, asserts MCP-reference + gated-appendix + preserved front-matter, binds the constitution write block to `load.FromYAML`, and guards the authoring `output` block against camelCase field-name typos.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the two critical-path skills MCP-first</name>
  <read_first>
    - internal/mcp/skills/embedded/specgraph-constitution/SKILL.md (worst offender: Step 1 `constitution show`, Step 4 `constitution import`, Step 5 `constitution emit`; KEEP the YAML schema block)
    - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md (pass-3 review finding #5: the current file routes via the `author`/`author_start_stage` tools (`SKILL.md:38-49`) but does NOT teach the friendly-YAML write payload — no `output` snake_case example, no `exchanges` example. It is NOT "already MCP-first" in the sense of teaching the write format; the rewrite scope is larger than a voice pass. Use its prime→skills→author routing structure as the skeleton, but ADD the friendly `output` YAML per stage and the `exchanges` JSON example, and PRESERVE the existing `author_start_stage` entry-path reference (do not drop it — suggestion #6).)
    - internal/mcp/tools_core.go (constitution tool `def()` actions get/update — the tool the skill must teach)
    - internal/mcp/tools_authoring.go (author tool `def()` actions + per-stage output the skill must teach)
    - proto/specgraph/v1/authoring.proto (per-stage friendly field names for the `output` YAML examples)
  </read_first>
  <files>internal/mcp/skills/embedded/specgraph-constitution/SKILL.md, internal/mcp/skills/embedded/specgraph-authoring/SKILL.md</files>
  <action>
    Rewrite specgraph-constitution: replace the CLI-first `constitution show` step with the `constitution` MCP tool `action: get`; replace "Write and Import" (`specgraph constitution import`) with the `constitution` MCP tool `action: update` passing the SAME friendly-YAML block inline (the exact #1002 fix — D-06); KEEP the existing friendly YAML schema block verbatim (`layer: "project"`, `type: "adr"`) — it is the write payload the plan-04 handler parses. Preserve front-matter (name, summary, description, license). Move `specgraph constitution show/import/emit` into a single trailing appendix headed "Requires local CLI (source/CLI users only — MCP-only agents skip this)" (D-07). Rewrite specgraph-authoring to document the friendly-YAML `output` per stage (spark/shape/specify/decompose) with snake_case fields matching the funnel proto, show the full round-trip prime→skills→author spark→…→approve (criterion #1/#2), and add the same gated CLI appendix. EXCHANGES TEACHING (addresses review finding #1a): for each post-spark stage (shape/specify/decompose), teach a minimal valid `exchanges` argument as a copy-pasteable JSON array of 1–2 elements using the proto field names `role` ("probe"/"response"), `content`, `stage`, `sequence` (strictly increasing, ≥1) — e.g. a probe at sequence 1 and a response at sequence 2, mirroring the canonical shape in `e2e/api/helpers_test.go:117-119`. State explicitly in the skill that `exchanges` is REQUIRED for shape/specify/decompose (server enforces at least one exchange) and remains a JSON array (not YAML) for this milestone, while `output` is friendly YAML. Note that `approve` (accept) needs only the slug and does not require exchanges. PRESERVE `author_start_stage` AS A DOCUMENTED ENTRY PATH (addresses pass-3 review finding #5 / suggestion #6): the current specgraph-authoring skill references `author_start_stage` (`SKILL.md:43`) and the tool still exists (`tools_authoring.go:29`); the rewrite MUST keep an explicit mention of `author_start_stage` as the documented way to begin a spec, so the MCP-first rewrite does not silently drop a working entry point. Edit the canonical path under `internal/mcp/skills/embedded/` only — do NOT create a second copy (reverse-symlinks; skills_symlink_test.go guards this, Pitfall 4).
  </action>
  <verify>
    <automated>go test ./internal/mcp/skills/ -run MCPReference && task skills:validate</automated>
  </verify>
  <acceptance_criteria>
    - specgraph-constitution SKILL.md no longer presents `specgraph constitution import` as the primary write path; `constitution` tool `update` with inline friendly YAML is the primary path.
    - The friendly YAML schema block is retained.
    - specgraph-authoring teaches a minimal valid `exchanges` JSON array (fields role/content/stage/sequence) for shape/specify/decompose and states exchanges are required for those stages and stay JSON (not YAML) this milestone.
    - specgraph-authoring retains an explicit `author_start_stage` reference as a documented entry path (not dropped by the rewrite — pass-3 finding #5 / suggestion #6).
    - Both skills contain the "Requires local CLI" gated appendix.
    - Front-matter `name:` and `summary:` preserved in both.
    - `task skills:validate` passes for both canonicals; the content-reference test (including the exchanges/sequence gate) passes for these two names.
  </acceptance_criteria>
  <done>specgraph-constitution and specgraph-authoring are MCP-first with the friendly-YAML write pattern and gated CLI appendix; their content-reference assertions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Align the remaining 5 skills MCP-first + uniform gated appendix</name>
  <read_first>
    - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md (post-Task-2 voice target)
    - internal/mcp/skills/embedded/specgraph-troubleshooting/SKILL.md (heaviest CLI — `doctor`/`health` need reframing on the `health` MCP tool + resources)
    - internal/mcp/skills/embedded/specgraph-drift/SKILL.md
    - internal/mcp/skills/embedded/specgraph-graph-query/SKILL.md
    - internal/mcp/skills/embedded/specgraph-analytical-passes/SKILL.md
    - internal/mcp/skills/embedded/specgraph-conventions/SKILL.md
  </read_first>
  <files>internal/mcp/skills/embedded/specgraph-graph-query/SKILL.md, internal/mcp/skills/embedded/specgraph-analytical-passes/SKILL.md, internal/mcp/skills/embedded/specgraph-drift/SKILL.md, internal/mcp/skills/embedded/specgraph-conventions/SKILL.md, internal/mcp/skills/embedded/specgraph-troubleshooting/SKILL.md</files>
  <action>
    Reframe specgraph-troubleshooting on the `health` MCP tool + resources and demote `doctor`/`health` CLI diagnostics to the gated appendix; make specgraph-drift MCP-first with a gated appendix. For specgraph-graph-query, specgraph-analytical-passes, specgraph-conventions (already MCP-leaning), do a light voice pass and append the same uniform "Requires local CLI (source/CLI users only — MCP-only agents skip this)" section (D-05/D-07). Every skill must reference at least one MCP tool/resource in its body. Preserve each file's front-matter (name, summary, description, license). Edit only the canonical `internal/mcp/skills/embedded/` paths (no second copy — Pitfall 4).
  </action>
  <verify>
    <automated>go test ./internal/mcp/skills/ && task skills:validate</automated>
  </verify>
  <acceptance_criteria>
    - All 5 skills contain the "Requires local CLI" gated appendix and at least one MCP tool/resource reference in the body.
    - Front-matter `name:`/`summary:` preserved in all 5.
    - The content-reference test (`skill_mcp_reference_test.go`) passes for all 7 skills.
    - `task skills:validate` and `task check` pass.
  </acceptance_criteria>
  <done>All 7 skills are MCP-first with a uniform gated CLI appendix; the content-reference gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| served skill content → agent | SKILL.md is served verbatim to agents; incorrect guidance (CLI-first) is the #1002 defect, not a security exploit. No new runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering (content correctness) | embedded SKILL.md canonicals | low | mitigate | Content-reference test (`skill_mcp_reference_test.go`) + `task skills:validate` gate the MCP-first posture and CLI-appendix gating against regression; the parser-binding assertion (constitution block → `load.FromYAML`; authoring `output` block → snake_case-key guard) binds the taught YAML to the parsers so a field-name typo fails `task check` rather than silently teaching an unacceptable payload (#1002 class). The taught `exchanges` example uses the server-valid shape (role ∈ {probe,response}, non-empty content, sequence ≥1 strictly increasing per `internal/authoring/validate.go`), so the skill does not teach a payload the server's V5 validation would reject; the exchanges/sequence content gate guards this against regression. |
</threat_model>

<verification>
- `go test ./internal/mcp/skills/` green (content-reference gate covers criteria #1/#4).
- `task skills:validate` green for all 7 canonicals.
- `task check` green.
</verification>

<success_criteria>
All 7 embedded skills are MCP-first, teach the friendly-YAML write pattern, gate the CLI appendix, and are protected by an automated content-level assertion.
</success_criteria>

<output>
Create `.planning/phases/06-mcp-authoring-self-teaching-path/06-02-SUMMARY.md` when done.
</output>
