---
phase: 05-ui-project-selector-and-refinements
plan: 13
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - web/src/routes/constitution/+page.ts
  - web/src/routes/constitution/+page.svelte
  - web/src/lib/constitution-load.test.ts
autonomous: true
requirements: [D-10, D-01, D-02, D-09, D-13]
user_setup: []

must_haves:
  truths:
    - "Constitution fetches in universal +page.ts load() (replacing the component-body $effect anti-pattern), re-fetching on switch (D-01/D-02/Pitfall 4)."
    - "Merged/Layer badge and the four layer badges re-derive from the reloaded provenance array and are correct after a project switch (D-10)."
    - "Switching to a project with no constitution shows the 'No constitution for this project' empty state (D-10), not stale badges/sections."
    - "The per-page `<nav class=\"breadcrumb\">` stays REMOVED (stripped atomically in 05-03 to avoid the mid-wave transient — review Round-2 MEDIUM #1); the layout owns the project breadcrumb (D-11). The `Constitution` view label is static; the page `<h1>` carries the constitution name (review Round-2 MEDIUM #2)."
    - "The view renders Loading (Skeleton), Empty, and Error (Retry) states per the UI-SPEC State Matrix, with streamed `{#await}` skeleton-on-switch (phase default)."
    - "A Vitest-only load-seam unit test (no new dep, no component harness) pins the constitution `load()` contract: it registers `depends('app:project')` (the mechanism that makes `invalidateAll()` re-run it — D-01/D-02), maps the RPC response to `{ constitution, provenance }`, and returns `loadError` on RPC throw (review Round-2 MEDIUM #3)."
  artifacts:
    - "web/src/routes/constitution/+page.ts (constitution load)"
    - "web/src/routes/constitution/+page.svelte (data-prop, provenance-derived badges, empty state)"
    - "web/src/lib/constitution-load.test.ts (Vitest unit test of the constitution load() seam — depends('app:project') + data mapping + loadError)"
  key_links:
    - "load returns { constitution, provenance } → $derived(data.provenance) → layer badges re-render on invalidateAll"
    - "categorical layer badge pairs from badge-variants (fixed light/dark, not --primary)"
---

<objective>
Give the Constitution view its D-10 polish: move the fetch out of the component-body `$effect` (the exact anti-pattern causing stale badges — RESEARCH Pitfall 4) into a universal `+page.ts` `load()`, derive the Merged/Layer badge and the four categorical layer badges from the reloaded `provenance`, and render a correct "No constitution for this project" empty state across project switches (D-10, D-01, D-02, D-09).

Purpose: The constitution view is the one explicitly called out for extra correctness across switches; badges and empty state must always reflect the current project.
Output: A load-ified constitution view whose badges and empty state re-derive correctly on every switch.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constitution load() — constitution/+page.ts</name>
  <files>web/src/routes/constitution/+page.ts</files>
  <read_first>
    - web/src/routes/constitution/+page.svelte (current component-body $effect load L12-24/L26 — the anti-pattern; layerOf L53-58; layerBadge snippet L61-66; empty state L239 — PATTERNS L181-188)
    - 05-RESEARCH.md "Architecture Pattern 1", Pitfall 4 (stale badges), Pitfall 6, L279
    - web/src/lib/api/client.ts (constitutionClient)
  </read_first>
  <action>
    Create `web/src/routes/constitution/+page.ts` as a `PageLoad`: `await parent()`, `depends('app:project')`, call `constitutionClient.getConstitution({})`, and return `{ constitution: resp.constitution ?? null, provenance: resp.provenance ?? [] }` (or a caught `loadError` per RESEARCH L279). This replaces the component-body `$effect(() => { load() })` fetch. Do not derive badges here — return the raw `provenance` array so the component derives from it (Pitfall 4 fix).
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');if(!fs.existsSync('src/routes/constitution/+page.ts'))process.exit(1);const l=fs.readFileSync('src/routes/constitution/+page.ts','utf8');if(!/await parent\(\)/.test(l)||!/getConstitution/.test(l)||!/provenance/.test(l))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `constitution/+page.ts` is a `PageLoad` that `await parent()`, `depends('app:project')`, fetches the constitution, and returns `{ constitution, provenance }` or `loadError`.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>Constitution data + provenance fetched in load(), re-runnable on switch.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle constitution/+page.svelte — provenance-derived badges + empty state + skeleton</name>
  <files>web/src/routes/constitution/+page.svelte</files>
  <read_first>
    - web/src/routes/constitution/+page.svelte (layerOf L53-58, layerBadge snippet L61-66, layer hex L388-391, empty {:else} L239 — PATTERNS L181-188, "Categorical Badge Palette" L306-321)
    - web/src/lib/components/badge-variants.ts (layer map from 05-01), AccordionSection.svelte (05-04)
    - 05-UI-SPEC.md "Categorical badge palette" (four layer pairs + Merged/Layer secondary), "Per-View State Matrix" (Constitution row), "Copywriting Contract" (empty-state constitution copy)
    - web/src/lib/components/ui/skeleton, card, button, badge, separator
  </read_first>
  <action>
    Rewrite `web/src/routes/constitution/+page.svelte` to consume `let { data } = $props()`. Remove the component-body `$effect` fetch and any local `$state` provenance. **KEEP the per-page `<nav class="breadcrumb">` REMOVED — it was already stripped atomically in 05-03 (Wave 2) to avoid the transient double-breadcrumb (review Round-2 MEDIUM #1); the layout owns the `{project} / {View}` breadcrumb now (D-11). Do NOT re-introduce it in the rewrite. The layout shows the static `Constitution` view label; the page `<h1>{constitution.name || 'Constitution'}</h1>` carries the specific title (review Round-2 MEDIUM #2). The orphaned scoped `.breadcrumb` styles go when the `<style>` block is deleted below.** Change `layerOf()` and the layer-badge rendering to read `$derived(data.provenance)` so on `invalidateAll()` the load re-runs and every badge re-derives automatically (Pitfall 4 / D-10). Replace the four layer hex classes with the fixed categorical light/dark Tailwind pairs from `badge-variants` (User→blue, Org→amber, Project→green, Domain→violet per UI-SPEC) — these are categorical, NOT `--primary`. The Merged vs single-Layer badge uses the neutral shadcn `Badge variant="secondary"`. Render states per the State Matrix: Loading → `Skeleton` meta + section rows; Empty → the "No constitution for this project" card (UI-SPEC copy: heading "No constitution for this project", body about defining one via the CLI) when `data.constitution` is null — ensure NO stale badges/sections linger; Error → inline Retry `Card` reading `data.loadError`. Use migrated `AccordionSection` for sections. Delete the scoped `<style>`; use tokens. **Skeleton-on-switch via streamed promises + `{#await}` (phase DEFAULT per review MEDIUM #4 — NOT a `switching` flag, NOT `$navigating`).**
  </action>
  <verify>
    <automated>cd web && node -e "const p=require('fs').readFileSync('src/routes/constitution/+page.svelte','utf8');if(/<style/.test(p))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(p))process.exit(1);if(/onMount/.test(p))process.exit(1);if(/class=\"breadcrumb\"/.test(p))process.exit(1);if(!/\$props\(\)/.test(p)||!/data\.provenance/.test(p)||!/[Ss]keleton/.test(p))process.exit(1)" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `+page.svelte` consumes `data`, has no `$effect`/`onMount` fetch, no per-page `<nav class="breadcrumb">` (layout owns it — D-11), and derives layer badges from `data.provenance`.
    - Layer badges use the fixed categorical light/dark pairs (not `--primary`); Merged/Layer badge is `variant="secondary"`.
    - Empty state renders the UI-SPEC "No constitution for this project" copy with no stale badges/sections; Loading/Error states present.
    - No `<style>`/hex; `pnpm build` and `pnpm test` pass.
  </acceptance_criteria>
  <done>Constitution badges + empty state re-derive correctly on every project switch (D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest load-seam unit test — constitution load() depends('app:project') + data mapping + loadError (review Round-2 MEDIUM #3)</name>
  <files>web/src/lib/constitution-load.test.ts</files>
  <read_first>
    - web/src/lib/oidc.test.ts, web/src/lib/keys.test.ts (existing plain-`.ts` Vitest analogs — `describe/it/expect`, `vi.mock`, fetch/dep mocking; SAME category, NO `@testing-library/svelte`)
    - web/src/routes/constitution/+page.ts (the `load` under test, created in Task 1)
    - web/src/lib/api/client.ts (the `constitutionClient` to `vi.mock`)
    - 05-RESEARCH.md "Architecture Pattern 1", L279 (loadError contract)
    - 05-VALIDATION.md "Wave 0 Requirements" (component-harness OUT of appetite; this is a plain load-fn unit test, in appetite)
  </read_first>
  <behavior>
    - Test 1 (D-01/D-02 mechanism): calling `load` with a fake `{ parent: async()=>({}), depends, fetch }` invokes `depends('app:project')` exactly once — this is the dependency that makes `invalidateAll()`/`invalidate('app:project')` re-run the load after a switch.
    - Test 2 (data mapping): with `constitutionClient.getConstitution` mocked to resolve `{ constitution: {…}, provenance: [layers] }`, `load` returns `{ constitution, provenance }` mapped from the response and NO `loadError`.
    - Test 3 (D-10 empty seam): mocked resolve with `constitution: undefined` → `load` returns `{ constitution: null, provenance: [] }` (or equivalent empty), NO throw.
    - Test 4 (loadError, RESEARCH L279): mocked `getConstitution` REJECTS → `load` catches and returns a `loadError` field (does NOT throw to `+error.svelte`).
  </behavior>
  <action>
    Create `web/src/lib/constitution-load.test.ts` modeled on `oidc.test.ts`/`keys.test.ts` (plain Vitest — `import { describe, it, expect, vi } from 'vitest'`). `vi.mock('$lib/api/client', ...)` to stub `constitutionClient.getConstitution`, then `import { load } from '../routes/constitution/+page'` and invoke it with a hand-rolled fake load event: `{ parent: vi.fn().mockResolvedValue({}), depends: vi.fn(), fetch: vi.fn() }` cast `as any`. Assert the four behaviors above. This is a LOAD-FUNCTION unit test — it needs NO jsdom, NO `@testing-library/svelte`, NO `@vitest/browser` (the component harness stays out of appetite per VALIDATION). It anchors the D-01/D-02 switch mechanism (the `depends('app:project')` registration) and the D-10 constitution mapping structurally, going beyond the `node -e` string greps. Do NOT assert framework re-run behavior (that is SvelteKit's contract, guaranteed once `depends('app:project')` is registered and 05-03's `switchProject` calls `invalidateAll()`).
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');const t=fs.readFileSync('src/lib/constitution-load.test.ts','utf8');if(!/depends/.test(t)||!/loadError/.test(t)||!/vi\.mock/.test(t)||/testing-library/.test(t))process.exit(1)" && pnpm test -- constitution-load</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/constitution-load.test.ts` exists, imports the constitution `load` from `../routes/constitution/+page`, and `vi.mock`s `$lib/api/client`.
    - Tests assert: `depends('app:project')` called; resolved response → `{ constitution, provenance }` with no `loadError`; empty constitution → null/empty mapping; rejected RPC → `loadError` (no throw).
    - The test uses ONLY Vitest (no `@testing-library/svelte`/`@vitest/browser`); `pnpm test -- constitution-load` passes.
  </acceptance_criteria>
  <done>The constitution load() seam (depends + mapping + loadError) is pinned by a Vitest-only unit test — D-01/D-02/D-10 anchored beyond string greps.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

New files:
- `web/src/routes/constitution/+page.ts` — constitution universal load()
- `web/src/lib/constitution-load.test.ts` — Vitest-only load-seam unit test (depends('app:project') + data mapping + loadError; no component harness)

Modified files:
- `web/src/routes/constitution/+page.svelte` — consumes `data`; provenance-derived categorical layer badges; per-page breadcrumb stays removed (stripped in 05-03, D-11); empty/loading/error states
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client switch → constitution load | invalidateAll re-runs the constitution load with new project header |
| server constitution/provenance → rendered badges | Layer provenance drives categorical badges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information disclosure | stale layer badges/sections from prior project after switch | high | mitigate | Fetch in load(); badges are `$derived(data.provenance)`; on invalidateAll the load re-runs and badges/empty-state re-derive — no stale prior-project provenance retained (D-10 / Pitfall 4). |
| T-05-XSS-13 | Tampering | constitution/section text rendered | high | mitigate | Svelte `{text}` bindings (auto-escaped); no `{@html}` of server content. |
| T-05-15 | Denial of Service | load error hitting +error.svelte | medium | mitigate | Catch in load(), return `loadError`, inline Retry card (RESEARCH L279). |
</threat_model>

<verification>
- `pnpm -C web build` and `pnpm -C web test` pass; no `<style>`/hex.
- `task web:build && task build && task check` pass.
- Manual UAT (D-10): switch between a project with a constitution and one without → badges/sections re-derive correctly and the empty state appears with no stale content; layer badge hues correct in light and dark.
- **Verification appetite (review Round-2 MEDIUM #3):** the full Svelte component-test harness (`@testing-library/svelte` + jsdom) stays **out of appetite** this phase (recorded in VALIDATION.md) — so cross-switch VISUAL badge re-derivation (D-10) is confirmed via manual UAT. HOWEVER, a cheap Vitest-only **load-seam unit test** (Task 3, no new dep) now anchors the `load()` contract — `depends('app:project')` registration (the D-01/D-02 switch mechanism) + constitution data mapping + `loadError` — going beyond string greps. This narrows the manual-UAT surface from "the whole load+switch path" to "the visual badge rendering only".
</verification>

<success_criteria>
- Constitution loads via `+page.ts`, badges re-derive from reloaded provenance, and the empty state is correct across switches (D-10, D-01, D-02); app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-13-SUMMARY.md` when done.
</output>
