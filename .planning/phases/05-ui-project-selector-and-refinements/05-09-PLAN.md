---
phase: 05-ui-project-selector-and-refinements
plan: 09
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/lib/components/Graph.svelte
  - web/src/lib/components/GraphMini.svelte
autonomous: true
requirements: [D-12, D-13]
user_setup: []

must_haves:
  truths:
    - "Graph and GraphMini keep their dagre layout logic and render node/edge colors from theme tokens (dark-mode legible)."
    - "Both are wrapped in a shadcn Card; the old navy/blue scoped palette is retired."
  artifacts:
    - "web/src/lib/components/Graph.svelte (dagre + Card + token colors)"
    - "web/src/lib/components/GraphMini.svelte (compact Graph)"
  key_links:
    - "theme CSS variables → dagre node/edge fill colors → legible in .dark"
---

<objective>
Migrate `Graph` and `GraphMini` to the shadcn look WITHOUT touching the dagre layout logic: wrap the canvas in a shadcn `Card` and drive node/edge colors from theme tokens so the graph is legible in both light and dark mode (D-12/D-13).

Purpose: The graph is the most bespoke component; only its framing and colors change — the dagre algorithm stays intact.
Output: Two token-colored, Card-framed graph components.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe Graph on a Card with theme-token node/edge colors</name>
  <files>web/src/lib/components/Graph.svelte</files>
  <read_first>
    - web/src/lib/components/Graph.svelte (dagre layout logic — KEEP untouched; recolor only — PATTERNS L300)
    - web/src/lib/components/ui/card
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (Graph → keep dagre render; wrap in Card; recolor nodes/edges from theme tokens, must be dark-mode legible)
  </read_first>
  <action>
    Modify `Graph.svelte`: keep the dagre graph construction/layout code exactly as-is. Wrap the rendered canvas/SVG in a shadcn `Card`. Replace hard-coded node/edge/label color hexes with theme-token-derived colors — reference the CSS variables from `app.css` (e.g. read via `var(--foreground)`, `var(--muted)`, `var(--border)`, `var(--primary)`, or `getComputedStyle` on the CSS custom properties) so colors adapt to `.dark`. **Note (review suggestion #8): `Graph.svelte` currently hardcodes many light-theme hex fills; a single set of "theme tokens" is not enough — where a fill has no direct token analog, define explicit light/dark value pairs (e.g. via a CSS variable resolved under `:root` vs `.dark`, or `getComputedStyle`-read tokens) so dagre nodes/edges/labels stay legible in BOTH themes, not just light.** Ensure node text and edges remain legible in dark mode. Preserve props/behavior and Svelte 5 runes. Retire the old navy/blue palette; a minimal `<style>` is acceptable ONLY if it references CSS variables (no hard-coded navy/blue hex).
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/Graph.svelte','utf8');if(/#1a1a2e|#2563eb/.test(t))process.exit(1);if(!/[Cc]ard/.test(t)||!/--(foreground|primary|muted|border)/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - dagre layout logic unchanged; canvas wrapped in shadcn `Card`.
    - No legacy navy/blue palette hex; node/edge colors derive from theme CSS variables (`--foreground`/`--primary`/`--muted`/`--border`).
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>Graph reframed on a Card with theme-token colors, dagre intact.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the same reframe to GraphMini</name>
  <files>web/src/lib/components/GraphMini.svelte</files>
  <read_first>
    - web/src/lib/components/GraphMini.svelte (compact graph — same recipe as Graph)
    - web/src/lib/components/ui/card
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (GraphMini → same as Graph, compact)
  </read_first>
  <action>
    Apply the same treatment to `GraphMini.svelte`: keep the dagre/compact layout logic, wrap in a shadcn `Card`, and drive colors from theme CSS variables (dark-mode legible). Preserve props/behavior and Svelte 5 runes; retire legacy navy/blue hex.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/GraphMini.svelte','utf8');if(/#1a1a2e|#2563eb/.test(t))process.exit(1);if(!/--(foreground|primary|muted|border)/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - dagre/compact logic unchanged; colors derive from theme CSS variables; no legacy navy/blue hex.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>GraphMini reframed with theme-token colors.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

Modified files:
- `web/src/lib/components/Graph.svelte` — dagre intact, Card frame, theme-token colors
- `web/src/lib/components/GraphMini.svelte` — compact variant, same treatment
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server graph nodes/edges → dagre render | Node labels (spec/decision slugs) rendered in SVG/canvas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-XSS-09 | Tampering/Info-disclosure | node labels rendered in Graph | high | mitigate | Render node labels as SVG `<text>`/escaped bindings, not raw HTML; no `{@html}` of server-provided labels. |
| T-05-14 | Denial of Service | large graph render cost | low | accept | Existing dagre bounds unchanged; migration is color/frame only. |
</threat_model>

<verification>
- `pnpm -C web build` passes; no legacy navy/blue hex in either file.
- `task web:build && task build && task check` pass.
- Manual UAT (review suggestion #8) — **graph contrast checklist, BOTH themes:** in light AND dark mode, confirm (1) node fills contrast against the Card background, (2) node label text is readable, (3) edges/arrows are visible, (4) any legend/highlight colors remain distinguishable. Specifically re-check the fills that were hardcoded light-theme hex to ensure their dark-mode pair is legible.
</verification>

<success_criteria>
- Graph/GraphMini reframed on shadcn Card with theme-token colors, dagre logic untouched, dark-mode legible; app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-09-SUMMARY.md` when done.
</output>
