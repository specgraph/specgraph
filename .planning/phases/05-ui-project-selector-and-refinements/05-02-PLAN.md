---
phase: 05-ui-project-selector-and-refinements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/project.svelte.ts
  - web/src/lib/project.test.ts
autonomous: true
requirements: [D-04, D-05, D-06]
user_setup: []

must_haves:
  truths:
    - "The project list is sorted case-insensitively alphabetically (D-05)."
    - "Default selection follows precedence: valid last-used localStorage → project named 'default' → alphabetically-first (D-04)."
    - "A saved project no longer present auto-falls-back per the precedence (D-06)."
    - "Zero available projects leaves project.current empty (no phantom selection)."
  artifacts:
    - "web/src/lib/project.svelte.ts (loadProjects extended with sort + precedence)"
    - "web/src/lib/project.test.ts (Vitest unit tests for D-04/D-05/D-06)"
  key_links:
    - "loadProjects() sort + precedence → project.current setter → localStorage['specgraph-project'] → projectInterceptor X-Specgraph-Project header"
---

<objective>
Extend the existing project store (`web/src/lib/project.svelte.ts`) so default-project resolution follows the locked precedence (D-04), the list is deterministically sorted case-insensitive (D-05), and a stale saved project auto-falls-back (D-06). Add a Vitest unit test that pins this behavior (Nyquist Wave-0 scaffold for the selector logic).

Purpose: Make project selection deterministic and correct before the UI consumes it. This is pure TS/store logic — independent of shadcn, runs in Wave 1 in parallel with the foundation install.
Output: A correct, tested `loadProjects()` and a `project.test.ts` covering the three decisions.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing test — loadProjects sort + precedence + stale fallback</name>
  <files>web/src/lib/project.test.ts</files>
  <read_first>
    - web/src/lib/oidc.test.ts (Vitest structure analog — PATTERNS L267-273)
    - web/src/lib/keys.test.ts (analog for mocking `fetch` in Vitest)
    - web/src/lib/project.svelte.ts (current loadProjects seam — PATTERNS L104-124)
    - 05-RESEARCH.md "Architecture Pattern 3" (D-04/D-05/D-06 rules), "Validation Architecture → Phase Requirements → Test Map"
  </read_first>
  <behavior>
    - Test 1 (D-05): given `/api/projects` returns `["Zebra","alpha","Beta"]`, `project.available` is sorted case-insensitive → `["alpha","Beta","Zebra"]`.
    - Test 2 (D-04 tier 1): a valid saved `specgraph-project` present in the list is kept as `current`.
    - Test 3 (D-04 tier 2): no valid saved project but a project literally named `default` present → `current === 'default'`.
    - Test 4 (D-04 tier 3): no saved, no `default` → `current` is the alphabetically-first project.
    - Test 5 (D-06): a saved project absent from the returned list falls back per precedence (not retained).
    - Test 6 (D-07 seam): empty project list → `current === ''`.
  </behavior>
  <action>
    Create `web/src/lib/project.test.ts` modeled on `oidc.test.ts` (imports `describe/it/expect` from vitest) and `keys.test.ts` (fetch mocking). Mock `globalThis.fetch('/api/projects')` and a `localStorage` shim per test to drive each precedence branch. Assert on the exported `project.available` / `project.current` after `await loadProjects()`. Write the tests to describe the TARGET behavior (they will fail until Task 2). Do NOT weaken assertions to match the current implementation. Cover all six cases above. Vitest is already a devDependency — no framework install.
  </action>
  <verify>
    <automated>cd web && pnpm test -- project 2>&1 | grep -Eq "fail|FAIL|✗|expected" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/project.test.ts` exists with six named tests mapping to D-04 (three tiers), D-05, D-06, and the zero-projects seam.
    - Running the test BEFORE Task 2 fails (RED) because current `loadProjects()` neither sorts nor applies the `default` tier.
    - Tests mock `fetch` and `localStorage`; no network access.
  </acceptance_criteria>
  <done>A failing spec pins the D-04/D-05/D-06 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend loadProjects with case-insensitive sort + D-04 precedence (GREEN)</name>
  <files>web/src/lib/project.svelte.ts</files>
  <read_first>
    - web/src/lib/project.svelte.ts (the ONLY block to change is loadProjects L20-37; keep getter/setter + STORAGE_KEY verbatim — PATTERNS L82-124)
    - 05-RESEARCH.md "Architecture Pattern 3" (exact precedence + sort logic)
  </read_first>
  <action>
    Modify ONLY the `loadProjects()` body in `web/src/lib/project.svelte.ts`. After fetching `/api/projects`, sort the returned slugs case-insensitively (`a.toLowerCase().localeCompare(b.toLowerCase())`) before assigning to `available` (D-05, single client-side source of truth per RESEARCH — do not rely on server order). Then apply the D-04 three-tier precedence: (1) if `current` is truthy AND in `available`, keep it; (2) else if `available` includes `'default'`, set `project.current = 'default'`; (3) else if `available.length > 0`, set `project.current = available[0]` (alpha-first after sort); (4) else set `project.current = ''` (D-07 zero-projects seam). D-06 falls out of the tier-1 "in available" guard. Do NOT touch the `get/set current`, `STORAGE_KEY`, or the `available`/`loaded` state declarations — the setter's localStorage write is what D-03 depends on downstream. Keep the silent try/catch fallback.
  </action>
  <verify>
    <automated>cd web && pnpm test -- project</automated>
  </verify>
  <acceptance_criteria>
    - All six tests from Task 1 pass (GREEN).
    - `loadProjects()` sorts case-insensitively and applies the exact D-04 precedence including the `'default'` tier and the empty-string zero-projects case.
    - The getter/setter, `STORAGE_KEY`, and state declarations are unchanged (diff limited to the `loadProjects` body).
  </acceptance_criteria>
  <done>Deterministic sort + default precedence + stale fallback, verified by unit tests.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

New files:
- `web/src/lib/project.test.ts` — Vitest unit tests for D-04/D-05/D-06 + zero-projects seam

Modified files:
- `web/src/lib/project.svelte.ts` — `loadProjects()` extended with case-insensitive sort + D-04 precedence
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → client state | Persisted `specgraph-project` slug seeds `project.current` |
| `/api/projects` → client | Server-provided allow-list of project slugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | localStorage `specgraph-project` (user-editable) | medium | mitigate | D-04 tier-1 guard only keeps a saved slug if it is in the server-returned `available` list; a tampered/stale slug auto-falls-back (D-06). The header is advisory — the server authorizes via `storage.Scoper` regardless. |
| T-05-04 | Elevation of Privilege | client picks arbitrary project via header | high | accept | `X-Specgraph-Project` is advisory; `internal/server` scopes access via the authenticated identity + `storage.Scoper`, not the raw client header. No client-side change can bypass server authz. Verified: api_handler.go / ProjectMiddleware scope by auth context. |
</threat_model>

<verification>
- `pnpm -C web test` passes (project.test.ts green).
- `task check` passes.
</verification>

<success_criteria>
- Project selection is deterministic and correct across all D-04 tiers, sorted per D-05, with stale-fallback per D-06, proven by passing unit tests.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-02-SUMMARY.md` when done.
</output>
