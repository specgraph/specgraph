---
phase: 05-ui-project-selector-and-refinements
plan: 11
type: execute
wave: 3
depends_on: ["05-03", "05-04", "05-06", "05-08"]
files_modified:
  - web/src/routes/spec/[...slug]/+page.ts
  - web/src/routes/spec/[...slug]/+page.svelte
  - web/src/routes/decision/[...slug]/+page.ts
  - web/src/routes/decision/[...slug]/+page.svelte
autonomous: true
requirements: [D-01, D-02, D-09, D-13, D-10]
user_setup: []

must_haves:
  truths:
    - "Spec detail and Decision detail fetch in universal +page.ts load() keyed on params.slug, re-fetching on switch and on slug change (D-01/D-02/D-09)."
    - "The manual stale-guard (activeSlug) is removed; SvelteKit load re-run + param dependency replaces it (this also fixes the decision-detail slug-nav gap that onMount-only fetch left broken — review LOW)."
    - "The per-page `<nav class=\"breadcrumb\">` blocks in spec and decision are REMOVED — the layout is the single owner of the project breadcrumb (D-11; review MEDIUM #3)."
    - "Each view renders Loading (Skeleton), Empty (not-found), and Error (Retry) states per the UI-SPEC State Matrix, with streamed `{#await}` skeleton-on-switch (phase default)."
    - "Categorical status/stage badges render via the shared badge map; direction-aware edge grouping preserved."
  artifacts:
    - "web/src/routes/spec/[...slug]/+page.ts, +page.svelte"
    - "web/src/routes/decision/[...slug]/+page.ts, +page.svelte"
  key_links:
    - "params.slug → load primary getSpec/getDecision → secondary fetches try/caught → data prop"
    - "invalidateAll on switch → load re-runs → new-project spec/decision or not-found"
---

<objective>
Load-ify the Spec detail (`/spec/[slug]`) and Decision detail (`/decision/[slug]`) views: move fetches into universal `+page.ts` `load()` keyed on `params.slug`, delete the manual `activeSlug` stale-guard, and render the shadcn Loading/Empty/Error states (D-01/D-02/D-09). Preserve the direction-aware edge grouping and label helpers.

Purpose: Completes reactive switching + not-found handling for the two detail views on the Wave 2 migrated components.
Output: Two load-ified detail pages that re-fetch on switch/slug change with proper not-found and error states.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec detail load() — spec/[...slug]/+page.ts + restyle page with states</name>
  <files>web/src/routes/spec/[...slug]/+page.ts, web/src/routes/spec/[...slug]/+page.svelte</files>
  <read_first>
    - web/src/routes/spec/[...slug]/+page.svelte (loadSpec L33-69, activeSlug stale-guard L31/39/44 to REMOVE, groupedEdges L146-168, label helpers L98-138, changelog lazy L80-92 — PATTERNS L192-205)
    - 05-RESEARCH.md "Architecture Pattern 1", Pitfall 3, Pitfall 6, L279
    - 05-UI-SPEC.md "Per-View State Matrix" (Spec: skeleton title+metadata+rows / "Spec not found in this project." / Retry)
    - web/src/lib/components/AccordionSection.svelte (05-04), MetadataBar.svelte (05-06), DiffView/VersionCompare (05-08), badge-variants.ts (05-01)
    - web/src/lib/components/ui/skeleton, card, button
  </read_first>
  <action>
    Create `web/src/routes/spec/[...slug]/+page.ts` as a `PageLoad({ params, parent, depends })`: `await parent()`, `depends('app:project')`, primary `specClient.getSpec({ slug: params.slug })` (may throw → return `loadError` / not-found sentinel); keep the three non-critical secondary fetches (edges, findings, slices) each individually try/caught with `[]` fallback (mirror current L42-63). Return `{ spec, edges, findings, slices, loadError? }`. Rewrite `+page.svelte` to consume `data` via `$props()`: DELETE the manual `activeSlug` stale-guard (SvelteKit load re-run + the `params.slug` dependency replaces it — RESEARCH); **REMOVE the per-page `<nav class="breadcrumb">` block (currently at L171) and its scoped `.breadcrumb` styles — the layout owns the `{project} / {View}` breadcrumb now (D-11; review MEDIUM #3), so a page-level one would double it;** keep the direction-aware `groupedEdges` `$derived` and all label helpers reading `data.*`; keep the changelog lazy-loaded on demand (user-triggered, not part of switch refetch). Categorical stage/status badges use the shared `badge-variants` map. Render states: Loading → shadcn `Skeleton` title + metadata + section rows; Empty → "Spec not found in this project." card; Error → inline Retry `Card` reading `data.loadError`. **Skeleton-on-switch via streamed promises + `{#await}` (phase DEFAULT per review MEDIUM #4 — NOT a `switching` flag, NOT `$navigating`, Pitfall 3).**
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');const l=fs.readFileSync('src/routes/spec/[...slug]/+page.ts','utf8');if(!/await parent\(\)/.test(l)||!/getSpec/.test(l)||!/params/.test(l))process.exit(1);const p=fs.readFileSync('src/routes/spec/[...slug]/+page.svelte','utf8');if(/activeSlug/.test(p))process.exit(1);if(/onMount/.test(p))process.exit(1);if(/class=\"breadcrumb\"/.test(p))process.exit(1);if(!/\$props\(\)/.test(p)||!/[Ss]keleton/.test(p))process.exit(1)" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `spec/[...slug]/+page.ts` is a `PageLoad` keyed on `params.slug` with primary getSpec + try/caught secondaries, returning data or `loadError`.
    - `+page.svelte` consumes `data`, has NO `activeSlug` guard, no `onMount` fetch, and NO per-page `<nav class="breadcrumb">` (layout owns it — D-11); preserves `groupedEdges`/label helpers, and renders Skeleton/Empty/Error with UI-SPEC copy.
    - `pnpm build` and `pnpm test` pass.
  </acceptance_criteria>
  <done>Spec detail loads via load(), re-fetches on switch/slug change, handles not-found and error.</done>
</task>

<task type="auto">
  <name>Task 2: Decision detail load() — decision/[...slug]/+page.ts + restyle page with states</name>
  <files>web/src/routes/decision/[...slug]/+page.ts, web/src/routes/decision/[...slug]/+page.svelte</files>
  <read_first>
    - web/src/routes/decision/[...slug]/+page.svelte (loadDecision L26-45, statusLabel L16-24, linkedSpecs filter L31-37 — PATTERNS L209-216)
    - 05-RESEARCH.md "Architecture Pattern 1", Pitfall 3, Pitfall 6, L279
    - 05-UI-SPEC.md "Per-View State Matrix" (Decision: skeleton title+metadata / "Decision not found in this project." / Retry)
    - web/src/lib/components/MetadataBar.svelte (05-06), badge-variants.ts (05-01)
    - web/src/lib/components/ui/skeleton, card, button
  </read_first>
  <action>
    Create `web/src/routes/decision/[...slug]/+page.ts` as a `PageLoad({ params, parent, depends })`: `await parent()`, `depends('app:project')`, primary `decisionClient.getDecision({ slug: params.slug })` (may throw → not-found/loadError); non-critical `linkedSpecs` via `graphClient.listEdges` filtered to `DECIDED_IN` + `toId===slug` (mirror current L31-37), try/caught. Return `{ decision, linkedSpecs, loadError? }`. Keying the load on `params.slug` also fixes the pre-existing slug-nav bug (the old `onMount(() => loadDecision(slug))` never re-ran on in-project slug change — review LOW). Rewrite `+page.svelte` to consume `data` via `$props()`; **REMOVE the per-page `<nav class="breadcrumb">` block (currently at L48) and its scoped `.breadcrumb` styles — the layout owns the project breadcrumb (D-11; review MEDIUM #3);** keep `statusLabel()` in the component reading `data.*`; decision-status badge uses the shared `badge-variants` status map. Render states: Loading → `Skeleton` title + metadata; Empty → "Decision not found in this project." card; Error → inline Retry `Card` reading `data.loadError`. Remove the old `onMount`/`$effect` fetch and any stale-guard. **Skeleton-on-switch via streamed promises + `{#await}` (phase DEFAULT per review MEDIUM #4 — NOT a `switching` flag, NOT `$navigating`).**
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');const l=fs.readFileSync('src/routes/decision/[...slug]/+page.ts','utf8');if(!/await parent\(\)/.test(l)||!/getDecision/.test(l)||!/params/.test(l))process.exit(1);const p=fs.readFileSync('src/routes/decision/[...slug]/+page.svelte','utf8');if(/onMount/.test(p))process.exit(1);if(/class=\"breadcrumb\"/.test(p))process.exit(1);if(!/\$props\(\)/.test(p)||!/[Ss]keleton/.test(p))process.exit(1)" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `decision/[...slug]/+page.ts` is a `PageLoad` keyed on `params.slug` with primary getDecision + try/caught linkedSpecs, returning data or `loadError`.
    - `+page.svelte` consumes `data`, has no `onMount` fetch and NO per-page `<nav class="breadcrumb">` (layout owns it — D-11), keeps `statusLabel`, uses the categorical status badge, and renders Skeleton/Empty/Error with UI-SPEC copy.
    - `pnpm build` and `pnpm test` pass.
  </acceptance_criteria>
  <done>Decision detail loads via load(), re-fetches on switch/slug change, handles not-found and error.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

New files:
- `web/src/routes/spec/[...slug]/+page.ts` — spec detail universal load()
- `web/src/routes/decision/[...slug]/+page.ts` — decision detail universal load()

Modified files:
- `web/src/routes/spec/[...slug]/+page.svelte` — consumes `data`; removed `activeSlug`; removed per-page breadcrumb (D-11); Skeleton/Empty/Error
- `web/src/routes/decision/[...slug]/+page.svelte` — consumes `data`; removed per-page breadcrumb (D-11); Skeleton/Empty/Error
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| params.slug → RPC | Route slug flows into getSpec/getDecision requests |
| server spec/decision content → rendered page | Detail content rendered in migrated components |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information disclosure | stale/cross-project detail after switch | high | mitigate | load() re-runs on invalidateAll and on `params.slug` change; `activeSlug` guard removed in favor of load-driven data; not-found renders the empty state — no stale prior-project content retained. |
| T-05-XSS-11 | Tampering | spec/decision body text rendered | high | mitigate | Text via `{text}`/escaped bindings; no `{@html}` of server content (syntax highlighting UI-01 deferred — out of scope). |
| T-05-15 | Denial of Service | load error hitting +error.svelte | medium | mitigate | Catch in load(), return `loadError`, render inline Retry card (RESEARCH L279). |
</threat_model>

<verification>
- `pnpm -C web build` and `pnpm -C web test` pass.
- `task web:build && task build && task check` pass.
- Manual UAT: switching to a project without the spec/decision shows the not-found empty state; switch returns view to skeleton then new data; no stale content.
</verification>

<success_criteria>
- Spec and Decision detail load via `+page.ts` keyed on slug, re-fetch on switch/slug change, render all three states, and drop the manual stale-guard (D-01/D-02/D-09).
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-11-SUMMARY.md` when done.
</output>
