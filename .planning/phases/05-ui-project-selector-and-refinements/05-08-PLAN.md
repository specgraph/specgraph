---
phase: 05-ui-project-selector-and-refinements
plan: 08
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/lib/components/DiffView.svelte
  - web/src/lib/components/VersionCompare.svelte
autonomous: true
requirements: [D-12, D-13]
user_setup: []

must_haves:
  truths:
    - "DiffView and VersionCompare render on shadcn Card/Select/Tabs + Slate tokens with no scoped navy/blue CSS."
    - "Diff add/remove lines use dark-aware muted green/red tints; diff logic preserved."
    - "VersionCompare version picker uses Select/Tabs and frames DiffView in a Card."
  artifacts:
    - "web/src/lib/components/DiffView.svelte (Card + font-mono diff)"
    - "web/src/lib/components/VersionCompare.svelte (Select/Tabs + Card frame)"
  key_links:
    - "VersionCompare version selection → DiffView render inside Card frame"
---

<objective>
Migrate `DiffView` and `VersionCompare` to shadcn primitives + Slate tokens, keeping the diff-rendering logic intact (D-12/D-13). Diff add/remove lines use dark-aware muted tints; the version picker uses `Select`/`Tabs`.

Purpose: These render spec version history on the spec-detail page; migrating them cleanly enables Wave 3 restyle.
Output: Two token-based history components.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DiffView to Card + font-mono with dark-aware diff tints</name>
  <files>web/src/lib/components/DiffView.svelte</files>
  <read_first>
    - web/src/lib/components/DiffView.svelte (diff logic to keep)
    - web/src/lib/components/ui/card
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (DiffView → Card, font-mono, add/remove via bg-green-*/bg-red-* muted tints, dark-aware)
  </read_first>
  <action>
    Re-implement `DiffView.svelte`: keep the diff-computation logic and props exactly; wrap output in a shadcn `Card` with `font-mono`. Added/removed lines use muted green/red tint utility classes with dark-mode variants (`bg-green-*`/`bg-red-*` light + `dark:` pairs) so they stay legible in dark mode. Delete the scoped `<style>`; use token utility classes. Keep Svelte 5 runes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/DiffView.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/font-mono/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; wrapped in `Card` with `font-mono`; add/remove lines use dark-aware green/red tints; diff logic + props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>DiffView on shadcn Card with dark-aware diff tints.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate VersionCompare to Select/Tabs + Card frame</name>
  <files>web/src/lib/components/VersionCompare.svelte</files>
  <read_first>
    - web/src/lib/components/VersionCompare.svelte (version-pick + DiffView usage)
    - web/src/lib/components/ui/select, tabs, card
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (VersionCompare → Select/Tabs for version pick + Card frame around DiffView)
  </read_first>
  <action>
    Re-implement `VersionCompare.svelte`: version selection uses shadcn `Select` (or `Tabs`); the compared `DiffView` is framed in a shadcn `Card`. Preserve props/behavior and the version-selection logic. Delete the scoped `<style>`; use token utility classes. Keep Svelte 5 runes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/VersionCompare.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Ss]elect|[Tt]abs/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; uses shadcn `Select`/`Tabs` + `Card`; version-selection logic + props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>VersionCompare on shadcn Select/Tabs with a Card-framed DiffView.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

Modified files:
- `web/src/lib/components/DiffView.svelte` — shadcn Card + font-mono, dark-aware diff tints
- `web/src/lib/components/VersionCompare.svelte` — shadcn Select/Tabs + Card frame
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server spec-version content → diff render | Version text diffed and rendered line-by-line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-XSS-08 | Tampering/Info-disclosure | spec version text rendered in DiffView | high | mitigate | Render diff lines via Svelte `{text}` bindings (auto-escaped); no `{@html}` of server content (syntax highlighting UI-01 is deferred — keep it out). |
| T-05-13 | Tampering | vendored `ui/` primitive edits | low | accept | Compose/wrap; don't hand-edit `ui/`. |
</threat_model>

<verification>
- `pnpm -C web build` passes; no `<style>`/hex in either file.
- `task web:build && task build && task check` pass.
- Manual UAT: version pick + diff render legibly in light and dark.
</verification>

<success_criteria>
- DiffView/VersionCompare migrated to shadcn with tokens, dark-aware diff tints, and preserved diff/version logic; app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-08-SUMMARY.md` when done.
</output>
