---
phase: 05-ui-project-selector-and-refinements
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/lib/components/SpecTable.svelte
  - web/src/lib/components/StatsBar.svelte
  - web/src/lib/components/FunnelBar.svelte
autonomous: true
requirements: [D-12, D-13, D-10]
user_setup: []

must_haves:
  truths:
    - "SpecTable, StatsBar, FunnelBar render on shadcn primitives + Slate tokens with no scoped navy/blue CSS."
    - "SpecTable stage/priority badges use the shared categorical badge map (D-10 palette rule); sort runes preserved."
    - "StatsBar shows Display-size numbers; FunnelBar segments use muted/accent tokens."
  artifacts:
    - "web/src/lib/components/SpecTable.svelte (shadcn Table + Badge)"
    - "web/src/lib/components/StatsBar.svelte (grid of Card)"
    - "web/src/lib/components/FunnelBar.svelte (div segments + Badge)"
  key_links:
    - "badge-variants.ts stage map → SpecTable stage Badge classes"
    - "spec slug links use --primary accent (UI-SPEC accent rule #5)"
---

<objective>
Migrate the dashboard data-display components — `SpecTable`, `StatsBar`, `FunnelBar` — to shadcn primitives + Slate tokens, preserving props, sort logic, and Svelte 5 runes (D-12/D-13). SpecTable stage/priority badges use the shared categorical map (D-10).

Purpose: These render the Dashboard; migrating them cleanly lets the Wave 3 dashboard load-ification restyle without contract changes.
Output: Three token-based data-display components.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SpecTable to shadcn Table + categorical stage Badge</name>
  <files>web/src/lib/components/SpecTable.svelte</files>
  <read_first>
    - web/src/lib/components/SpecTable.svelte (keep sort runes L10-52, markup L55-81; stage badges L145-151 are categorical — PATTERNS L296)
    - web/src/lib/components/badge-variants.ts (stage map from 05-01)
    - web/src/lib/components/ui/table, badge
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (SpecTable → Table + Badge), "Color → Accent reserved list" (slug links use accent), "Categorical badge palette"
  </read_first>
  <action>
    Re-implement `SpecTable.svelte` on shadcn `Table`. Keep the existing sort `$state`/`$derived` runes and all props/behavior. Stage and priority badges are CATEGORICAL — use the shared `badge-variants.ts` stage map (fixed light/dark pairs), NOT `--primary`. Spec-slug links use the `--primary` accent (UI-SPEC accent rule #5). Delete the scoped `<style>`; use token utility classes for header rows (`bg-muted`), borders (`border-border`), body text. Keep Svelte 5 runes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/SpecTable.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Tt]able/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>` block, no 6-digit hex; uses shadcn `Table` + categorical stage/priority `Badge` from `badge-variants`.
    - Sort runes and props preserved; slug links use accent.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>SpecTable on shadcn Table with categorical badges and preserved sorting.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate StatsBar to a grid of shadcn Cards</name>
  <files>web/src/lib/components/StatsBar.svelte</files>
  <read_first>
    - web/src/lib/components/StatsBar.svelte (keep `cards` $derived L13-20, markup L23-30 — PATTERNS L297)
    - web/src/lib/components/ui/card
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (StatsBar → grid of Cards, Display numbers), "Typography" (Display 30px/text-3xl)
  </read_first>
  <action>
    Re-implement `StatsBar.svelte` as a grid of shadcn `Card`s. Keep the `cards` `$derived` and props. Large stat numbers use the Display role (`text-3xl` weight 600) per UI-SPEC Typography. Per-card accent colors from the old hex become token/categorical stat accents (muted/secondary tokens; reserve `--primary` per accent budget). Delete the scoped `<style>`; use `gap-6` grid spacing per UI-SPEC. Keep runes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/StatsBar.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Cc]ard/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; uses shadcn `Card` grid; numbers use `text-3xl`.
    - `cards` `$derived` and props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>StatsBar as a shadcn Card grid with Display-size numbers.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate FunnelBar to token div-segments + Badge counts</name>
  <files>web/src/lib/components/FunnelBar.svelte</files>
  <read_first>
    - web/src/lib/components/FunnelBar.svelte (current segments + counts)
    - web/src/lib/components/ui/badge
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (FunnelBar → div segments using muted/accent tokens + Badge counts; no plain CSS)
  </read_first>
  <action>
    Re-implement `FunnelBar.svelte` as horizontal stage segments built from `div`s using muted/accent token utility classes, with `Badge` count chips. Preserve props/behavior and Svelte 5 runes. Delete the scoped `<style>`; no plain CSS color values.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/FunnelBar.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; segments use token utilities + `Badge` counts.
    - Props/behavior preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>FunnelBar on token utilities with Badge counts.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

Modified files:
- `web/src/lib/components/SpecTable.svelte` — shadcn Table + categorical stage/priority Badge
- `web/src/lib/components/StatsBar.svelte` — grid of shadcn Card, Display numbers
- `web/src/lib/components/FunnelBar.svelte` — token div-segments + Badge counts
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server spec data → SpecTable rows | Spec titles/slugs rendered in the table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-XSS-05 | Tampering/Info-disclosure | spec title/slug rendered in SpecTable | high | mitigate | Use Svelte `{text}` bindings (auto-escaped); no `{@html}` of server data. |
| T-05-08 | Tampering | vendored `ui/` primitive edits | low | accept | Compose/wrap; don't hand-edit `ui/`. |
</threat_model>

<verification>
- `pnpm -C web build` passes; no `<style>`/hex in the three files.
- `task web:build && task build && task check` pass.
- Manual UAT: table sorting, stat cards, and funnel render correctly in both themes.
</verification>

<success_criteria>
- SpecTable/StatsBar/FunnelBar migrated to shadcn with tokens, categorical badges, and preserved sort/derive logic; app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-05-SUMMARY.md` when done.
</output>
