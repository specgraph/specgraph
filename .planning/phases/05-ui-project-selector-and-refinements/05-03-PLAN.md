---
phase: 05-ui-project-selector-and-refinements
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - web/src/routes/+layout.ts
  - web/src/routes/+layout.svelte
  - web/src/app.html
  - web/src/lib/components/ModeToggle.svelte
autonomous: true
requirements: [D-01, D-02, D-03, D-07, D-08, D-11, D-13, D-14]
user_setup: []

must_haves:
  truths:
    - "Auth + project bootstrap runs in +layout.ts load() so page loads see the resolved project before firing RPCs (D-02)."
    - "The switch handler persists project.current (D-03) then calls invalidateAll() — this refreshes the shell/layout load and shell state. Full project-scoped page data re-fetch (D-01 end-to-end) is completed in Wave 3 (05-10..13) when pages gain +page.ts loads; this plan does NOT claim end-to-end data re-fetch on switch."
    - "The project Select fires switchProject via onValueChange (not bind:value alone) so every user change guarantees invalidateAll() (review suggestion #6)."
    - "Zero projects → no selector, main-area empty state; one project → static label; many → shadcn Select sorted alpha (D-07/D-08)."
    - "The layout is the SINGLE owner of the {project} / {View} active-project breadcrumb (D-11); Wave-3 page plans remove their now-duplicate per-page breadcrumbs (Keys excluded, D-09)."
    - "Light/dark toggle works with no theme flash on reload (D-14)."
  artifacts:
    - "web/src/routes/+layout.ts (LayoutLoad bootstrap)"
    - "web/src/routes/+layout.svelte (shadcn nav, Select/label/empty, switch handler, breadcrumb, ModeWatcher)"
    - "web/src/lib/components/ModeToggle.svelte"
    - "web/src/app.html (FOUC-guard script)"
  key_links:
    - "+layout.ts load resolves project default → pages await parent() → RPCs carry resolved header"
    - "Select onValueChange → switchProject → project.current setter (localStorage) → invalidateAll() → all load() re-run"
    - "app.html inline script reads mode-watcher localStorage key → sets .dark before first paint"
    - "layout-owned Breadcrumb {project} / {View} → single active-project indicator (Wave-3 pages delete per-page breadcrumbs)"
---

<objective>
Rebuild the app shell on shadcn: move auth + project bootstrap into `+layout.ts` `load()` (D-02, fixes Pitfall 6), rebuild the nav on Slate tokens with the shadcn project `Select` / single-label / zero-empty-state branches (D-07/D-08), wire the project-switch handler with `invalidateAll()` (D-01/D-03), add the active-project breadcrumb indicator (D-11), and activate dark mode (`ModeWatcher` + `ModeToggle` + FOUC guard, D-14). This unblocks Wave 3 page load-ification.

Purpose: The shell is the single seam where project switching and theming become real, and where page `load()` functions can `await parent()` for a resolved project.
Output: A shadcn-styled shell with a working selector, switch-on-invalidate, active-project indicator, and dark mode.

**Scope boundary (review HIGH #2):** After this plan the shell *switches and the layout load + shell state refresh*, but the project-scoped **pages still fetch via `onMount`/`$effect`** until they are load-ified in Wave 3 (05-10..13). Full D-01 acceptance (every project-scoped view re-fetches on switch) is therefore NOT claimed here — it is completed by Wave 3. Do not present intermediate "switch works end-to-end" as done at this stage.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModeToggle component + FOUC-guard script (D-14)</name>
  <files>web/src/lib/components/ModeToggle.svelte, web/src/app.html</files>
  <read_first>
    - 05-RESEARCH.md "Architecture Pattern 4" (ModeToggle body), Pitfall 5 (FOUC guard + localStorage key), "Runtime State Inventory" (mode-watcher key)
    - 05-PATTERNS.md "ModeToggle.svelte" assignment (nearest analog SearchFilter/TabBar shape)
    - web/src/app.html (current head — insert guard before %sveltekit.head%)
    - web/src/lib/components/ui/button (generated Button primitive to import)
    - 05-UI-SPEC.md "Copywriting Contract" (theme toggle aria-label)
  </read_first>
  <action>
    Create `web/src/lib/components/ModeToggle.svelte` per RESEARCH Pattern 4: import `toggleMode` from `mode-watcher`, sun/moon icons from `@lucide/svelte/icons/sun` and `@lucide/svelte/icons/moon`, and the shadcn `Button` (`variant="outline"`, `size="icon"`, `aria-label="Toggle theme"` per UI-SPEC). Keep Svelte 5 runes. Add a small blocking inline `<script>` in `web/src/app.html` `<head>` BEFORE `%sveltekit.head%` that reads mode-watcher's localStorage key and toggles `document.documentElement.classList` `.dark` before first paint (RESEARCH Pitfall 5). First VERIFY the exact key mode-watcher@1.1.0 writes (default `mode-watcher-mode`) against the installed package and match it exactly — [ASSUMED default key; confirm before relying on it].
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');const t=fs.readFileSync('src/lib/components/ModeToggle.svelte','utf8');if(!/toggleMode/.test(t)||!/aria-label=\"Toggle theme\"/.test(t))process.exit(1);const h=fs.readFileSync('src/app.html','utf8');if(!/classList/.test(h)||h.indexOf('<script')>h.indexOf('%sveltekit.head%'))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `ModeToggle.svelte` renders a shadcn icon `Button` with `aria-label="Toggle theme"` calling `toggleMode`.
    - `app.html` contains a blocking inline script (before `%sveltekit.head%`) that reads the confirmed mode-watcher localStorage key and sets `.dark` pre-paint.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>Theme toggle exists and no-flash guard is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Move auth + project bootstrap into +layout.ts load() (D-02)</name>
  <files>web/src/routes/+layout.ts</files>
  <read_first>
    - web/src/routes/+layout.ts (current ssr/prerender flags — keep verbatim)
    - web/src/routes/+layout.svelte (current onMount bootstrap L12-26 — the logic being moved; PATTERNS L220-229)
    - web/src/lib/auth.svelte (checkAuth + auth state)
    - web/src/lib/project.svelte.ts (loadProjects — now with D-04 precedence from 05-02)
    - 05-RESEARCH.md Pitfall 6 (load runs before onMount), "Architecture Diagram"
  </read_first>
  <action>
    Extend `web/src/routes/+layout.ts`: KEEP `export const ssr = false;` and `export const prerender = false;` verbatim (D-02 static SPA — no `+layout.server.ts`, no server load). Add `export const load: LayoutLoad` that runs `await checkAuth()`, then `if (auth.authenticated) await loadProjects()` (mirrors the current onMount body), and returns `{ authenticated: auth.authenticated }`. This ensures universal page loads that `await parent()` see the resolved project default before issuing RPCs (Pitfall 6). Parse the `auth_error` query param handling can stay in the component (it needs `window`/`history`). Do not fetch page data here — only auth + project resolution.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/routes/+layout.ts','utf8');if(!/export const ssr = false/.test(t)||!/export const prerender = false/.test(t)||!/export const load/.test(t)||!/loadProjects/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `+layout.ts` retains `ssr=false` and `prerender=false` and adds a `LayoutLoad` that awaits `checkAuth()` then `loadProjects()` and returns `{ authenticated }`.
    - No `+layout.server.ts` created; no server `load`.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>Project default is resolved in load() before any page RPC fires.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild +layout.svelte shell — nav, selector, switch, breadcrumb, ModeWatcher (D-01/D-03/D-07/D-08/D-11/D-14)</name>
  <files>web/src/routes/+layout.svelte</files>
  <read_first>
    - web/src/routes/+layout.svelte (current nav + selector branch L38-63, style block L65-126 to retire — PATTERNS L233-255)
    - 05-RESEARCH.md "Architecture Pattern 2" (switch handler), Pattern 4 (ModeWatcher import)
    - 05-UI-SPEC.md "Copywriting Contract" (selector aria-label, zero/one/many states, empty-state copy, active-project breadcrumb), "Color → Accent reserved list", "Per-View State Matrix" (project-switch behavior)
    - 05-PATTERNS.md "+layout.svelte (REBUILD nav on shadcn)", "Project header propagation (UNCHANGED)"
    - web/src/lib/components/ui/select, web/src/lib/components/ui/breadcrumb, web/src/lib/components/ui/separator
  </read_first>
  <action>
    Rebuild `web/src/routes/+layout.svelte`. Ensure `import '../app.css';` is present (it was added in 05-01 Task 3 — KEEP it; do not remove) and add `<ModeWatcher />` (from `mode-watcher`). Consume the `+layout.ts` load `data.authenticated` for the auth gate instead of the removed `onMount`/`ready` bootstrap (keep the `LoginModal` gate and the `auth_error` query handling in a minimal `onMount`). Rebuild the nav using Slate tokens (retire the entire `<style>` block incl. navy `#1a1a2e` and `.project-picker`): nav links keep `class:active` logic but the active link uses `--primary` (UI-SPEC accent rule #2); place `<ModeToggle />` in the nav's spacer zone. Implement the D-08 selector branch with shadcn primitives: `project.available.length > 1` → shadcn `Select` (`aria-label="Select project"`, options sorted alpha per D-05); zero → render NO selector; exactly one → static `text-sm text-muted-foreground` label; zero → the D-07 zero-projects main-area empty state is shown by the main region (heading "No projects found", body per UI-SPEC copy). **Wire the switch via explicit `onValueChange` → `switchProject(slug)` (review suggestion #6 — do NOT rely on `bind:value` alone, which is the original bug pattern that updated `project.current` without invalidating).** `switchProject(slug)` sets `project.current = slug` (setter persists localStorage, D-03) then `await invalidateAll()` (D-03; and D-01 once Wave-3 pages have loads) so `+layout.ts` and every `+page.ts` re-run with the new `X-Specgraph-Project` header. Accepted tradeoff (review MED): `invalidateAll()` re-runs `checkAuth()` + `loadProjects()` each switch — acceptable per the CONTEXT-locked `invalidateAll()` mechanism; the `depends('app:project')` escape hatch is wired in Wave-3 loads for a one-line switch to `invalidate('app:project')` if latency ever matters. Add the D-11 active-project indicator — **the layout is the SINGLE owner of this breadcrumb**: a shadcn `Breadcrumb` in the content header zone rendering `{project.current} / {View}` where the project segment is `text-foreground font-semibold` and the trailing view is `text-muted-foreground`; it reads `project.current` reactively so it updates on switch. **Conditionally SUPPRESS this project breadcrumb on the user-scoped `/keys` route** (e.g. gate on `$page.url.pathname !== '/keys'`) — Keys is user-scoped (D-09) and must not show a project breadcrumb (review MEDIUM #3; Keys keeps only its own user-scoped breadcrumb in 05-12). The per-page `<nav class="breadcrumb">` blocks in constitution/spec/decision are removed by their Wave-3 plans (05-11, 05-13) to avoid double breadcrumbs. Do NOT bind switch skeletons to `$navigating` (Pitfall 3 — that is the pages' job in Wave 3). Optional `sonner` toast "Switched to {project}" is allowed but not required.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/routes/+layout.svelte','utf8');if(/#1a1a2e/.test(t))process.exit(1);if(!/invalidateAll/.test(t)||!/ModeWatcher/.test(t)||!/import '..\/app.css'/.test(t))process.exit(1)" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `+layout.svelte` imports `../app.css`, renders `<ModeWatcher />` and `<ModeToggle />`, and the old `<style>` navy palette (`#1a1a2e`, `.project-picker`) is gone.
    - Selector branches match D-08 (Select for many with `aria-label="Select project"`, muted label for one, none for zero); Select options reflect the alpha-sorted `project.available`.
    - The Select uses explicit `onValueChange` → `switchProject` (not `bind:value` alone); `switchProject` sets `project.current` then `await invalidateAll()` (D-03).
    - A layout-owned `Breadcrumb` active-project indicator renders `{project} / {View}` and updates reactively (D-11); no additional project breadcrumb is expected on pages (per-page ones removed in Wave 3).
    - `pnpm build` and `pnpm test` pass.
  </acceptance_criteria>
  <done>Shell rebuilt on shadcn with a working, persisted, invalidate-on-switch selector, active-project breadcrumb, and dark mode.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

New files:
- `web/src/lib/components/ModeToggle.svelte` — theme toggle button (D-14)

Modified files:
- `web/src/routes/+layout.ts` — `LayoutLoad` auth+project bootstrap (D-02)
- `web/src/routes/+layout.svelte` — shadcn nav, Select/label/empty selector, switch handler, active-project breadcrumb, ModeWatcher; retired navy `<style>`
- `web/src/app.html` — blocking FOUC-guard inline script (D-14)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → client | `specgraph-project` and mode-watcher key seed selection/theme |
| client Select → header | Project choice becomes `X-Specgraph-Project` on every RPC |
| client → server RPC | ConnectRPC POSTs cross into the Go server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Elevation of Privilege | `X-Specgraph-Project` header chosen client-side | high | accept | Header is advisory. `ProjectMiddleware` only injects the header value into request context; actual scoping is enforced per-handler via `storage.Scoper`/`scopeStore`, not by header validation. The client may request any valid kebab-case slug; access is gated by the scoper's `Scoped` implementation under the authenticated identity. Acceptable for the trusted single-tenant UI; noted for any future multi-tenant hardening. No client-side change bypasses server scoping. |
| T-05-05 | Information disclosure | stale previous-project data lingering after switch | medium | mitigate | `invalidateAll()` re-runs every load; Wave 3 pages return to skeleton then new-project data. Shell exposes no page data itself. |
| T-05-06 | Tampering | FOUC inline script reads localStorage | low | accept | Reads only a theme string; toggles a CSS class; no injection surface (value not interpolated into HTML). |
</threat_model>

<verification>
- `pnpm -C web build` and `pnpm -C web test` pass.
- `task web:build && task build && task check` pass (static bundle embeds; no Docker).
- Manual UAT (visual, D-14/D-11/D-07/D-08): toggle flips light/dark with no reload flash; layout breadcrumb shows the active project; multi/single/zero selector states render per UI-SPEC.
- **Verification appetite (review MEDIUM #6):** D-07/D-08/D-11 (and D-01/D-10 in Wave 3) are accepted via **manual UAT**, not component tests. A Svelte component-test harness (`@testing-library/svelte` + jsdom) is deliberately **out of appetite** for this phase (today only plain `.ts` unit tests exist — `oidc.test.ts`, `keys.test.ts`); adopting one is recorded as a follow-up in VALIDATION.md, not a blocker here. Automated coverage this phase = `project.test.ts` (D-04/05/06) + the Go `_server` test (05-02) + `pnpm build`/`task check` smoke gates.
</verification>

<success_criteria>
- Shell rebuilt on shadcn; project switch persists + invalidates; selector honors D-07/D-08; active-project indicator present (D-11); dark mode works without flash (D-14); app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-03-SUMMARY.md` when done.
</output>
