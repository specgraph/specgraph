---
phase: 05-ui-project-selector-and-refinements
plan: 10
type: execute
wave: 3
depends_on: ["05-03", "05-05", "05-09"]
files_modified:
  - web/src/routes/+page.ts
  - web/src/routes/+page.svelte
  - web/src/routes/graph/+page.ts
  - web/src/routes/graph/+page.svelte
autonomous: true
requirements: [D-01, D-02, D-09, D-13, D-10]
user_setup: []

must_haves:
  truths:
    - "Dashboard and Graph fetch data in universal +page.ts load() (not onMount/$effect), so invalidateAll() on switch re-fetches with the new project header (D-01/D-02/D-09)."
    - "Each view renders Loading (Skeleton), Empty, and Error (Retry card) states per the UI-SPEC State Matrix."
    - "No stale previous-project data remains visible after a switch skeleton clears."
    - "The active-project indicator (from the shell) reflects the current project on both pages (D-11)."
  artifacts:
    - "web/src/routes/+page.ts (dashboard load)"
    - "web/src/routes/graph/+page.ts (graph load)"
    - "web/src/routes/+page.svelte (consumes data prop; skeleton/empty/error)"
    - "web/src/routes/graph/+page.svelte (consumes data prop; skeleton/empty/error)"
  key_links:
    - "+page.ts await parent() → resolved project → RPCs carry X-Specgraph-Project → invalidateAll re-runs"
    - "load returns loadError (caught in load) → inline Retry Card, not +error.svelte"
---

<objective>
Load-ify the Dashboard (`/`) and Graph (`/graph`) views: move their data fetches from `onMount`/`$effect` into universal `+page.ts` `load()` functions returning a `data` prop, so a project switch's `invalidateAll()` re-fetches with the new `X-Specgraph-Project` header (D-01/D-02/D-09). Render the shadcn Loading/Empty/Error states per the UI-SPEC State Matrix.

Purpose: This is the core reactive-switch behavior for the two dashboard views, on the components migrated in Wave 2.
Output: Two load-ified, state-complete pages that re-fetch on switch with no stale data.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard load() — +page.ts + restyle +page.svelte to data prop with states</name>
  <files>web/src/routes/+page.ts, web/src/routes/+page.svelte</files>
  <read_first>
    - web/src/routes/+page.svelte (current onMount loadDashboard L60-95, $state L25-35, derived helpers L40-58 — PATTERNS L128-166)
    - 05-RESEARCH.md "Architecture Pattern 1" (load skeleton), Pitfall 3 (skeleton on switch, no $navigating), Pitfall 6 (await parent), L279 (catch in load → loadError)
    - 05-UI-SPEC.md "Per-View State Matrix" (Dashboard: skeleton stat cards + rows / "Nothing here yet" / Retry), "Copywriting Contract" (empty + error copy)
    - web/src/lib/components/ui/skeleton, card, button
    - web/src/lib/components/SpecTable.svelte, StatsBar.svelte, FunnelBar.svelte (migrated in 05-05), Graph.svelte (05-09)
  </read_first>
  <action>
    Create `web/src/routes/+page.ts` as a `PageLoad`: `await parent()` (resolve project default first — Pitfall 6), `depends('app:project')` (cheap targeted-invalidate insurance), then run the exact 5-way `Promise.all` from the current `loadDashboard` (specs/ready/fullGraph/decisions/checkDrift) and return the mapped data object. Catch errors INSIDE `load()` and return a `loadError` field (do NOT let it reach `+error.svelte`) per RESEARCH L279. Rewrite `web/src/routes/+page.svelte` to consume `let { data } = $props()` and port the existing `$derived` helpers (recentSpecs/priorityGroups/decisionSpecCounts) to read `data.*`; remove the `onMount` fetch and the `$state` data block. Render the three states per the State Matrix: **Loading → shadcn `Skeleton` stat cards + table rows driven by streamed promises from `load()` consumed with `{#await}` — this is the phase DEFAULT skeleton-on-switch strategy (review MEDIUM #4: idiomatic SvelteKit streamed promises, NOT a `switching` flag and NOT `$navigating`, Pitfall 3);** Empty → "Nothing here yet" card when zero specs & decisions (UI-SPEC copy); Error → inline Retry `Card` with `Button variant="outline"` reading `data.loadError`, whose Retry calls `invalidateAll()`. No stale previous-project data may remain after the skeleton clears.
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');if(!fs.existsSync('src/routes/+page.ts'))process.exit(1);const l=fs.readFileSync('src/routes/+page.ts','utf8');if(!/await parent\(\)/.test(l)||!/loadError/.test(l))process.exit(1);const p=fs.readFileSync('src/routes/+page.svelte','utf8');if(/onMount/.test(p))process.exit(1);if(!/\$props\(\)/.test(p)||!/[Ss]keleton/.test(p))process.exit(1)" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `+page.ts` is a `PageLoad` that `await parent()`, `depends('app:project')`, runs the 5-way `Promise.all`, and returns data or a caught `loadError`.
    - `+page.svelte` consumes `data` via `$props()`, has no `onMount`/`$state` data fetch, and renders Skeleton/Empty/Error states with the UI-SPEC copy.
    - Switch skeletons are NOT bound to `$navigating`.
    - `pnpm build` and `pnpm test` pass.
  </acceptance_criteria>
  <done>Dashboard fetches via load(), re-fetches on switch, and renders all three states with no stale data.</done>
</task>

<task type="auto">
  <name>Task 2: Graph load() — graph/+page.ts + restyle graph/+page.svelte with states</name>
  <files>web/src/routes/graph/+page.ts, web/src/routes/graph/+page.svelte</files>
  <read_first>
    - web/src/routes/graph/+page.svelte (current loadGraph L15-26, empty guard L35, local filterText L—keep — PATTERNS L170-177)
    - 05-RESEARCH.md "Architecture Pattern 1", Pitfall 3, Pitfall 6, L279
    - 05-UI-SPEC.md "Per-View State Matrix" (Graph: skeleton Card block / "Nothing here yet — no graph nodes for this project." / Retry)
    - web/src/lib/components/ui/skeleton, card, button
    - web/src/lib/components/Graph.svelte (migrated in 05-09)
  </read_first>
  <action>
    Create `web/src/routes/graph/+page.ts` as a `PageLoad`: `await parent()`, `depends('app:project')`, call `graphClient.getFullGraph({})`, return `{ nodes, edges }` (or a caught `loadError`). Rewrite `web/src/routes/graph/+page.svelte` to consume `data` via `$props()`; KEEP the local `filterText = $state('')` UI state (only the fetch moves). Render the three states: Loading → shadcn `Skeleton` `Card` block (graph canvas footprint); Empty → "Nothing here yet — no graph nodes for this project." card when `data.nodes.length === 0`; Error → inline Retry `Card` reading `data.loadError`. **Skeleton-on-switch strategy: default to streamed promises + `{#await}` like the dashboard; the module-level `switching`-flag fallback is EXPLICITLY ALLOWED HERE ONLY (review MEDIUM #4) because the dagre layout consumes the fully-resolved node/edge set and streaming a partial graph into the layout is awkward — pick whichever keeps the dagre render correct, but NEVER `$navigating` (Pitfall 3).** Remove the old `onMount`/`$effect` fetch.
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');if(!fs.existsSync('src/routes/graph/+page.ts'))process.exit(1);const l=fs.readFileSync('src/routes/graph/+page.ts','utf8');if(!/await parent\(\)/.test(l)||!/getFullGraph/.test(l))process.exit(1);const p=fs.readFileSync('src/routes/graph/+page.svelte','utf8');if(/onMount/.test(p))process.exit(1);if(!/\$props\(\)/.test(p)||!/[Ss]keleton/.test(p))process.exit(1)" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `graph/+page.ts` is a `PageLoad` that `await parent()`, `depends('app:project')`, fetches the graph, returns data or `loadError`.
    - `graph/+page.svelte` consumes `data`, keeps `filterText` local, has no `onMount` fetch, and renders Skeleton/Empty/Error with UI-SPEC copy.
    - `pnpm build` and `pnpm test` pass.
  </acceptance_criteria>
  <done>Graph fetches via load(), re-fetches on switch, renders all three states.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

New files:
- `web/src/routes/+page.ts` — dashboard universal load()
- `web/src/routes/graph/+page.ts` — graph universal load()

Modified files:
- `web/src/routes/+page.svelte` — consumes `data` prop; Skeleton/Empty/Error states
- `web/src/routes/graph/+page.svelte` — consumes `data` prop; Skeleton/Empty/Error states
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client Select switch → load re-run | invalidateAll re-issues RPCs with new project header |
| server spec/graph data → rendered page | Spec/graph content rendered in migrated components |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information disclosure | stale previous-project data after switch | high | mitigate | load() re-runs on invalidateAll; skeleton clears only when new-project data resolves; no stale `$state` retained (data comes solely from `data` prop). Verified by the State Matrix contract. |
| T-05-15 | Denial of Service | error in load reaching +error.svelte (whole-app boundary) | medium | mitigate | Catch inside load(), return `loadError`, render per-view inline Retry card (RESEARCH L279). |
| T-05-XSS-10 | Tampering | spec/graph text rendered | high | mitigate | Text via `{text}`/escaped bindings in migrated components; no `{@html}` of server data. |
</threat_model>

<verification>
- `pnpm -C web build` and `pnpm -C web test` pass.
- `task web:build && task build && task check` pass.
- Manual UAT: switching projects returns each view to skeleton then new-project data; empty/error states render per State Matrix; active-project breadcrumb updates.
- **Verification appetite (review MEDIUM #6):** the end-to-end switch-refetch behavior (D-01) is accepted via **manual UAT**; a Svelte component-test harness (`@testing-library/svelte`/jsdom) is out of appetite this phase (recorded in VALIDATION.md). The automated `load()` seam is covered structurally by the `+page.ts` verify checks and `pnpm build`; the visual re-fetch is confirmed manually.
</verification>

<success_criteria>
- Dashboard and Graph load via `+page.ts`, re-fetch on `invalidateAll()` with the new project header, show all three states, and leave no stale data (D-01/D-02/D-09).
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-10-SUMMARY.md` when done.
</output>
