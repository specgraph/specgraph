---
phase: 05-ui-project-selector-and-refinements
plan: 12
type: execute
wave: 3
depends_on: ["05-03", "05-07"]
files_modified:
  - web/src/routes/keys/+page.svelte
autonomous: true
requirements: [D-09, D-12, D-13]
user_setup: []

must_haves:
  truths:
    - "The Keys page renders on shadcn primitives + Slate tokens with no scoped navy/blue CSS."
    - "Keys keeps its existing user-scoped data flow — it is NOT load-ified and NOT project-scoped (D-09)."
    - "The revoke path uses the migrated RevealKeyModal AlertDialog destructive contract."
  artifacts:
    - "web/src/routes/keys/+page.svelte (shadcn restyle only)"
  key_links:
    - "keys page → migrated RevealKeyModal (05-07) for reveal/revoke"
---

<objective>
Restyle the Keys page (`/keys`) to shadcn + Slate tokens as a visual-only migration. Per D-09 the Keys page is USER-scoped, not project-scoped: it is explicitly EXCLUDED from the `load()`/`invalidateAll()` project-switch refactor. Only its visual layer changes; its existing data flow and CSRF-protected mutations stay exactly as they are.

Purpose: Visual consistency across the app without altering the deliberately user-scoped Keys data model.
Output: A shadcn-styled Keys page with unchanged behavior.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Keys page to shadcn tokens (no data-scope change)</name>
  <files>web/src/routes/keys/+page.svelte</files>
  <read_first>
    - web/src/routes/keys/+page.svelte (current markup + user-scoped data flow — PATTERNS L43 "restyle-only, data-scope OUT of scope")
    - web/src/lib/components/RevealKeyModal.svelte (migrated in 05-07 — reuse for reveal/revoke)
    - web/src/lib/components/ui/table, card, button, input
    - 05-UI-SPEC.md "Component Inventory & Migration Map", "Copywriting Contract" (Revoke AlertDialog copy), "Color"/"Typography"/"Spacing"
    - 05-CONTEXT.md D-09 (Keys excluded from project-scope refactor)
  </read_first>
  <action>
    Restyle `web/src/routes/keys/+page.svelte` onto shadcn primitives (`Table` for the key list, `Card` for sections, `Button` for actions, `Input` for any mint form) with Slate token utility classes. Reuse the migrated `RevealKeyModal` (05-07) for the reveal and destructive-revoke `AlertDialog` (UI-SPEC copy: title "Revoke this key?", confirm "Revoke" `variant="destructive"`, cancel "Cancel"). Delete the scoped `<style>` navy/blue palette; use tokens. CRITICAL: do NOT add a `+page.ts` load(), do NOT move fetches into `load()`, and do NOT wire this page to `invalidateAll()`/project switching — Keys stays user-scoped per D-09. Keep all existing data-fetch, CSRF-protected mint/rotate/revoke behavior, and Svelte 5 runes exactly as they are. Keys is correctly EXCLUDED from the layout-owned project breadcrumb (05-03 suppresses `{project} / {View}` on `/keys`, review MEDIUM #3); Keys may keep its OWN user-scoped breadcrumb markup restyled to shadcn tokens, but it does NOT gain the active-project indicator (it is not project-scoped, D-09).
  </action>
  <verify>
    <automated>cd web && node -e "const fs=require('fs');const p=fs.readFileSync('src/routes/keys/+page.svelte','utf8');if(/<style/.test(p))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(p))process.exit(1);if(fs.existsSync('src/routes/keys/+page.ts'))process.exit(1);if(/invalidateAll/.test(p))process.exit(1)" && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `keys/+page.svelte` uses shadcn primitives + tokens; no `<style>` block and no 6-digit hex.
    - No `keys/+page.ts` exists and the page does not call `invalidateAll()` (D-09 user-scope preserved).
    - Existing mint/rotate/revoke + CSRF behavior and runes unchanged; revoke uses the migrated AlertDialog contract.
    - `pnpm build` and `pnpm test` pass.
  </acceptance_criteria>
  <done>Keys page visually migrated to shadcn while remaining user-scoped and behavior-identical.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

Modified files:
- `web/src/routes/keys/+page.svelte` — shadcn restyle only (no load(), no project-scope change)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key material → DOM | Minted/revealed key plaintext shown on the page |
| revoke/mint/rotate → cookie-authed mutation | CSRF-protected key mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Spoofing/CSRF | mint/rotate/revoke mutations | medium | accept | Unchanged — existing `csrfInterceptor` double-submit still covers these; this is a visual-only restyle adding no new mutation path. |
| T-05-16 | Elevation of Privilege | accidental project-scoping of user-scoped keys | high | mitigate | Explicitly forbid `+page.ts`/`invalidateAll()` wiring here (D-09); verify gate asserts no `keys/+page.ts` and no `invalidateAll` — keeps keys user-scoped, preventing cross-scope leakage of key state. |
| T-05-XSS-12 | Tampering | key/label text rendered | high | mitigate | Svelte `{text}` bindings (auto-escaped); no `{@html}`. |
</threat_model>

<verification>
- `pnpm -C web build` and `pnpm -C web test` pass; no `<style>`/hex; no `keys/+page.ts`.
- `task web:build && task build && task check` pass.
- Manual UAT: keys list, mint, and revoke flows work identically in both themes; page is unaffected by project switch.
</verification>

<success_criteria>
- Keys page restyled to shadcn with tokens, behavior-identical, and provably still user-scoped (no load()/invalidateAll) per D-09; app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-12-SUMMARY.md` when done.
</output>
