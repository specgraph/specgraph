---
phase: 05-ui-project-selector-and-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/pnpm-workspace.yaml
  - web/vite.config.ts
  - web/src/app.css
  - web/components.json
  - web/src/lib/utils.ts
  - web/src/lib/components/ui/
  - web/src/lib/components/badge-variants.ts
autonomous: true
requirements: [D-12, D-13, D-14]
user_setup: []

must_haves:
  truths:
    - "The web app builds as a static bundle with Tailwind v4 active (pnpm build succeeds)."
    - "shadcn-svelte primitives exist under $lib/components/ui/ and import cleanly."
    - "The Slate OKLCH token block is present in app.css for both :root and .dark."
    - "A shared categorical badge-variant map exists for stage/layer/status/severity encodings."
  artifacts:
    - "web/src/app.css (Tailwind v4 entry + @custom-variant dark + Slate :root/.dark tokens)"
    - "web/components.json (shadcn metadata, baseColor slate, aliases per UI-SPEC)"
    - "web/src/lib/utils.ts (generated cn())"
    - "web/src/lib/components/ui/* (button card dialog alert-dialog dropdown-menu select tabs table accordion badge input separator skeleton tooltip sonner breadcrumb)"
    - "web/src/lib/components/badge-variants.ts (categorical class-pair maps)"
    - "web/vite.config.ts wired with @tailwindcss/vite plugin before sveltekit()"
  key_links:
    - "vite.config.ts tailwindcss() plugin → Tailwind v4 build → static bundle → web/embed.go all:build glob"
    - "components.json aliases → $lib/components/ui generation target"
    - "pnpm-workspace.yaml minimumReleaseAgeExclude → frozen-lockfile CI install"
---

<objective>
Install and wire the shadcn-svelte + Tailwind v4 design-system foundation for the `web/` SvelteKit app: dependencies, Vite plugin, generated `app.css` with the verified **Slate** OKLCH token block, `components.json`, the `cn()` utility, the shadcn primitive component set, and a shared categorical badge-variant map. This is the base every later wave builds on (D-12 install, D-13 aesthetic, D-14 theming tokens).

Purpose: Establish the shadcn/Tailwind-v4 foundation so component migrations (Wave 2) and page load-ification (Wave 3) have primitives and tokens to consume. No existing component/page is changed yet — the app must still build.
Output: Tailwind-v4-enabled static build with vendored shadcn primitives and Slate theming tokens.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind v4 + shadcn dependencies and wire the Vite plugin</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/pnpm-workspace.yaml, web/vite.config.ts</files>
  <read_first>
    - web/package.json (current deps — RESEARCH "Standard Stack" pins versions)
    - web/pnpm-workspace.yaml (existing minimumReleaseAgeExclude for svelte@5.56.2 — the pattern to extend)
    - web/vite.config.ts (current plugins + /specgraph.v1 proxy to preserve)
    - 05-RESEARCH.md "Standard Stack", "Installation", "Package Legitimacy Audit", Pitfall 1, Pitfall 7
    - 05-RESEARCH.md "Code Examples → vite.config.ts" (target plugin order)
  </read_first>
  <action>
    From `web/`, add the verified dependency set (RESEARCH Standard Stack): `tailwindcss`, `@tailwindcss/vite`, `bits-ui`, `mode-watcher`, `@lucide/svelte`, `tailwind-variants`, `tailwind-merge`, `clsx`, `tw-animate-css`. Use `@lucide/svelte` NOT the superseded `lucide-svelte` (RESEARCH Package Audit flags the legacy name as SUS). Prefer running `sv add tailwindcss` non-interactively; if the adder blocks on a prompt, fall back to the manual path: `pnpm add -D tailwindcss @tailwindcss/vite` plus the runtime deps, then wire the plugin by hand (RESEARCH Pitfall 1). Edit `web/vite.config.ts` to add the `@tailwindcss/vite` plugin as `tailwindcss()` placed BEFORE `sveltekit()` in the plugins array, preserving the existing `/specgraph.v1` proxy verbatim. Because several packages published within the pnpm `minimumReleaseAge` window (RESEARCH Pitfall 7), add targeted `minimumReleaseAgeExclude` entries in `web/pnpm-workspace.yaml` for any newly added package that trips `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION` under `pnpm install --frozen-lockfile` (same mechanism already used for `svelte@5.56.2`); pin to the RESEARCH-verified versions. Do NOT add `tailwind.config.js` or `postcss.config.js` — Tailwind v4 is CSS-first (RESEARCH State of the Art). Do NOT create `app.css` here (Task 2 generates it via init). Implements D-12 install.
  </action>
  <verify>
    <automated>cd web && pnpm install --frozen-lockfile && node -e "const c=require('fs').readFileSync('vite.config.ts','utf8'); if(!/tailwindcss\(\)/.test(c)||c.indexOf('tailwindcss()')>c.indexOf('sveltekit()'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install --frozen-lockfile` in `web/` succeeds (no minimumReleaseAge violation).
    - `web/package.json` lists all nine RESEARCH-pinned packages; `@lucide/svelte` present, `lucide-svelte` absent.
    - `web/vite.config.ts` includes `tailwindcss()` before `sveltekit()` and retains the `/specgraph.v1` proxy.
    - No `tailwind.config.js` or `postcss.config.js` file exists in `web/`.
  </acceptance_criteria>
  <done>Dependencies installed under the supply-chain policy; Vite serves Tailwind v4; proxy preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Run shadcn-svelte init, paste Slate tokens, add primitive set, create badge-variants map</name>
  <files>web/components.json, web/src/app.css, web/src/lib/utils.ts, web/src/lib/components/ui/, web/src/lib/components/badge-variants.ts</files>
  <read_first>
    - 05-RESEARCH.md "Installation" (init flags), Pitfall 2 (base-color enum lacks slate), "Code Examples → app.css" (verified Slate OKLCH block), "Code Examples → components.json" (target shape)
    - 05-UI-SPEC.md "Design System" (components.json fields, component list to add), "Categorical badge palette"
    - 05-PATTERNS.md "Shared Patterns → Categorical Badge Palette" (stage/layer/status/severity class pairs), "Build Config Integration", "No Analog Found"
  </read_first>
  <action>
    From `web/`, run `shadcn-svelte init` non-interactively with flags (RESEARCH Installation): `--base-color neutral` (the CLI enum has NO `slate` — Pitfall 2), `--css src/app.css`, and the alias flags (`--lib-alias '$lib'`, `--components-alias '$lib/components'`, `--utils-alias '$lib/utils'`, `--hooks-alias '$lib/hooks'`, `--ui-alias '$lib/components/ui'`). This generates `src/app.css`, `components.json`, and `src/lib/utils.ts` (`cn()`). Then OVERWRITE the `:root` and `.dark` token blocks in `src/app.css` with the verified Slate OKLCH values from RESEARCH "Code Examples → app.css" (keep the init-generated `@import "tailwindcss";`, `@import "tw-animate-css";`, `@custom-variant dark (&:is(.dark *));`, and the `@theme inline { … }` map). Set `components.json` `tailwind.baseColor` to `slate` (cosmetic metadata per Pitfall 2) — the real colors are the pasted Slate token block. Add the shadcn primitive set with `pnpm dlx shadcn-svelte@latest add -y -o button card dialog alert-dialog dropdown-menu select tabs table accordion badge input separator skeleton tooltip sonner breadcrumb` (UI-SPEC component list). Treat everything under `$lib/components/ui/` as vendored — do not hand-edit (RESEARCH Anti-Patterns). Finally, create `web/src/lib/components/badge-variants.ts` exporting categorical class-pair maps for the four constitution layers (User/Org/Project/Domain), spec stage badges, decision-status badges, and finding-severity badges, using the fixed light/dark Tailwind utility pairs from PATTERNS "Categorical Badge Palette" / UI-SPEC "Categorical badge palette" table (e.g. User → `bg-blue-100 text-blue-800 dark:bg-blue-950 dark:text-blue-300`). These are categorical data encodings — do NOT map them to `--primary`. Implements D-12 (primitives), D-13 (Slate aesthetic), D-14 (dark token block).
  </action>
  <verify>
    <automated>cd web && pnpm build && node -e "const fs=require('fs');['components.json','src/app.css','src/lib/utils.ts','src/lib/components/badge-variants.ts'].forEach(f=>{if(!fs.existsSync(f))process.exit(1)});const css=fs.readFileSync('src/app.css','utf8');if(!/oklch\(0\.129 0\.042 264\.695\)/.test(css)||!/@custom-variant dark/.test(css))process.exit(1);if(!fs.existsSync('src/lib/components/ui/button'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` produces a static bundle (`web/build/` non-empty) with Tailwind active.
    - `src/app.css` contains the Slate `:root`/`.dark` OKLCH tokens (marker value `oklch(0.129 0.042 264.695)` present) and `@custom-variant dark`.
    - `components.json` has `style: default`, `tailwind.css: src/app.css`, `tailwind.baseColor: slate`, and the UI-SPEC aliases.
    - `$lib/components/ui/` contains all 16 added primitives (button…breadcrumb).
    - `src/lib/components/badge-variants.ts` exports fixed light/dark class pairs for the four constitution layers, stage, status, and severity — none using `--primary`.
  </acceptance_criteria>
  <done>shadcn primitives vendored, Slate theming live, categorical badge map available for later waves.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

New files:
- `web/src/app.css` — Tailwind v4 entry, `@custom-variant dark`, Slate `:root`/`.dark` OKLCH tokens, `@theme inline` map
- `web/components.json` — shadcn-svelte metadata (baseColor slate, style default, aliases)
- `web/src/lib/utils.ts` — generated `cn()` (clsx + tailwind-merge)
- `web/src/lib/components/ui/**` — vendored shadcn primitives (button, card, dialog, alert-dialog, dropdown-menu, select, tabs, table, accordion, badge, input, separator, skeleton, tooltip, sonner, breadcrumb)
- `web/src/lib/components/badge-variants.ts` — categorical class-pair maps (layer/stage/status/severity)

Modified files:
- `web/package.json`, `web/pnpm-lock.yaml` — new dependency set
- `web/pnpm-workspace.yaml` — targeted `minimumReleaseAgeExclude` entries
- `web/vite.config.ts` — `@tailwindcss/vite` plugin wired before `sveltekit()`
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Newly added packages enter the build/supply chain |
| Vite build → Go embed | Static bundle embedded into the server binary via `web/embed.go` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | new npm deps (shadcn/tailwind/bits-ui/@lucide) | high | mitigate | Use only RESEARCH Package Legitimacy Audit-approved packages/versions; use `@lucide/svelte` not the SUS `lucide-svelte`; keep pnpm `minimumReleaseAge` policy, add only targeted excludes; `pnpm install --frozen-lockfile` in CI |
| T-05-01 | Tampering | components.json / app.css generation | low | accept | Generated files are static assets; no runtime privilege; reviewed in diff |
| T-05-02 | Denial of Service | build embeds larger static bundle | low | accept | `web/embed.go all:build` glob already captures new assets; size increase is benign |
</threat_model>

<verification>
- `pnpm -C web install --frozen-lockfile` passes under the supply-chain policy.
- `pnpm -C web build` produces a non-empty `web/build/`.
- `task web:build && task build` succeeds (static bundle embeds into the binary).
- `task check` passes (fmt/license/lint/build/unit; no Docker).
</verification>

<success_criteria>
- shadcn-svelte foundation installed; Tailwind v4 active; Slate tokens present; primitive set vendored; badge-variants map available; app still builds. No component or page behavior changed.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-01-SUMMARY.md` when done.
</output>
