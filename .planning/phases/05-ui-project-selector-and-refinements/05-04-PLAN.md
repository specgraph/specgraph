---
phase: 05-ui-project-selector-and-refinements
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/lib/components/AccordionSection.svelte
  - web/src/lib/components/TabBar.svelte
  - web/src/lib/components/FindingsSection.svelte
autonomous: true
requirements: [D-12, D-13, D-10]
user_setup: []

must_haves:
  truths:
    - "AccordionSection, TabBar, and FindingsSection render on shadcn primitives with Slate tokens and no scoped navy/blue CSS."
    - "Existing props, Svelte 5 runes, and Snippet contracts are unchanged so call-sites need no edits."
    - "FindingsSection severity uses the shared categorical badge map, not the theme accent (D-10 palette rule)."
  artifacts:
    - "web/src/lib/components/AccordionSection.svelte (on Accordion + Badge)"
    - "web/src/lib/components/TabBar.svelte (on Tabs)"
    - "web/src/lib/components/FindingsSection.svelte (on Accordion/Card + severity Badge)"
  key_links:
    - "badge-variants.ts severity map → FindingsSection Badge classes"
    - "unchanged props/Snippet contract → constitution/spec call-sites unaffected"
---

<objective>
Migrate three structural components — `AccordionSection`, `TabBar`, `FindingsSection` — to shadcn-svelte primitives + Tailwind Slate tokens, keeping every prop and Svelte 5 runes contract identical (D-12 migration, D-13 aesthetic). FindingsSection severity badges use the shared categorical map (D-10 palette carve-out).

Purpose: These are reused across constitution/spec pages; migrating them without contract changes lets Wave 3 pages restyle without call-site churn.
Output: Three token-based components on shadcn primitives.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AccordionSection to shadcn Accordion + Badge</name>
  <files>web/src/lib/components/AccordionSection.svelte</files>
  <read_first>
    - web/src/lib/components/AccordionSection.svelte (keep script/markup shape L1-24, delete style L26-75 — PATTERNS L283-292)
    - web/src/lib/components/ui/accordion, web/src/lib/components/ui/badge
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (AccordionSection → Accordion + Badge)
    - 05-PATTERNS.md "Component Migration Pattern" (the reference recipe)
  </read_first>
  <action>
    Re-implement `AccordionSection.svelte` on shadcn `Accordion` (`Accordion.Item/Trigger/Content`) with Tailwind utility classes over the Slate tokens. Keep the `Props` interface (`title`, `expanded`, `badge`, `children` Snippet) and the `$props`/`$state`/`$derived` runes exactly as-is so call-sites are untouched. Render the `badge` count via shadcn `Badge`. Delete the entire scoped `<style>` block; replace every hard-coded hex from the old palette with token utility classes (`text-foreground`, `text-muted-foreground`, `border-border`, `bg-muted`, etc.) per the PATTERNS migration invariant. Do not hand-edit the vendored `ui/` primitives.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/AccordionSection.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Aa]ccordion/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>` block and no 6-digit hex color remains in the file.
    - Uses shadcn `Accordion` primitives + `Badge`; `Props` and Snippet contract unchanged.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>AccordionSection renders on shadcn with tokens; call-sites need no change.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TabBar to shadcn Tabs</name>
  <files>web/src/lib/components/TabBar.svelte</files>
  <read_first>
    - web/src/lib/components/TabBar.svelte (keep `{ tabs, active, onchange }` props L1-20, drop `.tab.active` navy/blue L47-51 — PATTERNS L295)
    - web/src/lib/components/ui/tabs
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (TabBar → Tabs)
  </read_first>
  <action>
    Re-implement `TabBar.svelte` on shadcn `Tabs` (`Tabs.List/Trigger/Content` as applicable) preserving the `tabs`, `active`, and `onchange` props and their behavior. The active tab uses `--primary` accent per UI-SPEC accent rule. Delete the scoped `<style>` navy/blue palette; use token utility classes. Keep Svelte 5 runes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/TabBar.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Tt]abs/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>` block and no 6-digit hex; uses shadcn `Tabs`; `tabs`/`active`/`onchange` props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>TabBar on shadcn Tabs with token styling.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate FindingsSection to Accordion/Card + categorical severity Badge (D-10 palette)</name>
  <files>web/src/lib/components/FindingsSection.svelte</files>
  <read_first>
    - web/src/lib/components/FindingsSection.svelte (current severity styling)
    - web/src/lib/components/badge-variants.ts (severity class-pair map from 05-01)
    - web/src/lib/components/ui/accordion, card, badge
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (FindingsSection → Accordion/Card + severity Badge), "Categorical badge palette"
    - 05-PATTERNS.md "Shared Patterns → Categorical Badge Palette"
  </read_first>
  <action>
    Re-implement `FindingsSection.svelte` on shadcn `Accordion`/`Card` with severity `Badge`s. Severity badges are CATEGORICAL data encoding — pull their fixed light/dark class pairs from the shared `badge-variants.ts` severity map (do NOT map severity to `--primary`, per the palette carve-out). Preserve props/behavior and Svelte 5 runes; delete the scoped `<style>`; use tokens for structural styling.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/FindingsSection.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/badge-variants|[Bb]adge/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>` block, no 6-digit hex; severity uses the shared categorical `badge-variants` map (not `--primary`).
    - Uses shadcn `Accordion`/`Card` + `Badge`; props/behavior preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>FindingsSection on shadcn with correct categorical severity badges.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

Modified files:
- `web/src/lib/components/AccordionSection.svelte` — shadcn Accordion + Badge
- `web/src/lib/components/TabBar.svelte` — shadcn Tabs
- `web/src/lib/components/FindingsSection.svelte` — shadcn Accordion/Card + categorical severity Badge
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server data → rendered component | Finding/spec text rendered inside these components |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-XSS-04 | Tampering/Info-disclosure | finding/section text rendered in migrated components | high | mitigate | Render server text via Svelte `{text}` bindings (auto-escaped); do NOT introduce `{@html}` for untrusted content (RESEARCH Security Domain). |
| T-05-07 | Tampering | accidental hand-edit of vendored `ui/` primitives | low | accept | Wrap/compose primitives; keep `ui/` re-sync-clean (RESEARCH Anti-Patterns). |
</threat_model>

<verification>
- `pnpm -C web build` passes; no `<style>`/hex remains in the three files.
- `task web:build && task build && task check` pass.
- Manual UAT (visual): accordions/tabs/findings render correctly in light and dark.
</verification>

<success_criteria>
- Three structural components migrated to shadcn with tokens and preserved contracts; FindingsSection severity uses the categorical palette; app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-04-SUMMARY.md` when done.
</output>
