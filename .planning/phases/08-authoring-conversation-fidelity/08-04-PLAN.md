---
phase: 08-authoring-conversation-fidelity
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - e2e/api/mcp_only_authoring_test.go
  - e2e/api/mcp_only_conversation_test.go
autonomous: true
requirements: [CONV-01]
must_haves:
  truths:
    - "An MCP-only agent drives a spec spark->shape->specify->decompose->approve via the author tool and every required stage (shape/specify/decompose/approve) has a non-empty, retrievable conversation (criteria #1/#3/#4)"
    - "Omitting exchanges on a required stage (approve) via the author tool returns InvalidArgument (criterion #2/#3 — a missing conversation cannot silently pass)"
    - "Conversations are retrievable after the funnel completes via the conversation list action (criterion #4)"
  artifacts:
    - e2e/api/mcp_only_conversation_test.go
    - e2e/api/mcp_only_authoring_test.go
  key_links:
    - "author approve now supplies exchanges (existing e2e :242 must be updated or it breaks under the new server enforcement)"
    - "conversation list (MCP) -> ListConversations -> non-empty per stage assertion"
---

<objective>
Add the D-10 phase gate: an MCP-only Ginkgo e2e that drives the full authoring funnel via the `author` tool and proves conversation fidelity end-to-end — every required stage records a non-empty, retrievable conversation, and omitting exchanges on a required stage is rejected with `InvalidArgument`. This is the backstop that recording is protocol-enforced, not agent-discretionary (CONV-01, all four success criteria).

Purpose: A single MCP-only test that would have caught #906 — it exercises the only remaining recording path (inline-with-save) and asserts non-empty conversations at shape/specify/decompose/approve plus the negative rejection case.
Output: Updated existing MCP-only funnel test (approve now supplies exchanges) + a new conversation-fidelity e2e.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-authoring-conversation-fidelity/08-CONTEXT.md
@.planning/phases/08-authoring-conversation-fidelity/08-RESEARCH.md
@.planning/phases/08-authoring-conversation-fidelity/08-PATTERNS.md
@.planning/phases/08-authoring-conversation-fidelity/08-VALIDATION.md
</context>

<artifacts>
## Artifacts this plan produces

- **Modified** `e2e/api/mcp_only_authoring_test.go` — the existing full-funnel test calls `author action=approve` with NO exchanges at ~:242; under the new server enforcement (Plan 01) + MCP threading (Plan 02) this now fails. Update that call to supply a valid `exchanges` JSON array, and add per-stage assertions that each required stage recorded a non-empty conversation (via `conversation` action=list).
- **New** `e2e/api/mcp_only_conversation_test.go` — a dedicated MCP-only conversation-fidelity spec: full funnel via `author`, assert non-empty retrievable conversation at shape/specify/decompose/approve, and a negative case where approve without exchanges returns InvalidArgument.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Update existing MCP-only funnel test — approve supplies exchanges + per-stage conversation assertions</name>
  <files>e2e/api/mcp_only_authoring_test.go</files>
  <read_first>
    - e2e/api/mcp_only_authoring_test.go (mcpProjectClient harness :49; the funnel It block :182-242; the `author` closure :228-242; existing mcpOnlyShapeExchanges/mcpOnlySpecifyExchanges/mcpOnlyDecomposeExchanges vars; approve call :242 which currently passes NO exchanges)
    - e2e/api/conversation_test.go (existing conversation e2e patterns for asserting recorded conversations)
    - AGENTS.md (e2e uses Ginkgo/Gomega under `//go:build e2e`; `go test -tags e2e ./e2e/api/...`; Docker required)
  </read_first>
  <action>
    The existing test calls `author(map[string]any{"action": "approve"})` at ~:242 with no exchanges — under the new enforcement this returns InvalidArgument and the test breaks. Add an approve exchanges fixture (a bare JSON array string, same shape as `mcpOnlyShapeExchanges`, with `stage: "approve"` and strictly-increasing sequences) and change the approve call to `author(map[string]any{"action": "approve", "exchanges": <approveExchanges>})`. After the funnel completes, add assertions: for each required stage (shape, specify, decompose, approve) call the `conversation` tool with `action: "list"` (filter by `stage`) and assert the returned conversation list is non-empty (criteria #1/#3/#4). Reuse the existing `mcpCli` MCP client and the CallTool result-parsing idiom already in the file. Keep the constitution-authoring preamble untouched.
  </action>
  <verify>
    <automated>echo "PRECONDITION: Docker must be running" && go test -tags e2e ./e2e/api/... -run "MCP-only authoring" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Source: the approve call in the funnel It block passes a non-empty `exchanges` argument; per-stage `conversation list` assertions confirm non-empty conversations for shape/specify/decompose/approve.
    - Behavior: the full MCP-only funnel test passes end-to-end under the new enforcement.
    - Test-command: `go test -tags e2e ./e2e/api/... -run "MCP-only authoring"` green (Docker running).
  </acceptance_criteria>
  <done>Existing funnel e2e updated to supply approve exchanges and assert non-empty per-stage conversations; passes with Docker.</done>
</task>

<task type="auto">
  <name>Task 2: New MCP-only conversation-fidelity e2e (positive + negative)</name>
  <files>e2e/api/mcp_only_conversation_test.go</files>
  <read_first>
    - e2e/api/mcp_only_authoring_test.go (mcpProjectClient harness + the per-Describe project-slug convention noted at :24-33; author/conversation CallTool idiom)
    - e2e/api/conversation_test.go (conversation retrieval assertions)
    - .planning/phases/08-authoring-conversation-fidelity/08-VALIDATION.md (Wave 0: the "missing conversation cannot silently pass" backstop)
  </read_first>
  <action>
    Add a new `//go:build e2e` Ginkgo file `e2e/api/mcp_only_conversation_test.go` with its own dedicated project slug (follow the per-Describe project convention at :24-33 to avoid cross-test bleed) and its own `mcpProjectClient` setup/cleanup. Positive spec: drive a fresh spec through spark->shape->specify->decompose->approve via the `author` tool supplying exchanges at every required stage, then assert via `conversation` action=list that each of shape/specify/decompose/approve has a non-empty conversation whose exchange content round-trips (retrievable after the funnel completes — criterion #4). Negative spec: attempt `author action=approve` with the `exchanges` argument omitted (or empty) on a spec at the decompose gate and assert the tool result surfaces an InvalidArgument / error (criteria #2/#3 — a missing conversation cannot silently pass). Reuse the friendly-YAML fixtures pattern from `mcp_only_authoring_test.go`; do not duplicate the constitution preamble beyond what the harness needs.
  </action>
  <verify>
    <automated>echo "PRECONDITION: Docker must be running" && go test -tags e2e ./e2e/api/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source: `mcp_only_conversation_test.go` has a positive full-funnel fidelity spec and a negative approve-without-exchanges spec, each with an isolated project slug.
    - Behavior: positive spec asserts non-empty retrievable conversations at all four required stages; negative spec asserts InvalidArgument when exchanges are omitted.
    - Test-command: `go test -tags e2e ./e2e/api/...` green (Docker running).
  </acceptance_criteria>
  <done>New MCP-only conversation-fidelity e2e (positive + negative) passes; full e2e tier green with Docker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e MCP client -> in-process MCP server -> handlers -> testcontainers Postgres | Test-only; exercises the same scopeStore/validation path as production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-11 | Tampering (test integrity) | Cross-test project-state bleed | low | mitigate | Use a dedicated per-Describe project slug + isolated `mcpProjectClient` (existing suite convention :24-33) so the fidelity assertions are deterministic. |
| T-08-12 | Repudiation | Missing conversation passing silently | high | mitigate | The negative spec asserts `InvalidArgument` when exchanges are omitted — this test IS the backstop that a missing conversation cannot silently pass (criterion #3). |

No package installs in this plan (no npm/pip/cargo) — supply-chain threat T-08-SC not applicable. T-08-12 is the high-severity concern and is precisely what this plan's negative e2e verifies is mitigated.
</threat_model>

<verification>
- `go test -tags e2e ./e2e/api/...` green with Docker running (both the updated funnel test and the new fidelity test).
- Every required stage (shape/specify/decompose/approve) has a non-empty, retrievable conversation.
- Approve without exchanges returns InvalidArgument.
</verification>

<success_criteria>
- MCP-only full-funnel e2e records + retrieves a non-empty conversation at every required stage (criteria #1/#3/#4).
- Negative e2e proves omitting exchanges on a required stage is rejected (criterion #2/#3).
- Existing MCP-only funnel test updated so it passes under the new enforcement.
</success_criteria>

<output>
Create `.planning/phases/08-authoring-conversation-fidelity/08-04-SUMMARY.md` when done.
</output>
