---
phase: 08-authoring-conversation-fidelity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/mcp/tools_authoring.go
  - internal/mcp/tools_authoring_test.go
  - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md
autonomous: true
requirements: [CONV-01]
must_haves:
  truths:
    - "MCP author approve threads conversation exchanges into ApproveRequest and rejects empty exchanges with a friendly message (D-02)"
    - "The MCP conversation tool exposes only the list action; a record action is no longer offered so inline-with-save is the only agent recording path (D-06)"
    - "The author tool Description and specgraph-authoring SKILL.md teach that approve now requires exchanges (D-09)"
  artifacts:
    - internal/mcp/tools_authoring.go
    - internal/mcp/tools_authoring_test.go
    - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md
  key_links:
    - "handleApprove -> parseOptionalExchanges -> ApproveRequest.ConversationExchanges (mirrors handleShape)"
    - "conversationTool.handle -> only case \"list\" remains; handleRecord removed"
---

<objective>
Collapse the MCP recording surface to a single inline-with-save path (CONV-01 success criteria #1/#2; the #906 root cause is the skippable standalone record action). Three changes to the MCP surface: (1) `handleApprove` threads required `exchanges` into `ApproveRequest` (D-02, mirroring `handleShape`); (2) remove the `conversationTool` `record` action so `list` is the only action (D-06, Phase 7 single-source-of-truth precedent), keeping retrieval intact (criterion #4); (3) flip the `author` tool Description and `specgraph-authoring` SKILL.md so approve is taught as requiring exchanges (D-09, Pitfall #5).

Purpose: After this plan, an MCP-only agent has no "forgot to record" path — inline-with-save is the only way, and approve joins the enforced stages.
Output: Updated MCP tool handlers + descriptions + skill; pruned record-action unit tests.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-authoring-conversation-fidelity/08-CONTEXT.md
@.planning/phases/08-authoring-conversation-fidelity/08-RESEARCH.md
@.planning/phases/08-authoring-conversation-fidelity/08-PATTERNS.md
</context>

<artifacts>
## Artifacts this plan produces

- **Modified** `internal/mcp/tools_authoring.go`:
  - `handleApprove` (:332) — now parses exchanges via `parseOptionalExchanges`, errors client-side if empty ("exchanges is required for approve"), and sets `ConversationExchanges` on `ApproveRequest`.
  - `authorTool.def()` Description (:118-129, :146-155) — `exchanges` prop (:146-148) flipped from "not needed for approve" to "required for shape/specify/decompose/approve, optional for spark"; AND the top-level Description sentence at :123-124 (currently lists only shape/specify/decompose) widened to include approve (review finding #4 — both the prop and the top-level sentence).
  - `conversationTool.def()` (:424-442) + `conversationTool.handle` (:444-454) — `record` action removed; Description "Record and list…" -> "List…"; action enum drops "record"; `exchanges`/`is_amend` props dropped; `stage` prop kept (valid `list` filter).
  - **Deleted** `handleRecord` (:456-485).
- **Modified** `internal/mcp/tools_authoring_test.go` — `TestConversationTool_Record*` (record/InvalidJSON/MissingSlug/MissingStage/MissingExchanges, ~:676-773) removed; add/adjust a test that `conversation` action=record returns an unknown-action error and that `author` approve threads exchanges.
- **Modified** `internal/mcp/skills/embedded/specgraph-authoring/SKILL.md` — approve teaching flipped to require `exchanges` at :66-67, :167-169, :176-177 (review finding #7: SKILL.md already persists via `author` inline at :55-57 and does NOT reference a standalone `conversation record` tool, so this is the approve-exchanges flip only, not removal of already-gone record-step prose).

**Hard merge gate (review finding #6):** this plan's MCP `handleApprove` must land in the SAME wave-1 merge as 08-01 (server enforcement) and 08-03 (CLI). If 08-01 merges without this plan, MCP approve still sends no exchanges and breaks. Wave 1 (08-01 + 08-02 + 08-03) merges together; run `task check` after all three.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread required exchanges into MCP handleApprove + flip author Description</name>
  <files>internal/mcp/tools_authoring.go, internal/mcp/tools_authoring_test.go</files>
  <behavior>
    - author action=approve with no exchanges returns a client-side error result naming that exchanges is required for approve.
    - author action=approve with a valid exchanges JSON array sends ApproveRequest with ConversationExchanges populated.
    - author action=approve still errors when slug is missing.
  </behavior>
  <read_first>
    - internal/mcp/tools_authoring.go (handleShape :230-262 as the threading template; handleApprove :332-344 current; parseOptionalExchanges :71; authorTool.def Description :115-177, exchanges prop :146-155)
    - .planning/phases/08-authoring-conversation-fidelity/08-PATTERNS.md (handleApprove section + author Description flip)
  </read_first>
  <action>
    In `handleApprove` (D-02): after the existing slug guard, call `parseOptionalExchanges(params)` (reuse verbatim — it parses the `exchanges` string param in isolation to prevent JSON injection, a V5 control; do not hand-roll). If it errors, return that error result. Because approve REQUIRES exchanges (unlike optional spark), if the parsed slice is empty return `errResult("exchanges is required for approve (JSON array of ConversationExchange)")` — friendlier than waiting for the server. Set `ConversationExchanges: exchanges` on the `ApproveRequest` (Action stays default UNSPECIFIED -> server treats as accept). In `authorTool.def()` Description (D-09): change the `exchanges` prop text (:146-148) so it reads that exchanges are required for shape/specify/decompose/approve and optional for spark (remove the "not needed for approve" clause at :148). ALSO update the top-level `author` Description sentence at :123-124 (review finding #4) — it currently reads "shape/specify/decompose also require `exchanges`" and MUST be widened to include approve (e.g. "shape/specify/decompose/approve also require `exchanges`"); flipping only the prop and leaving the top-level sentence stale would keep teaching approve as exchange-free. Add a unit test in `tools_authoring_test.go` covering the empty-exchanges error and the populated-request happy path (use the existing MCP tool test harness pattern).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/mcp/... 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `handleApprove` calls `parseOptionalExchanges` and sets `ConversationExchanges` on `ApproveRequest`; empty -> `errResult` naming approve requires exchanges.
    - Source: `exchanges` prop description (:146-148) lists approve among the required stages; no "not needed for approve" clause remains.
    - Source (review finding #4): the top-level `author` Description sentence (:123-124) names approve among the actions that require `exchanges` — not only the prop text.
    - Test-command: `go test ./internal/mcp/...` passes including the new approve-threading cases.
  </acceptance_criteria>
  <done>MCP approve threads required exchanges; author Description teaches approve-requires-exchanges; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the conversationTool record action (keep list); prune record tests</name>
  <files>internal/mcp/tools_authoring.go, internal/mcp/tools_authoring_test.go</files>
  <read_first>
    - internal/mcp/tools_authoring.go (conversationTool :420-500 — def :424-442, handle :444-454, handleRecord :456-485, handleList :487-500 KEEP)
    - internal/mcp/tools_core.go (findingsTool.handle single-action list-only switch shape, ~:170-178 — the canonical pattern)
    - internal/mcp/tools_authoring_test.go (TestConversationTool_Record* ~:676-773 — must be removed so the package compiles)
    - .planning/phases/08-authoring-conversation-fidelity/08-PATTERNS.md (conversationTool remove-record section)
  </read_first>
  <action>
    Remove the agent-facing standalone record path (D-06). In `conversationTool.handle`, drop the `case "record":` branch so the switch has only `case "list": return t.handleList(...)` and a default returning `errResult(fmt.Sprintf("unknown action %q — valid: list", action))` (model on `findingsTool.handle`). Delete the `handleRecord` method entirely. In `conversationTool.def()`: change Description from "Record and list authoring conversation logs…" to "List authoring conversation logs…"; drop "record" from the `action` stringProp enum (leave "list"); drop the now-unused `exchanges` and `is_amend` props; KEEP the `stage` prop (it is a valid `list` filter used by `handleList`). Do NOT touch `handleList`. In `tools_authoring_test.go` remove `TestConversationTool_Record`, `TestConversationTool_Record_InvalidJSON`, `_MissingSlug`, `_MissingStage`, `_MissingExchanges` (they assert on the removed branch and will not compile). Add one small test asserting `conversation` with action=record now returns the unknown-action error and that action=list still works. Keep `Store.RecordConversation` and the CLI `conversation record` command out of scope (D-07/D-08 — not this plan).
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/mcp/... 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `conversationTool.handle` has no record branch; `handleRecord` no longer exists; `handleList` unchanged.
    - Behavior: `conversation` action=record returns "unknown action" error; action=list returns conversations.
    - Test-command: `go test ./internal/mcp/...` compiles (record tests removed) and passes.
  </acceptance_criteria>
  <done>Record action removed, list retained; record-action unit tests pruned; package compiles and MCP tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Flip approve teaching in specgraph-authoring SKILL.md</name>
  <files>internal/mcp/skills/embedded/specgraph-authoring/SKILL.md</files>
  <read_first>
    - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md (already teaches persist-via-`author`-inline at :55-57; exchanges requirement lines :66-67 and :167-169; approve block :176-177 — these are the lines that actually need editing)
    - AGENTS.md (SKILL.md edits gated by `task skills:validate` and TestContentProtoDrift on backticked snake_case tokens)
    - .planning/phases/08-authoring-conversation-fidelity/08-RESEARCH.md (Pitfall #5)
  </read_first>
  <action>
    Flip the approve teaching (D-09). Update the `exchanges` requirement lines (:66-67 and :167-169) so they read that exchanges are required for shape / specify / decompose / **approve** (optional for spark). Rewrite the Approve block (:176-177) which currently says approve "needs only the `slug`… does not require an `output` or `exchanges` on a clean acceptance" to state that approve now REQUIRES `exchanges` on clean acceptance (still no `output`), recorded inline with the approve call.

    Review finding #7 — scope note: SKILL.md ALREADY persists via `author` inline (:55-57) and does NOT reference the standalone `conversation record` tool, so the required edits are ONLY the approve-exchanges flip at the lines above plus author-Description parity. Do NOT go hunting for "then call `conversation record`" / standalone-record prose to delete — a grep for it is expected to return nothing (that prose is already gone). If the grep finds nothing, that is the correct state; do not invent removals or add new record-step framing.

    Keep all backticked snake_case tokens mapping to real proto fields (do not introduce a token that fails TestContentProtoDrift). Run `task skills:validate` after editing.
  </action>
  <verify>
    <automated>task skills:validate && go test ./internal/authoring/... -run TestContentProtoDrift 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: SKILL.md states approve requires `exchanges` on clean acceptance and lists approve among the required-exchanges stages (:66-67, :167-169, :176-177).
    - Behavior (review finding #7): no standalone-record framing is introduced; the edit targets the approve-exchanges flip, not removal of prose that no longer exists.
    - Test-command: `task skills:validate` passes; `TestContentProtoDrift` passes.
  </acceptance_criteria>
  <done>Skill teaches approve-requires-exchanges inline-with-save; validation + content-drift tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent -> author/conversation tool params | Untrusted `exchanges` string param crosses into JSON parsing before ConnectRPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | JSON injection via the `exchanges` string param in handleApprove | medium | mitigate | Reuse `parseOptionalExchanges` verbatim — it wraps the raw value as a single `{"exchanges":…}` field and never splices untrusted JSON into a larger document (V5 control already in place). |
| T-08-06 | Repudiation | Removing the standalone record action | low | accept | Retrieval (`list`) and the storage method are retained (D-06/D-07); inline recording is now the only agent path, which increases (not decreases) auditability. Accepted as an improvement. |
| T-08-07 | Information Disclosure | MCP error results | low | mitigate | Errors returned via `errResult`/`connectErrResult` (existing sanitization); no raw parser internals surfaced. |

No package installs in this plan (no npm/pip/cargo) — supply-chain threat T-08-SC not applicable. No high-severity threats in this plan.
</threat_model>

<verification>
- `go build ./...` + `go test ./internal/mcp/...` green.
- `task skills:validate` + `TestContentProtoDrift` green.
- MCP approve threads exchanges; conversation tool offers only `list`; skill teaches approve-requires-exchanges.
</verification>

<success_criteria>
- Inline-with-save is the only MCP recording path (record action removed, list kept).
- Approve threads required exchanges; author Description + skill teach it (D-02/D-06/D-09).
- Record-action unit tests pruned; package compiles; all MCP tests green.
</success_criteria>

<output>
Create `.planning/phases/08-authoring-conversation-fidelity/08-02-SUMMARY.md` when done.
</output>
