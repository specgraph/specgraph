---
phase: 03-external-identity-provider-integration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - internal/auth/loginprovider.go
  - internal/auth/oauth2_provider.go
  - internal/auth/oauth2_provider_test.go
  - internal/auth/loginprovider_test.go
autonomous: true
requirements: [AUTH-01]
user_setup:
  - service: github
    why: "Manual/e2e verification of the native GitHub OAuth2 login flow (not needed for unit tests, which stub the endpoints)"
    env_vars:
      - name: SPECGRAPH_GITHUB_CLIENT_SECRET
        source: "GitHub → Settings → Developer settings → OAuth Apps → your app → Client secret (referenced via the provider's client_secret_env)"
    dashboard_config:
      - task: "Register a GitHub OAuth App with callback URL <base>/api/auth/oidc/callback and scopes read:user, user:email"
        location: "GitHub → Settings → Developer settings → OAuth Apps → New OAuth App"
must_haves:
  truths:
    - "BuildLoginProviders constructs an oauth2 LoginProvider for a kind==\"oauth2\" provider and no longer hard-rejects non-oidc kinds (D-01)"
    - "The oauth2 provider exchanges the code for an access token then GETs userinfo, producing *OIDCClaims with Subject = the configured stable id selector (e.g. GitHub numeric id) and a synthetic Issuer (D-02/D-09)"
    - "When the primary userinfo email is absent, the provider fetches the primary&&verified email via the emails endpoint; an unverified/absent email is left blank (D-02)"
    - "userinfo fields feed the same claims_mapping → role mechanism with the JIT default-role fallback (D-04)"
    - "Missing userinfo URL or subject selector for an oauth2 provider is startup-fatal (matches BuildLoginProviders discipline)"
  artifacts:
    - "internal/auth/oauth2_provider.go — oauth2LoginProvider implementation"
    - "internal/auth/oauth2_provider_test.go — httptest-stubbed userinfo/email/subject coverage"
  key_links:
    - "BuildLoginProviders (kind==oauth2) → oauth2LoginProvider → Exchange → userinfo GET → *OIDCClaims{Raw=userinfo} → materializeIdentity (Plan 01) → oidc_bindings (issuer,subject)"
---

<objective>
Deliver AUTH-01: a native, generic OAuth2 + userinfo login provider (GitHub as the driving
config), plugging into the Plan 01 seam so it produces `*OIDCClaims` that flow through the exact
same binding/JIT/claims-mapping machinery OIDC uses.

Scope:
1. Relax `BuildLoginProviders` so it constructs an `oauth2LoginProvider` for `kind=="oauth2"`
   instead of hard-rejecting it (D-01). GitHub ships as documented config, not a hardcoded preset.
2. New `oauth2LoginProvider` (Pattern 1): reuses `oauth2.Config` + PKCE/state for the authorize
   URL and code exchange, but replaces id_token verification with a userinfo GET and (when the
   primary email is private) a primary&&verified email fetch (D-02). Subject = the configured
   stable id selector; Issuer = a synthetic provider id (D-09). Roles via claims_mapping over the
   userinfo body (D-04).

Purpose: A second, non-OIDC IdP path so users log in via GitHub-direct with no broker, using the
same session model — and the data layer stores bindings the same `(issuer, subject) → user_id`
way, keeping N-providers-per-user possible (D-03) without building the linking flow (deferred).
Output: The oauth2 provider + unit coverage, green under `task test`.
</objective>

<artifacts_produced>
NEW symbols created by this plan (phase artifact registry):
- `oauth2LoginProvider` struct + its `ID`/`DisplayName`/`AuthCodeURL`/`Exchange` methods (oauth2_provider.go)
- userinfo/email selection helpers (e.g. `fetchUserinfo`, `fetchPrimaryVerifiedEmail`, `selectField`) (oauth2_provider.go)
- The `kind=="oauth2"` construction branch in `BuildLoginProviders` (loginprovider.go)
Consumes (created in Plan 01, not drift): `LoginProvider.Exchange (*OIDCClaims)`, `OIDCProviderConfig`
oauth2 fields (`AuthURL`/`TokenURL`/`UserinfoURL`/`EmailsURL`/`SubjectField`/`EmailField`).
</artifacts_produced>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-external-identity-provider-integration/03-CONTEXT.md
@.planning/phases/03-external-identity-provider-integration/03-RESEARCH.md
@.planning/phases/03-external-identity-provider-integration/03-VALIDATION.md
@.planning/phases/03-external-identity-provider-integration/03-01-PLAN.md
@docs/plans/2026-06-15-oidc-app-roles-login-sync-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement oauth2LoginProvider (Exchange → userinfo → *OIDCClaims) with verified-email fallback</name>
  <files>internal/auth/oauth2_provider.go, internal/auth/oauth2_provider_test.go</files>
  <read_first>
    - internal/auth/loginprovider.go (:32-79 oidcLoginProvider + oauth2Config helper to mirror, :46-53 AuthCodeURL PKCE usage)
    - internal/auth/oidc_verifier.go (:20-30 OIDCClaims — the return shape; Raw carries userinfo for claims_mapping)
    - internal/server/auth_oidc_handler.go (:146-147 PKCE generation, :161-164 AuthCodeURL/redirect usage — the machinery reused verbatim)
    - internal/config/global.go (OIDCProviderConfig oauth2 fields added in Plan 01: AuthURL/TokenURL/UserinfoURL/EmailsURL/SubjectField/EmailField)
    - cmd/specgraph/serve.go (:227 newHTTPClient pattern for a bounded outbound client)
  </read_first>
  <behavior>
    - AuthCodeURL mirrors oidcLoginProvider's but WITHOUT oidc.Nonce (no id_token to bind); state + PKCE S256 remain the CSRF/interception defenses.
    - Exchange: oauth2.Config.Exchange(code, code_verifier) → access token; GET userinfoURL with the token; build *OIDCClaims{Issuer: synthetic issuerID, Subject: stringified value at SubjectField, Email, Raw: full userinfo map}.
    - Subject is read from SubjectField (e.g. "id"); a numeric JSON id is stringified. Binding on the stable id, never a renameable username.
    - Email: read EmailField from userinfo; if blank and EmailsURL is set, GET it and pick the entry with primary==true && verified==true; otherwise leave Email blank. Never trust an unverified email.
    - All outbound calls use a bounded-timeout http.Client; non-2xx or malformed bodies return an error (fail-closed → ErrUnauthenticated upstream).
  </behavior>
  <action>
    Create internal/auth/oauth2_provider.go (SPDX Apache-2.0 header + copyright; the package already
    has a doc comment so no new package comment needed). Define `oauth2LoginProvider` with fields for
    id, displayName, authURL, tokenURL, userinfoURL, emailsURL, clientID, secret, scopes,
    subjectField, emailField, issuerID, and an *http.Client. Implement ID/DisplayName. Implement
    AuthCodeURL(state, nonce, codeChallenge, redirectURI) reusing an oauth2.Config like
    oidcLoginProvider.oauth2Config but adding only code_challenge/code_challenge_method=S256 and NOT
    oidc.Nonce (the nonce param is accepted for interface parity and ignored). Implement
    Exchange(ctx, code, codeVerifier, nonce, redirectURI) (*OIDCClaims, error): exchange the code
    (oauth2.SetAuthURLParam("code_verifier", codeVerifier)); GET userinfoURL with the bearer access
    token via the bounded client; decode into map[string]json.RawMessage; select Subject from
    subjectField (stringify a JSON number/string; error if absent — subject is mandatory); select
    Email from emailField; if Email is empty and emailsURL != "", GET it and choose the
    primary&&verified entry. Return &OIDCClaims{Issuer: issuerID, Subject: subject, Email: email,
    Name: <optional display selector>, Raw: userinfo}. Add small helpers for the userinfo GET, the
    emails fetch, and field selection. Do NOT place fenced examples of GitHub JSON in written code
    comments beyond a short prose reference. Preserve fail-closed error returns (wrap with context).
  </action>
  <verify>
    <automated>go test ./internal/auth/ -run OAuth2Provider 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - New file internal/auth/oauth2_provider.go exists with an SPDX header (`grep -n 'SPDX-License-Identifier: Apache-2.0' internal/auth/oauth2_provider.go`).
    - internal/auth/oauth2_provider_test.go (created here) stubs `/user` and `/user/emails` via httptest and asserts: Subject = numeric id stringified; Email falls back to the primary&&verified emails entry when the primary userinfo email is null; an unverified/absent email yields blank Email; Raw contains the userinfo fields.
    - `go test ./internal/auth/ -run OAuth2Provider` passes.
    - No `oidc.Nonce` in AuthCodeURL for the oauth2 provider (`grep -n 'oidc.Nonce' internal/auth/oauth2_provider.go` yields nothing).
  </acceptance_criteria>
  <done>The oauth2 provider exchanges a code and materializes verified *OIDCClaims (stable subject, verified-email fallback, userinfo Raw) with unit coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire oauth2 into BuildLoginProviders (kind gate relax + startup-fatal validation)</name>
  <files>internal/auth/loginprovider.go, internal/auth/loginprovider_test.go</files>
  <read_first>
    - internal/auth/loginprovider.go (:100-158 BuildLoginProviders, :117-119 the kind reject to relax, :136-155 scopes/display construction to mirror)
    - internal/auth/loginprovider_test.go (:14-40 existing BuildLoginProviders tests to extend)
    - internal/config/global.go (OIDCProviderConfig oauth2 fields + resolveClientSecret at loginprovider.go:83-95)
  </read_first>
  <behavior>
    - A provider with Kind=="oauth2" and Interactive==true is constructed as an oauth2LoginProvider (no OIDC discovery, no verifier).
    - Missing UserinfoURL, AuthURL, TokenURL, or SubjectField for an oauth2 provider returns a fatal error (boot abort), matching the existing missing-secret/bad-audience discipline.
    - kind=="oidc" (and empty kind) behavior is unchanged; the oidc path still discovers + verifies.
    - claims_mapping over userinfo works because Raw feeds the existing applyClaimsMapping/JIT-default fallback (no mapping code change needed — verified by an oauth2 claims-mapping test case).
  </behavior>
  <action>
    In internal/auth/loginprovider.go modify BuildLoginProviders: replace the `kind != "oidc"` hard
    reject with a switch on kind. For "oidc" keep the existing discovery + verifier construction. For
    "oauth2": resolve the client secret (resolveClientSecret), validate that UserinfoURL, AuthURL,
    TokenURL, and SubjectField are non-empty (return a fatal `OAuth2 provider %s: ...` error
    otherwise), default EmailField if you choose a sensible default, apply the scopes/displayName
    defaulting that the oidc branch does, derive the synthetic issuerID from the provider config
    (provider ID or its configured issuer/base URL per D-09), and append an oauth2LoginProvider
    (constructed with a bounded http.Client). Any other kind stays a fatal unsupported-kind error.
    Extend internal/auth/loginprovider_test.go with cases: (a) an oauth2 provider is constructed
    successfully; (b) an oauth2 provider missing UserinfoURL is startup-fatal; (c) an oauth2 provider's
    userinfo Raw drives a claims_mapping role match (or reuse the existing ClaimsMapping test with an
    oauth2-shaped claims map to prove D-04 reuse). Name tests so they match `-run BuildLoginProviders`
    and `-run ClaimsMapping`.
  </action>
  <verify>
    <automated>go test ./internal/auth/ -run 'BuildLoginProviders|ClaimsMapping|OAuth2Provider' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'oauth2' internal/auth/loginprovider.go` shows the new construction branch; the bare `kind != "oidc"` reject is gone.
    - `go test ./internal/auth/ -run BuildLoginProviders` passes including the missing-userinfo-URL startup-fatal case.
    - `go test ./internal/auth/ -run ClaimsMapping` passes with an oauth2/userinfo-shaped claims map (D-04 reuse, no mapping code change).
    - `task test` green.
  </acceptance_criteria>
  <done>BuildLoginProviders constructs oauth2 providers with startup-fatal config validation; userinfo roles reuse claims_mapping; oidc path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SpecGraph → GitHub token/userinfo/emails endpoints | Server-side outbound calls carrying the client secret + access token |
| userinfo response → identity binding | Attacker-influenced profile fields cross into subject/email/role derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Spoofing | subject selection | high | mitigate | Bind on the stable id selector (SubjectField, e.g. numeric id), never a renameable username (D-02); asserted by the oauth2 provider test. |
| T-03-02-02 | Spoofing | email trust | high | mitigate | Only a primary&&verified email is accepted; unverified/absent → blank Email (D-02); asserted by the verified-email fallback test. |
| T-03-02-03 | Tampering | authorization-code interception | high | mitigate | PKCE S256 + state reused from the existing login machinery; no nonce needed (no id_token). |
| T-03-02-04 | Information Disclosure | SSRF via userinfo/emails URLs | medium | mitigate | Endpoint URLs are operator-supplied config, validated non-empty at startup, never derived from token/user input; bounded-timeout client. |
| T-03-02-05 | Elevation of Privilege | cross-provider auto-link | high | mitigate | Do NOT auto-link across providers this phase; bindings stay per (issuer, subject) (D-03); linking flow deferred. |
| T-03-02-SC | Tampering | package installs | high | accept | No new packages (GitHub endpoints are config URLs, not registry artifacts — RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `go test ./internal/auth/ -run 'OAuth2Provider|BuildLoginProviders|ClaimsMapping'` green.
- `task test` green; `task check` green before wave merge.
- Manual UAT (requires GitHub OAuth App, user_setup): browser flow through
  `/api/auth/oidc/{github}/start` → consent → callback yields an spgr_ws_ cookie and an
  oidc_bindings row with issuer=synthetic, subject=numeric id.
</verification>

<success_criteria>
- BuildLoginProviders constructs oauth2 providers; missing userinfo URL is startup-fatal (D-01).
- Exchange yields *OIDCClaims with stable subject + verified-email fallback (D-02) and userinfo Raw.
- userinfo roles reuse claims_mapping + default fallback (D-04); bindings stay (issuer,subject) (D-03).
</success_criteria>

<output>
Create `.planning/phases/03-external-identity-provider-integration/03-02-SUMMARY.md` when done.
</output>
