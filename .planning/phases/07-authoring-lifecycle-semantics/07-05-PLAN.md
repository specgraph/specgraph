---
phase: 07-authoring-lifecycle-semantics
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md
  - internal/mcp/skills/embedded/specgraph-troubleshooting/SKILL.md
  - e2e/api/mcp_only_lifecycle_test.go
autonomous: true
requirements: [LIFE-01, LIFE-02]
must_haves:
  truths:
    - "An MCP-only agent can learn from the skills how to send an in-flight spec back and re-author it, including the land-one-before re-entry model (D-09)."
    - "An MCP-only Ginkgo e2e, using distinct per-scenario specs, amends an in-flight spec back and re-authors the landed stage, drives a separate spec to done and supersedes it, and asserts both rejection cases — all through the author/claim/report MCP tools, asserting correct stage landings and rejections (D-10)."
    - "The e2e asserts amend-on-done is rejected and supersede-on-non-done is rejected."
  artifacts:
    - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md (amend/supersede/re-entry teaching)
    - e2e/api/mcp_only_lifecycle_test.go (MCP-only done->amend->re-author->supersede)
  key_links:
    - "skills teach re_entry_stage land-one-before -> agent calls author action=<stage> after amend"
    - "MCP author + claim + report tools -> done->amend->re-author->supersede e2e"
---

<objective>
Complete the Phase 6 self-teaching story (D-09) by teaching amend/supersede/re-entry — including the land-one-before model — in the MCP-first skills, and add the MCP-only e2e that is the phase's primary verification gate (D-10 e2e half): a fully MCP-driven suite exercising, across DISTINCT specs, an in-flight amend + re-author, a drive-to-done + supersede, and the two rejection cases (amend-on-done, supersede-on-non-done).

Purpose: Make the MCP surface impossible for an MCP-only agent to get wrong, and prove the consolidated lifecycle semantics end-to-end through the `author` tool.
Output: Amend/supersede/re-entry teaching in `specgraph-authoring` (and `specgraph-troubleshooting`) SKILL.md; new `e2e/api/mcp_only_lifecycle_test.go` with per-scenario slugs.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md
@.planning/phases/07-authoring-lifecycle-semantics/07-PATTERNS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from source-grounding drift verification):
- Amend/supersede/re-entry skill sections — internal/mcp/skills/embedded/specgraph-authoring/SKILL.md, specgraph-troubleshooting/SKILL.md
- e2e/api/mcp_only_lifecycle_test.go (new MCP-only Ginkgo suite)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Teach amend/supersede/re-entry in the MCP-first skills</name>
  <files>internal/mcp/skills/embedded/specgraph-authoring/SKILL.md, internal/mcp/skills/embedded/specgraph-troubleshooting/SKILL.md</files>
  <read_first>
    - internal/mcp/skills/embedded/specgraph-authoring/SKILL.md (frontmatter ~:1-8; funnel table ~:32-38; invoke/when-applies sections ~:40-57 — copy the markdown-table + fenced-example style)
    - internal/mcp/skills/embedded/specgraph-troubleshooting/SKILL.md (same-package troubleshooting entries for the "what to do when a stage no-ops" framing)
    - .planning/phases/07-authoring-lifecycle-semantics/07-CONTEXT.md (<specifics>: the exact user framing "re_entry_stage: shape means redo shape -> spec lands at spark -> author action=shape succeeds")
    - .planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md (A3: no existing skills drift-gate — correctness is on the author; keep backticked tokens consistent)
    - internal/mcp/skills/skillvalidate (name regex / summary schema; task skills:validate gates SKILL.md)
  </read_first>
  <action>
    Add an amend/supersede/re-entry subsection to specgraph-authoring/SKILL.md using the existing markdown-table + fenced-example style. Teach: amend works while a spec is in flight (approved/in_progress/review) and returns it to authoring; supersede is permitted only on a done spec; the amend param is `re_entry_stage` (required, one of spark|shape|specify|decompose) and the supersede param is `new_slug`; `reason` is required for amend and optional for supersede. Teach the land-one-before model verbatim in the user's framing: `re_entry_stage: shape` means "redo shape", the spec lands at `spark`, and `author action=shape` then succeeds — so an MCP agent never reproduces the invalid-stage-transition no-op. Add a short troubleshooting entry to specgraph-troubleshooting/SKILL.md pointing an agent who hit an "invalid stage transition" after amend to the land-one-before rule. Keep backticked tokens (`re_entry_stage`, `new_slug`, stage names) exact and consistent across both files. Preserve each file's frontmatter shape (name kebab-case, summary) so task skills:validate passes.
  </action>
  <verify>
    <automated>rg -q 're_entry_stage' internal/mcp/skills/embedded/specgraph-authoring/SKILL.md && rg -q 'new_slug' internal/mcp/skills/embedded/specgraph-authoring/SKILL.md && task skills:validate</automated>
  </verify>
  <acceptance_criteria>
    - specgraph-authoring/SKILL.md contains an amend/supersede subsection referencing re_entry_stage, new_slug, and the land-one-before model (redo shape -> land at spark -> author action=shape).
    - specgraph-troubleshooting/SKILL.md contains an entry mapping a post-amend "invalid stage transition" to the land-one-before rule.
    - Backticked tokens re_entry_stage/new_slug appear consistently; no stale target_stage/superseded_by tokens remain.
    - task skills:validate exits 0.
  </acceptance_criteria>
  <done>Both skills teach amend/supersede/re-entry with the land-one-before model; skills validate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP-only e2e — done -> amend -> re-author -> supersede with rejection cases</name>
  <files>e2e/api/mcp_only_lifecycle_test.go</files>
  <read_first>
    - e2e/api/mcp_only_authoring_test.go (Phase 6 template, full file: mcpProjectClient harness ~:49-72; friendly-YAML spark/shape/specify/decompose fixtures ~:91-139; author helper ~:228-236 and the spark->shape->specify->decompose->approve walk ~:238-242; rejection assertion style res.IsError ~:275; toolText/ContainSubstring stage-landing assertions ~:257-258; per-Describe project-slug isolation rationale ~:24-31)
    - internal/mcp/tools_execution.go (claim tool ~:46-103 action=claim uses `spec_slug` + `agent`; report tool ~:336-373 action=completion uses `slug` + `agent` — the arg name DIFFERS between claim and report; the MCP-only path to reach done)
    - e2e/api/helpers_test.go (how a spec reaches done via claim+complete, for cross-checking arg names)
    - .planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md (Code Examples: MCP-only done via claim+report; A4 arg-name caveat)
    - AGENTS.md (e2e via Ginkgo/Gomega, go test -tags e2e; assert connect codes / res.IsError, not message strings)
  </read_first>
  <behavior>
    - Uses DISTINCT slugs per scenario (no single spec is both driven-to-done and amended-in-flight): `lifecycle-amend-spec` (in-flight amend + re-author, LIFE-02); `lifecycle-done-spec` + `lifecycle-replacement` (done → supersede, LIFE-01); `lifecycle-amend-on-done` (negative amend-on-done); `lifecycle-supersede-inflight` (negative supersede-on-non-done).
    - `lifecycle-amend-spec`: walk spark->shape->specify->decompose->approve via the author tool (now in-flight/approved), amend with re_entry_stage + reason, assert it lands one stage before the re_entry_stage and the next-step hint names the re_entry_stage, then re-author that stage successfully (no invalid-stage-transition no-op).
    - `lifecycle-done-spec`: walk to approved, then reach done via the claim tool (action=claim, args `spec_slug` + `agent`) followed by the report tool (action=completion, args `slug` + `agent` — NOTE the report tool uses `slug`, not `spec_slug`). `lifecycle-replacement`: a separate non-terminal spec. Supersede `lifecycle-done-spec` via author action=supersede{new_slug=lifecycle-replacement} and assert success.
    - `lifecycle-amend-on-done`: driven to done, then amend on it is rejected (res.IsError true).
    - `lifecycle-supersede-inflight`: an in-flight (approved, non-done) spec; supersede on it is rejected (res.IsError true).
  </behavior>
  <action>
    Create e2e/api/mcp_only_lifecycle_test.go modeled wholesale on mcp_only_authoring_test.go. Reuse the mcpProjectClient harness and the friendly-YAML fixtures verbatim, but use a fresh const project slug (e.g. mcp-only-lifecycle-project) for DB isolation. Use DISTINCT spec slugs per scenario so no spec is used contradictorily: `lifecycle-amend-spec`, `lifecycle-done-spec`, `lifecycle-replacement`, `lifecycle-amend-on-done`, `lifecycle-supersede-inflight`. Confirm the exact per-tool arg names against tools_execution.go before wiring: the claim tool takes `spec_slug` + `agent` (action=claim); the report tool takes `slug` + `agent` (action=completion) — do NOT pass `spec_slug` to the report tool. Build the scenarios as separate It blocks: (1) in-flight amend on `lifecycle-amend-spec` — walk it to approved via the author tool, amend with a re_entry_stage and a reason, assert via toolText/ContainSubstring that the spec landed at the preceding stage and that the next-step hint names the re_entry_stage, then call author action=<re_entry_stage> and assert it succeeds (LIFE-02); (2) done → supersede — drive `lifecycle-done-spec` to done via author (to approved) + claim (spec_slug+agent) + report completion (slug+agent), create a non-terminal `lifecycle-replacement`, then supersede `lifecycle-done-spec` with new_slug=lifecycle-replacement and assert success (LIFE-01); (3) a negative It block driving `lifecycle-amend-on-done` to done then asserting amend on it returns res.IsError true; (4) a negative It block asserting supersede on the in-flight `lifecycle-supersede-inflight` (approved, non-done) returns res.IsError true. Assert only on res.IsError / substrings, never sanitized error message equality. Include the e2e build tag and SPDX header.
  </action>
  <verify>
    <automated>go build -tags e2e ./e2e/api/ && task test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - e2e/api/mcp_only_lifecycle_test.go exists with the e2e build tag and drives amend/supersede purely through MCP tools (author + claim + report).
    - Uses distinct slugs per scenario: `lifecycle-amend-spec` (in-flight amend), `lifecycle-done-spec` + `lifecycle-replacement` (done→supersede), `lifecycle-amend-on-done` (negative amend-on-done), `lifecycle-supersede-inflight` (negative supersede-on-non-done) — no spec used contradictorily.
    - The claim tool is called with `spec_slug` + `agent`; the report tool (completion) is called with `slug` + `agent` (NOT `spec_slug`).
    - The suite asserts re-entry lands one stage before the re_entry_stage and the re-authored stage command succeeds (LIFE-02).
    - The suite asserts amend-on-done and supersede-on-non-done are rejected (res.IsError true) (LIFE-01).
    - task test:e2e passes the MCP-only lifecycle spec.
  </acceptance_criteria>
  <done>MCP-only done->amend->re-author->supersede e2e passes, including both rejection cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent → author/claim/report tools | The e2e exercises the full MCP-only surface; skills are agent-facing teaching content (no runtime trust boundary, but shape agent behavior). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Tampering | MCP agent reproduces the #899 no-op / misuses amend | low | mitigate | Skills teach the land-one-before model and required constrained params; the e2e proves the correct sequence and both rejection paths, so regressions are caught. |
| T-07-03 | Information Disclosure | e2e error assertions | low | mitigate | Assertions use res.IsError / substrings, never sanitized message equality (AGENTS.md). |
</threat_model>

<verification>
- task skills:validate green.
- go build -tags e2e ./e2e/api/ green; task test:e2e green for the MCP-only lifecycle suite (Docker).
- Phase gate: task pr-prep green before /gsd-verify-work.
</verification>

<success_criteria>
- Skills teach amend/supersede/re-entry with the land-one-before model.
- MCP-only e2e proves done->amend->re-author->supersede and both rejection cases.
</success_criteria>

<output>
Create `.planning/phases/07-authoring-lifecycle-semantics/07-05-SUMMARY.md` when done.
</output>
