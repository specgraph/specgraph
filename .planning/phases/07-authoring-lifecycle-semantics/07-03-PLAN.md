---
phase: 07-authoring-lifecycle-semantics
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - internal/mcp/tools_authoring.go
  - internal/mcp/tools_authoring_test.go
  - internal/mcp/testhelpers_test.go
autonomous: true
requirements: [LIFE-01, LIFE-02]
must_haves:
  truths:
    - "The MCP `author` tool's amend action routes to `client.Lifecycle.TransitionAmend`, inheriting in-flight eligibility and land-one-before re-entry (D-01)."
    - "The MCP `author` tool's supersede action routes to `client.Lifecycle.TransitionSupersede`, inheriting the done-only precondition (D-01)."
    - "The amend param is `re_entry_stage` (required; spark|shape|specify|decompose), NOT `target_stage`; the supersede param is `new_slug`, NOT `superseded_by` (D-03/D-07)."
    - "The amend action requires a `reason` (D-04) and echoes the next stage command to run (D-05)."
    - "The tool Description + param docs teach the land-one-before model so an MCP agent cannot reproduce the #899 no-op (D-03)."
  artifacts:
    - internal/mcp/tools_authoring.go (rerouted handleAmend/handleSupersede + renamed params + rewritten docs)
    - internal/mcp/tools_authoring_test.go (tool tests against a lifecycle mock)
  key_links:
    - "author action=amend {slug, reason(required), re_entry_stage(required)} -> client.Lifecycle.TransitionAmend -> next-step hint composed from re_entry_stage"
    - "author action=supersede {slug, new_slug(required), reason(optional)} -> client.Lifecycle.TransitionSupersede"
---

<objective>
Point the MCP `author` tool's amend/supersede handlers at the already-correct `LifecycleService` (D-01), reconcile the tool params (`re_entry_stage`, `new_slug`), require `reason` on amend (D-04), emit a next-step hint (D-05), and rewrite the tool teaching so an MCP-only agent lands the spec correctly (D-03 / D-07 MCP half). This is a call-site swap — `client.Lifecycle` already exists on the MCP `Client`.

Purpose: Bring the MCP surface — the canonical authoring path for MCP-only agents — onto the single correct lifecycle implementation so the inverted semantics and the #899 no-op cannot recur.
Output: Rerouted `handleAmend`/`handleSupersede`, renamed and re-documented params, next-step hint, `authoringStageFromString` deleted, tool tests rewritten against a lifecycle mock.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md
@.planning/phases/07-authoring-lifecycle-semantics/07-PATTERNS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
Symbols created/changed by THIS plan (exclude from source-grounding drift verification):
- `re_entry_stage` MCP param + `new_slug` MCP param — internal/mcp/tools_authoring.go
- Amend next-step hint string (D-05) — internal/mcp/tools_authoring.go
- Deleted: `authoringStageFromString` + `TestAuthoringStageFromString`
- Lifecycle mock for tool tests (`mockLifecycleService` or extension) — internal/mcp/testhelpers_test.go
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reroute handleAmend/handleSupersede to Lifecycle, rename/redocument params, add next-step hint</name>
  <files>internal/mcp/tools_authoring.go</files>
  <read_first>
    - internal/mcp/tools_authoring.go (authorTool def() props block ~:141-172 with target_stage/superseded_by docs ~:169-172; handleAmend ~:344-366; handleSupersede ~:368-385; authoringStageFromString ~:111; analog handleApprove ~:330-342; handleSpark required-field guard ~:201-207; jsonResult/errResult/connectErrResult/stringParam helpers)
    - internal/mcp/client.go (Client.Lifecycle field ~:25 already wired ~:43)
    - proto/specgraph/v1/lifecycle.proto (TransitionAmendRequest fields Slug/Reason/ReEntryStage; TransitionSupersedeRequest fields Slug/NewSlug/Reason — Reason added by Plan 07-01)
    - cmd/specgraph/lifecycle.go (runAmend ~:50-54 and runSupersede ~:79-82 for exact request field names)
    - internal/server/lifecycle_handler.go (TransitionAmend validates empty/invalid/excluded re_entry_stage ~:54-65 — do NOT duplicate this in the tool; surface connectErrResult)
  </read_first>
  <behavior>
    - author action=amend with missing slug returns an errResult naming slug.
    - author action=amend with missing re_entry_stage returns an errResult naming the valid stages spark|shape|specify|decompose.
    - author action=amend with missing reason returns an errResult naming reason (D-04).
    - A successful amend returns the spec plus a next-step hint naming the stage command to run, composed from re_entry_stage (D-05).
    - author action=supersede with missing new_slug returns an errResult; reason is optional.
    - Invalid re_entry_stage values (approved/done/terminal) surface the handler connect error, not a tool-side re-implementation.
  </behavior>
  <action>
    In handleAmend, replace the client.Authoring.Amend(AmendRequest{TargetStage}) call with client.Lifecycle.TransitionAmend using a TransitionAmendRequest carrying Slug, Reason, and ReEntryStage. Read the re-entry value from a re_entry_stage param and pass the string straight through — the handler validates it (RESEARCH Pitfall 5); do NOT convert to the AuthoringStage enum and do NOT re-validate stage names in the tool. Add required-field guards mirroring handleSpark: empty slug, empty re_entry_stage (message must name spark|shape|specify|decompose), and empty reason (D-04). After a successful response compose a plain-string next-step hint from the requested re_entry_stage — because the spec lands one stage before the re-entry stage, the hint tells the agent to call the author action for the re_entry_stage next (D-05) — and include it alongside the jsonResult payload. In handleSupersede, replace client.Authoring.Supersede(SupersedeRequest{SupersededBy}) with client.Lifecycle.TransitionSupersede using a TransitionSupersedeRequest carrying Slug, NewSlug, and Reason; read the replacement from a new_slug param (required guard) and reason from an optional param. In def(), rename the props key target_stage to re_entry_stage and superseded_by to new_slug, and rewrite both stringProp docs in the verbose example-laden style already used for output/exchanges so the re_entry_stage doc explicitly teaches the land-one-before model (name the stage you want to redo; the spec lands one stage before so that stage's authoring command is a valid transition). Update the tool Description to reference the corrected amend (in-flight) and supersede (done-only) semantics. Delete the now-unused authoringStageFromString helper. Keep the SPDX header and package doc comment. Do NOT leave the literal strings target_stage or superseded_by in any retained comment or doc.
  </action>
  <verify>
    <automated>go build ./... && rg -q 're_entry_stage' internal/mcp/tools_authoring.go && rg -q 'new_slug' internal/mcp/tools_authoring.go && rg -q 'Lifecycle.TransitionAmend' internal/mcp/tools_authoring.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/mcp/tools_authoring.go contains `re_entry_stage` and `new_slug` and does NOT contain `target_stage` or `superseded_by`.
    - handleAmend calls `client.Lifecycle.TransitionAmend`; handleSupersede calls `client.Lifecycle.TransitionSupersede`.
    - handleAmend rejects empty slug, empty re_entry_stage, and empty reason with distinct errResult messages.
    - A successful amend result includes a next-step hint referencing the re_entry_stage.
    - `authoringStageFromString` is removed from internal/mcp/tools_authoring.go.
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>Both handlers route to Lifecycle; params renamed and re-documented; next-step hint emitted; helper deleted; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite MCP amend/supersede tool tests against a lifecycle mock</name>
  <files>internal/mcp/tools_authoring_test.go, internal/mcp/testhelpers_test.go</files>
  <read_first>
    - internal/mcp/tools_authoring_test.go (amend/supersede tool tests ~:136-176 currently hitting mockAuthoringService; TestAuthoringStageFromString ~:952-960)
    - internal/mcp/testhelpers_test.go (mockAuthoringService amend/supersede fns + methods ~:316-317,:378-389; any existing lifecycle mock to extend)
    - AGENTS.md (mock backends return sentinel errors; assert connect codes not message strings)
  </read_first>
  <behavior>
    - The rerouted amend tool test drives a lifecycle mock and asserts TransitionAmend was called with the passed slug/reason/re_entry_stage and that a next-step hint appears in the result.
    - The rerouted supersede tool test asserts TransitionSupersede was called with slug/new_slug/reason.
    - Error-path tests assert the tool surfaces the handler connect error (via connectErrResult) when the mock returns a sentinel error; assertions use res.IsError / connect codes, never error message strings.
  </behavior>
  <action>
    In testhelpers_test.go add (or extend) a lifecycle service mock exposing TransitionAmend/TransitionSupersede that records the request and returns a configurable response or sentinel error (storage.ErrSpecNotAmendable, storage.ErrSpecNotDone) — never fmt.Errorf. Rewrite the amend/supersede cases in tools_authoring_test.go to construct the authorTool against that lifecycle mock instead of mockAuthoringService: assert the recorded request fields (slug, reason, re_entry_stage / new_slug), assert the success result carries the next-step hint, and assert error cases return res.IsError true. Delete TestAuthoringStageFromString (helper removed in Task 1). Do NOT remove mockAuthoringService's Amend/Supersede methods yet — Plan 07-04 owns that removal after the proto deletion; leaving them is harmless.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/mcp/...</automated>
  </verify>
  <acceptance_criteria>
    - tools_authoring_test.go amend/supersede cases construct the tool against a lifecycle mock and assert recorded slug/reason/re_entry_stage and new_slug.
    - Success case asserts the next-step hint substring; error cases assert res.IsError, not message strings.
    - The lifecycle mock returns sentinel errors, not fmt.Errorf.
    - TestAuthoringStageFromString is deleted.
    - `go test ./internal/mcp/...` passes.
  </acceptance_criteria>
  <done>MCP tool tests validate the rerouted lifecycle calls, param names, and next-step hint; unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP agent → author tool → LifecycleService | Untrusted action/slug/reason/re_entry_stage/new_slug cross here; project scope enforced downstream by `scopeStore`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | Reroute bypassing an existing precondition | low | mitigate | Rerouting to `TransitionAmend`/`TransitionSupersede` inherits the same preconditions (amend-eligible / done-only) enforced in `LifecycleAmendSpec`/`LifecycleSupersedeSpec`; the tool adds no bypass path and re-validates nothing that would diverge. |
| T-07-04 | Tampering | re_entry_stage / new_slug input | low | mitigate | Values passed through to the handler which validates via `SpecStage.IsValid`/`ExcludesReEntry`; tool does not duplicate/weaken validation (RESEARCH Pitfall 5). |
| T-07-03 | Information Disclosure | tool error surface | low | mitigate | Tool surfaces `connectErrResult` from the sanitized handler error; tests assert res.IsError / connect codes, not message strings. |
</threat_model>

<verification>
- `go build ./... && go vet ./...` green.
- `go test ./internal/mcp/...` green.
- End-to-end amend/supersede through the tool is covered by Plan 07-05's MCP-only e2e.
</verification>

<success_criteria>
- Both handlers route to LifecycleService; params are re_entry_stage/new_slug; amend requires reason and emits a next-step hint.
- Tool Description + param docs teach the land-one-before model.
- authoringStageFromString removed; MCP unit tests green against a lifecycle mock.
</success_criteria>

<output>
Create `.planning/phases/07-authoring-lifecycle-semantics/07-03-SUMMARY.md` when done.
</output>
