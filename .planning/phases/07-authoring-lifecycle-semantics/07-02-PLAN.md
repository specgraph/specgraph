---
phase: 07-authoring-lifecycle-semantics
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - internal/storage/postgres/lifecycle.go
  - internal/storage/postgres/lifecycle_test.go
autonomous: true
requirements: [LIFE-01, LIFE-02]
must_haves:
  truths:
    - "Amending an in_progress or review spec deletes its active claim row and CLAIMED_BY edge inside the same amend transaction (D-08)."
    - "Amending an unclaimed (approved) spec is a harmless no-op with respect to claims — no error, no spurious deletes."
    - "The decompose `slices` table is left intact on amend (deferred idea — slices are re-authored output)."
    - "Storage integration tests prove: claim release on amend, amend-eligible stages, done-only supersede, re-entry lands one-before-target, and supersede reason lands in the changelog."
  artifacts:
    - internal/storage/postgres/lifecycle.go (claim-release inside LifecycleAmendSpec tx)
    - internal/storage/postgres/lifecycle_test.go (TestLifecycleAmend_ReleasesClaim + supersede reason assertion)
  key_links:
    - "LifecycleAmendSpec RunInTransaction -> GetActiveClaim(txCtx, slug) -> conditional DELETE claims row + DELETE CLAIMED_BY edge (from=spec, to=agent)"
---

<objective>
Release a spec's active claim when it is amended back to authoring (D-08), closing the stale-lease gap flagged by #900, and add the storage integration tests that gate the amend/supersede lifecycle semantics (D-10 storage half). Slices are left intact.

Purpose: A spec is no longer executable once it returns to authoring, so its lease must not linger; and the correct amend/supersede storage semantics must be pinned by integration tests.
Output: Conditional claim + CLAIMED_BY edge release inside `LifecycleAmendSpec`; `TestLifecycleAmend_ReleasesClaim` plus supersede-reason and re-entry-landing assertions in `lifecycle_test.go`.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md
@.planning/phases/07-authoring-lifecycle-semantics/07-PATTERNS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
Symbols created/changed by THIS plan (exclude from source-grounding drift verification):
- Claim-release code path (GetActiveClaim guard + two DELETEs) inside `LifecycleAmendSpec` — internal/storage/postgres/lifecycle.go
- `TestLifecycleAmend_ReleasesClaim` — internal/storage/postgres/lifecycle_test.go
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Release active claim + CLAIMED_BY edge inside LifecycleAmendSpec</name>
  <files>internal/storage/postgres/lifecycle.go</files>
  <read_first>
    - internal/storage/postgres/lifecycle.go (LifecycleAmendSpec RunInTransaction body ~:55-107; stage UPDATE guard ~:71; recomputeContentHash call ~:83)
    - internal/storage/postgres/execution.go (RecordCompletion two-statement claim+edge DELETE ~:162-177; GetActiveClaim ~:326-350 returning *storage.Claim or nil at ~:337-338; ReleaseExpiredClaims edge direction ~:310-311)
    - internal/storage/postgres/migrations/001_initial_schema.sql (claims table + PK (project_slug, spec_slug) ~:139-146)
    - AGENTS.md (ADR-004 RunInTransaction: pass txCtx not ctx; parameterized pgx only)
  </read_first>
  <behavior>
    - Amending a spec that holds an active claim removes exactly one claims row and its CLAIMED_BY edge; both gone after the amend commit.
    - Amending an unclaimed spec performs no claim delete and returns no error.
    - The amend stage transition and content-hash/changelog work still occur in the same transaction.
  </behavior>
  <action>
    Inside the existing `RunInTransaction` in `LifecycleAmendSpec`, after the stage `UPDATE ... WHERE version = $n` guard passes and adjacent to the stage mutation (before `recomputeContentHash`), fetch the active claim with `GetActiveClaim(txCtx, slug)`. If the returned claim is non-nil, execute the two DELETE statements copied verbatim from `RecordCompletion`: delete the `claims` row scoped by `(project_slug, spec_slug, agent)` using `claim.Agent`, then delete the `CLAIMED_BY` edge from `edges` where `from_slug = <spec slug>` and `to_slug = <agent>` and `edge_type = 'CLAIMED_BY'`. Use `s.exec(txCtx, ...)` for both, same `txCtx` as the surrounding tx. If `GetActiveClaim` returns nil, skip deletion (unclaimed approved specs hold no claim). Do NOT touch the `slices` table. Wrap any GetActiveClaim error as `fmt.Errorf("postgres: amend spec: get active claim: %w", err)`. Keep the SPDX header intact.
  </action>
  <verify>
    <automated>go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/storage/postgres/lifecycle.go` `LifecycleAmendSpec` calls `GetActiveClaim(txCtx, slug)` and guards deletion on a non-nil claim.
    - Two DELETEs present: one against `claims` (agent-scoped), one against `edges` with `edge_type = 'CLAIMED_BY'` and `from_slug` = spec, `to_slug` = agent.
    - No statement touches the `slices` table in the amend path.
    - All DELETEs use `txCtx` (grep shows no `ctx,` passed to exec inside the amend tx).
    - `go build ./...` and `go vet ./...` exit 0.
  </acceptance_criteria>
  <done>Active claim + edge released conditionally inside the amend tx; slices untouched; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Storage integration tests — claim release, supersede reason, re-entry landing</name>
  <files>internal/storage/postgres/lifecycle_test.go</files>
  <read_first>
    - internal/storage/postgres/lifecycle_test.go (existing amend cases ~:19-183 and supersede cases ~:184-320; testcontainers setup pattern)
    - internal/storage/postgres/execution.go (claim path used to seed a claim: ClaimSpec/GetActiveClaim)
    - internal/storage/postgres/lifecycle.go (LifecycleAmendSpec land-one-before via PrecedingAuthStage; LifecycleSupersedeSpec reason precedence from Plan 01)
    - AGENTS.md (postgres integration tests require Docker/testcontainers `pgvector/pgvector:pg18`; build tag)
  </read_first>
  <behavior>
    - Seed a spec to in_progress/review, claim it, amend it: assert the claims row AND the CLAIMED_BY edge are gone, and the spec landed at `PrecedingAuthStage(re_entry_stage)`.
    - Amend an unclaimed approved spec: assert no error and no residual claim.
    - Supersede a done spec with a non-empty reason: assert the old spec's changelog Reason equals the supplied reason; with empty reason, assert the `Superseded by <new>` default.
    - Supersede on a non-done spec: assert `storage.ErrSpecNotDone`.
  </behavior>
  <action>
    Add `TestLifecycleAmend_ReleasesClaim` to `lifecycle_test.go`: seed an approved spec, claim it via the store's claim path, drive it to in_progress/review as the existing amend fixtures do, call `LifecycleAmendSpec` with a re_entry_stage, then assert (a) `GetActiveClaim` now returns nil, (b) the `CLAIMED_BY` edge no longer exists, (c) the spec stage equals the preceding-auth-stage of the re_entry_stage. Add an unclaimed-amend sub-case asserting no error and no claim. Extend the supersede integration cases to assert the changelog `Reason` reflects a supplied reason (non-empty) and the default when empty, and that a non-done supersede returns `storage.ErrSpecNotDone`. Reuse the existing testcontainers harness and build tag in the file. Do not assert on sanitized handler error strings — this is the storage layer, assert sentinel errors via `errors.Is`.
  </action>
  <verify>
    <automated>go build -tags integration ./internal/storage/postgres/ && task test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `internal/storage/postgres/lifecycle_test.go` defines `TestLifecycleAmend_ReleasesClaim`.
    - Test asserts claim row gone, CLAIMED_BY edge gone, and spec landed one stage before the re_entry_stage.
    - Unclaimed-amend sub-case asserts no error and no claim.
    - Supersede tests assert changelog `Reason` for supplied vs empty reason and `errors.Is(err, storage.ErrSpecNotDone)` for a non-done spec.
    - `task test:integration` passes the lifecycle suite (Docker).
  </acceptance_criteria>
  <done>Integration tests pin claim-release, re-entry landing, done-only supersede, and supersede reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Amend transaction → claims/edges tables | The lease that grants execution rights is deleted here; a lingering lease would let an ephemeral worker keep executing a spec that returned to authoring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege / Tampering | Stale claim/lease after amend (LifecycleAmendSpec) | high | mitigate | D-08: delete the `claims` row and `CLAIMED_BY` edge inside the same amend transaction so an in-flight lease cannot survive the return to authoring. Verified by `TestLifecycleAmend_ReleasesClaim`. |
| T-07-06 | Tampering | Slices left intact on amend | low | accept | Deferred idea: slices are the re-authored decompose output, not stale state; intentionally not deleted. |
</threat_model>

<verification>
- `go build ./... && go vet ./...` green.
- `task test:integration` green for the lifecycle storage suite (Docker required).
</verification>

<success_criteria>
- Claim + CLAIMED_BY edge released conditionally inside the amend tx; slices untouched.
- Integration tests cover claim-release, re-entry landing, done-only supersede, and supersede reason.
</success_criteria>

<output>
Create `.planning/phases/07-authoring-lifecycle-semantics/07-02-SUMMARY.md` when done.
</output>
