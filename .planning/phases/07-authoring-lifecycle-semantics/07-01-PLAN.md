---
phase: 07-authoring-lifecycle-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/specgraph/v1/lifecycle.proto
  - gen/specgraph/v1/lifecycle.pb.go
  - internal/storage/lifecycle.go
  - internal/storage/postgres/lifecycle.go
  - internal/server/lifecycle_handler.go
  - cmd/specgraph/lifecycle.go
  - internal/server/lifecycle_handler_test.go
  - internal/server/test_scoper_test.go
  - internal/server/error_sanitize_test.go
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "A user (CLI or MCP) can record an optional reason when superseding a done spec, and that reason lands in the superseded spec's changelog entry (D-06)."
    - "The CLI `supersede` command accepts an optional `--reason` flag (D-07)."
    - "go build ./... and task test are green after the LifecycleSupersedeSpec signature change; all lifecycle fake backends are updated."
  artifacts:
    - proto/specgraph/v1/lifecycle.proto (TransitionSupersedeRequest.reason = 3)
    - internal/storage/postgres/lifecycle.go (LifecycleSupersedeSpec reason threading)
    - cmd/specgraph/lifecycle.go (supersede --reason flag)
  key_links:
    - "TransitionSupersedeRequest.reason -> LifecycleHandler.TransitionSupersede(msg.Reason) -> store.LifecycleSupersedeSpec(...reason) -> ChangeLogEntry.Reason"
---

<objective>
Thread an optional `reason` through the supersede lifecycle path so both CLI and MCP can record why a done spec was replaced (D-06), and add the CLI `--reason` flag (D-07 CLI half). This is the Wave-1 foundation: it adds the proto field and changes the `LifecycleSupersedeSpec` signature that the MCP reroute (Plan 03) depends on.

Purpose: Preserve the audit note the old MCP supersede path carried, on the single correct lifecycle path, before the divergent path is retired.
Output: `TransitionSupersedeRequest.reason` proto field, `LifecycleSupersedeSpec(ctx, oldSlug, newSlug, reason)` signature, handler threading, CLI `--reason` flag, updated lifecycle fakes.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md
@.planning/phases/07-authoring-lifecycle-semantics/07-PATTERNS.md
@AGENTS.md
@.claude/skills/buf-regen/SKILL.md
</context>

<artifacts_this_phase_produces>
Symbols created/changed by THIS plan (exclude from source-grounding drift verification):
- `TransitionSupersedeRequest.reason` (field 3) — proto/specgraph/v1/lifecycle.proto
- `LifecycleSupersedeSpec(ctx, oldSlug, newSlug, reason string)` — new signature in internal/storage/lifecycle.go + internal/storage/postgres/lifecycle.go
- `supersedeReason` var + `--reason` flag on `supersedeCmd` — cmd/specgraph/lifecycle.go
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add optional reason field to TransitionSupersedeRequest and regenerate</name>
  <files>proto/specgraph/v1/lifecycle.proto, gen/specgraph/v1/lifecycle.pb.go</files>
  <read_first>
    - proto/specgraph/v1/lifecycle.proto (TransitionSupersedeRequest at ~:114-117; analog TransitionAbandonRequest.reason=2 at ~:124-127)
    - AGENTS.md ("gen/ is committed", "task proto is incremental", proto SPDX header rule)
    - .claude/skills/buf-regen/SKILL.md (the sanctioned regen workflow)
  </read_first>
  <action>
    In proto/specgraph/v1/lifecycle.proto add `string reason = 3;` to message `TransitionSupersedeRequest` (existing fields `slug = 1`, `new_slug = 2`; field 3 is free — do NOT reserve, this is a field addition to a retained message). Give it a doc comment mirroring `TransitionAbandonRequest.reason`, marked Optional. Do NOT touch TransitionAmendRequest (already has reason=2, re_entry_stage=3). Then regenerate generated code per the buf-regen skill: run `task proto` (edits `gen/specgraph/v1/lifecycle.pb.go`; gen/ is committed — never hand-edit gen/, edit the .proto source only). Confirm the SPDX Apache-2.0 header remains on the .proto file.
  </action>
  <verify>
    <automated>grep -q 'string reason = 3;' proto/specgraph/v1/lifecycle.proto && task proto && grep -q 'Reason' gen/specgraph/v1/lifecycle.pb.go && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - proto/specgraph/v1/lifecycle.proto contains `string reason = 3;` inside `message TransitionSupersedeRequest`.
    - `TransitionSupersedeRequest.reason` numbering does NOT collide (slug=1, new_slug=2, reason=3).
    - `task proto` exits 0 and regenerates `gen/specgraph/v1/lifecycle.pb.go` with a `Reason` accessor on the request type.
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>Proto field added, generated code regenerated and committed, build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread reason through LifecycleSupersedeSpec, handler, and lifecycle fakes</name>
  <files>internal/storage/lifecycle.go, internal/storage/postgres/lifecycle.go, internal/server/lifecycle_handler.go, internal/server/lifecycle_handler_test.go, internal/server/test_scoper_test.go, internal/server/error_sanitize_test.go</files>
  <read_first>
    - internal/storage/lifecycle.go (LifecycleBackend interface, LifecycleSupersedeSpec entry ~:114; sibling LifecycleAmendSpec/LifecycleAbandonSpec)
    - internal/storage/postgres/lifecycle.go (LifecycleSupersedeSpec impl ~:113; old-spec changelog default `fmt.Sprintf("Superseded by %s", newSlug)` ~:217; var _ storage.LifecycleBackend assertion ~:19; sibling LifecycleAbandonSpec reason->ChangeLogEntry.Reason wiring ~:246,:294)
    - internal/server/lifecycle_handler.go (TransitionSupersede ~:79-97; analog TransitionAbandon passing msg.Reason ~:127)
    - internal/storage/changelog.go (ChangeLogEntry.Reason already exists — no migration)
    - internal/server/lifecycle_handler_test.go (fakeLifecycleBackend.LifecycleSupersedeSpec ~:48)
    - internal/server/test_scoper_test.go (stubBackend.LifecycleSupersedeSpec ~:246)
    - internal/server/error_sanitize_test.go (errorBackend.LifecycleSupersedeSpec ~:196)
  </read_first>
  <behavior>
    - When supersede is called with a non-empty reason, the superseded (old) spec's changelog entry Reason equals the caller-supplied reason.
    - When reason is empty, the changelog Reason falls back to the existing `Superseded by <new_slug>` default (RESEARCH A2 precedence).
    - Handler-level error mapping is unchanged (assert connect codes, never message strings).
  </behavior>
  <action>
    Change the `LifecycleSupersedeSpec` signature from `(ctx, oldSlug, newSlug string)` to `(ctx, oldSlug, newSlug, reason string)` in the `LifecycleBackend` interface (internal/storage/lifecycle.go) and its postgres impl (internal/storage/postgres/lifecycle.go). In the impl, set the old-spec changelog `Reason` to the caller `reason` when non-empty, otherwise keep the existing `Superseded by <newSlug>` default; keep all work inside the existing `RunInTransaction` and pass `txCtx` (ADR-004). Update the handler `TransitionSupersede` to pass `msg.Reason` as the fourth argument (mirror `TransitionAbandon`); add NO new validation — reason is optional for supersede. Then fix every lifecycle fake so the compile-time `var _ storage.LifecycleBackend = (*Store)(nil)` assertion and the fake backends all satisfy the new signature: `fakeLifecycleBackend`, `stubBackend`, and `errorBackend` each gain the `reason string` parameter. Fakes must return sentinel errors (never `fmt.Errorf`) where handler code uses `errors.Is`.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/storage/... ./internal/server/... </automated>
  </verify>
  <acceptance_criteria>
    - `internal/storage/lifecycle.go` declares `LifecycleSupersedeSpec(ctx context.Context, oldSlug, newSlug, reason string) (*Spec, *Spec, error)`.
    - `internal/server/lifecycle_handler.go` calls `store.LifecycleSupersedeSpec(ctx, msg.Slug, msg.NewSlug, msg.Reason)`.
    - postgres impl uses caller `reason` when non-empty else the `Superseded by` default (grep both branches present).
    - `go build ./...` and `go vet ./...` exit 0; `internal/server` + `internal/storage` unit tests pass.
    - No fake backend returns a raw `fmt.Errorf` for a sentinel-checked path.
  </acceptance_criteria>
  <done>Reason threads proto->handler->store->changelog; all lifecycle fakes compile; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional --reason flag to the CLI supersede command</name>
  <files>cmd/specgraph/lifecycle.go</files>
  <read_first>
    - cmd/specgraph/lifecycle.go (runSupersede ~:79-82 building TransitionSupersedeRequest; amendReason var + MarkFlagRequired wiring ~:308-309; init() flag registration ~:314)
    - AGENTS.md (CLI conventions; do not touch amend/supersede beyond adding the flag)
  </read_first>
  <action>
    Add a package-level `supersedeReason` string var next to the existing amend reason var. In `runSupersede`, set `Reason: supersedeReason` on the `TransitionSupersedeRequest`. In `init()`, register `supersedeCmd.Flags().StringVar(&supersedeReason, "reason", "", "reason for supersession (optional)")`. Do NOT call `MarkFlagRequired` — supersede reason is optional (unlike amend's required `--reason`). Do not change any other CLI behavior. Ensure the SPDX header is intact.
  </action>
  <verify>
    <automated>go build ./... && go run ./cmd/specgraph supersede --help 2>&1 | grep -q -- '--reason'</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/specgraph/lifecycle.go` registers a `--reason` flag on `supersedeCmd` that is NOT marked required.
    - `runSupersede` populates `TransitionSupersedeRequest.Reason` from the flag.
    - `specgraph supersede --help` lists `--reason`.
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>CLI supersede accepts an optional --reason flag wired to the request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP client → LifecycleService handler | Untrusted `reason`, `new_slug`, `slug` cross here; project scope enforced by `scopeStore` (X-Specgraph-Project). |
| Handler → postgres store | Parameterized pgx only; reason written to changelog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Elevation of Privilege | LifecycleService.TransitionSupersede | low | mitigate | Handler runs `scopeStore(ctx, h.scoper)` first (inherited); reason addition adds no new authz surface. |
| T-07-03 | Information Disclosure | supersede error mapping | low | mitigate | Errors sanitized via `lifecycleError`; tests assert connect codes, not strings. |
| T-07-04 | Tampering | reason field in changelog | low | accept | `reason` is a free-text audit note stored via parameterized pgx (no injection); length bounds inherited from handler validation of the request. No high-severity finding — supersede is a done-only state transition with no execution effect. |
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` green.
- `task test` green (unit; no Docker).
- Supersede-reason changelog assertion is added in Plan 02's storage integration tests (owns lifecycle_test.go).
</verification>

<success_criteria>
- Proto `TransitionSupersedeRequest.reason` exists and gen/ regenerated.
- `LifecycleSupersedeSpec` threads reason into the changelog with correct precedence.
- CLI `supersede --reason` works.
- All lifecycle fakes updated; build + unit tests green.
</success_criteria>

<output>
Create `.planning/phases/07-authoring-lifecycle-semantics/07-01-SUMMARY.md` when done.
</output>
