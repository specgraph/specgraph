---
phase: 07-authoring-lifecycle-semantics
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - proto/specgraph/v1/authoring.proto
  - gen/specgraph/v1/authoring.pb.go
  - gen/specgraph/v1/specgraphv1connect/authoring.connect.go
  - internal/server/authoring_handler.go
  - internal/storage/postgres/authoring.go
  - internal/storage/authoring.go
  - internal/storage/stage_validation.go
  - internal/storage/stage_validation_test.go
  - internal/server/authoring_handler_test.go
  - internal/server/test_scoper_test.go
  - internal/mcp/testhelpers_test.go
  - internal/storage/postgres/authoring_test.go
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "The divergent broken amend/supersede path is fully retired: no AuthoringService.Amend/Supersede RPCs, no AmendSpec/SupersedeSpec storage methods, no storage.AmendResult (D-02)."
    - "There is a single source of truth for amend/supersede — the LifecycleService path — so the inverted semantics cannot re-diverge."
    - "go build ./... and task check are green at every commit boundary; compile-blocking fakes/tests land with the deletion."
    - "TransitionStage in postgres/authoring.go is preserved (it powers the funnel + approve)."
  artifacts:
    - proto/specgraph/v1/authoring.proto (Amend/Supersede RPCs + 4 messages removed)
    - internal/storage/authoring.go (AuthoringSpecLifecycle removed; AmendResult deleted)
  key_links:
    - "Removing AuthoringSpecLifecycle methods -> AuthoringBackend drops the embed -> all fakes updated -> proto regen shrinks AuthoringService interfaces"
---

<objective>
Delete the now-unused broken amend/supersede twin (D-02): the `AuthoringService.Amend/Supersede` RPCs and their four messages, the `AuthoringHandler.Amend/Supersede` handlers, `Store.AmendSpec/SupersedeSpec`, the `AuthoringSpecLifecycle` interface methods, `storage.AmendResult`, and the orphaned `ValidateAmendTransition` — with every compile-blocking fake and dead test pruned in the same change. Ordered Go-deletion-first, proto-deletion-second so the build is green at each commit boundary.

Purpose: One source of truth. After Plan 07-03 reroutes the MCP surface, this path has zero callers; retiring it makes the inverted semantics impossible to re-introduce.
Output: Retired proto RPCs/messages, deleted handlers/storage/interface/orphans, pruned fakes and tests, regenerated gen/.
</objective>

<execution_context>
@/Users/SeBrandt/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/SeBrandt/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md
@.planning/phases/07-authoring-lifecycle-semantics/07-PATTERNS.md
@AGENTS.md
@.claude/skills/buf-regen/SKILL.md
</context>

<artifacts_this_phase_produces>
Symbols DELETED by THIS plan (removals — nothing new to source-ground):
- proto: rpc Amend, rpc Supersede; messages AmendRequest/AmendResponse/SupersedeRequest/SupersedeResponse
- Go: AuthoringHandler.Amend/Supersede, Store.AmendSpec/SupersedeSpec, AuthoringSpecLifecycle methods/embed, storage.AmendResult, storage.ValidateAmendTransition (+TestValidateAmendTransition)
- test doubles: amend/supersede methods on fakeAuthoringBackend, authoringTestBackend, stubBackend, mockAuthoringService; TestAmend*/TestSupersedeSpec_* fixtures
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete Go impls + prune dead tests (build stays green with proto types still present)</name>
  <files>internal/server/authoring_handler.go, internal/storage/postgres/authoring.go, internal/storage/authoring.go, internal/storage/stage_validation.go, internal/storage/stage_validation_test.go, internal/server/authoring_handler_test.go, internal/server/test_scoper_test.go, internal/mcp/testhelpers_test.go, internal/storage/postgres/authoring_test.go</files>
  <read_first>
    - internal/server/authoring_handler.go (Amend ~:551, Supersede ~:593; preserve funnel handlers + NextPrompts/promptsToProto)
    - internal/storage/postgres/authoring.go (AmendSpec ~:363, SupersedeSpec ~:297; PRESERVE TransitionStage ~:27; var _ storage.AuthoringBackend assertion ~:20)
    - internal/storage/authoring.go (AuthoringSpecLifecycle iface ~:158-161 with only AmendSpec/SupersedeSpec; AuthoringBackend embed ~:166-171; AmendResult struct ~:136-141)
    - internal/storage/stage_validation.go (ValidateAmendTransition ~:78 — sole prod caller was AmendSpec; ValidateTransition preserved)
    - internal/storage/stage_validation_test.go (TestValidateAmendTransition ~:86)
    - internal/server/authoring_handler_test.go (fakeAuthoringBackend.amendResult ~:32, SupersedeSpec/AmendSpec ~:67,:71; authoringTestBackend methods ~:191,:195; TestAmend* fixtures ~:719,1120,1135,1330,1389,1630)
    - internal/server/test_scoper_test.go (stubBackend.SupersedeSpec/AmendSpec ~:192,:196)
    - internal/mcp/testhelpers_test.go (mockAuthoringService.amend/supersede fns + methods ~:316-317,:378-389)
    - internal/storage/postgres/authoring_test.go (TestSupersedeSpec_Authoring, TestSupersedeSpec_NotFound, TestAmendSpec* ~:423-560)
    - RESEARCH "Blast Radius" tables and Open Question 1 (delete AuthoringSpecLifecycle rather than keep empty)
  </read_first>
  <action>
    Delete the Go implementations while the proto message types still exist (so the build stays green): remove AuthoringHandler.Amend and AuthoringHandler.Supersede from internal/server/authoring_handler.go; remove Store.AmendSpec and Store.SupersedeSpec from internal/storage/postgres/authoring.go while PRESERVING TransitionStage; in internal/storage/authoring.go remove the AmendSpec/SupersedeSpec methods from AuthoringSpecLifecycle — since that leaves the interface empty, delete the AuthoringSpecLifecycle interface entirely and drop its embed from AuthoringBackend (RESEARCH A1/OQ1) — and delete the storage.AmendResult struct; delete storage.ValidateAmendTransition from stage_validation.go and TestValidateAmendTransition from stage_validation_test.go. Then prune every compile-blocking test double and dead test: remove amend/supersede methods plus the amendResult field from fakeAuthoringBackend, authoringTestBackend (authoring_handler_test.go), stubBackend (test_scoper_test.go), and mockAuthoringService (testhelpers_test.go); delete the TestAmend*/AmendResult handler fixtures in authoring_handler_test.go and the TestSupersedeSpec_Authoring/TestSupersedeSpec_NotFound/TestAmendSpec* cases in postgres/authoring_test.go. Keep SPDX headers and package doc comments intact. This whole task is one atomic build-green boundary; the proto RPCs/messages are removed in Task 2.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/server/... ./internal/mcp/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/server/authoring_handler.go does NOT contain a func AuthoringHandler Amend or Supersede.
    - internal/storage/postgres/authoring.go does NOT contain AmendSpec or SupersedeSpec but DOES still contain TransitionStage.
    - internal/storage/authoring.go does NOT contain AmendResult or AuthoringSpecLifecycle.
    - internal/storage/stage_validation.go does NOT contain ValidateAmendTransition.
    - go build ./... and go vet ./... exit 0; server and mcp unit tests pass.
  </acceptance_criteria>
  <done>Broken Go path deleted, all fakes/dead tests pruned, build + unit tests green (proto types still present).</done>
</task>

<task type="auto">
  <name>Task 2: Remove Amend/Supersede RPCs + messages from authoring.proto and regenerate</name>
  <files>proto/specgraph/v1/authoring.proto, gen/specgraph/v1/authoring.pb.go, gen/specgraph/v1/specgraphv1connect/authoring.connect.go</files>
  <read_first>
    - proto/specgraph/v1/authoring.proto (service AuthoringService rpc Amend ~:440, rpc Supersede ~:443; messages AmendRequest ~:375-384, AmendResponse ~:387-394, SupersedeRequest ~:397-404, SupersedeResponse ~:407-412)
    - .planning/phases/07-authoring-lifecycle-semantics/07-RESEARCH.md ("Proto removal semantics" correction — whole-message and RPC-method removals use plain deletion, NOT reserved)
    - .claude/skills/buf-regen/SKILL.md (regen workflow)
    - AGENTS.md (gen/ is committed; edits to gen/ blocked — edit .proto only)
  </read_first>
  <action>
    Delete the rpc Amend and rpc Supersede method lines from service AuthoringService, and delete the four message definitions AmendRequest, AmendResponse, SupersedeRequest, SupersedeResponse. Do NOT use reserved — per the RESEARCH correction, proto3 has no reserved for service methods or whole message types; the AGENTS.md reserved gotcha applies only to fields removed from a retained message, which is not the case here. Regenerate with task proto per the buf-regen skill (updates gen/specgraph/v1/authoring.pb.go and the specgraphv1connect authoring.connect.go; never hand-edit gen/). Confirm no remaining references in internal or gen.
  </action>
  <verify>
    <automated>task proto && go build ./... && task check</automated>
  </verify>
  <acceptance_criteria>
    - proto/specgraph/v1/authoring.proto contains no rpc Amend, no rpc Supersede, and none of the four Amend/Supersede request/response messages.
    - No reserved statement was added for the removed RPCs/messages.
    - rg for AmendRequest/SupersedeRequest across internal and gen/specgraph/v1/specgraphv1connect returns no matches.
    - task proto exits 0 and gen/ is regenerated; go build ./... exits 0; task check is green.
  </acceptance_criteria>
  <done>Broken proto path removed, gen/ regenerated and committed, full check green — single source of truth achieved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → AuthoringService | The retired RPCs are removed from the wire surface; the remaining funnel/approve RPCs are unchanged and still scope-enforced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering | Divergent second amend/supersede implementation | medium | mitigate | Deleting the broken path removes the possibility of the inverted (approved-rejecting, any-state-supersede, land-at-target) semantics being reachable again — the core consolidation goal. Verified by absence greps + green build. |
| T-07-08 | Denial of Service | Removing an in-use RPC | low | mitigate | Plan 07-03 rerouted the only callers first; blast-radius greps confirm zero remaining callers before deletion, so no live consumer breaks. |
</threat_model>

<verification>
- Task 1 commit: go build ./... + go vet + server/mcp unit tests green (proto still present).
- Task 2 commit: task proto + go build ./... + task check green (proto removed, gen regenerated).
</verification>

<success_criteria>
- No AuthoringService.Amend/Supersede RPCs, messages, handlers, storage methods, AmendResult, or ValidateAmendTransition remain.
- TransitionStage preserved; funnel + approve unaffected.
- task check green.
</success_criteria>

<output>
Create `.planning/phases/07-authoring-lifecycle-semantics/07-04-SUMMARY.md` when done.
</output>
