---
phase: 01-release-build-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Taskfile.yml
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - REL-01
  - CFG-01
  - CFG-02
must_haves:
  truths:
    - "Running `task tools` installs golangci-lint at the pinned version (v2.12.1) via `go install`, not via unpinned `brew install`"
    - "`task tools:golangci-lint-version` prints exactly the bare pinned version string, with no Task command-echo banner"
    - "The golangci-lint version has exactly one literal declaration in the repo (Taskfile.yml's vars block); ci.yml no longer declares its own"
    - "CI's build-and-test job resolves the golangci-lint version from Taskfile.yml via command substitution, so a clean local `task check` guarantees the same lint version CI runs"
  artifacts:
    - "Taskfile.yml — new global var GOLANGCI_LINT_VERSION, new leaf task tools:golangci-lint-version, edited tools task"
    - ".github/workflows/ci.yml — env var removed, Install Go tools step resolves version via task"
  key_links:
    - "ci.yml build-and-test 'Install Go tools' step -> Taskfile.yml tools:golangci-lint-version leaf task (via $(...) command substitution); breaks if the leaf task lacks silent: true or gains deps"
---

<objective>
Close CFG-02: make `task tools`' golangci-lint install use the exact same pinned version CI uses, with one source of truth. Switch the local install from unpinned `brew install golangci-lint` to `go install .../golangci-lint@{{.GOLANGCI_LINT_VERSION}}` (matching CI's method, per D-01), promote the version string to a single Taskfile.yml var that CI reads back (per D-03), and remove ci.yml's independent copy so the two can never drift again (per D-02, D-04).

Purpose: A clean local `task check` must guarantee a clean CI lint run — today it can't, because local (brew, currently v2.12.2) and CI (pinned v2.12.1) resolve different golangci-lint versions.
Output: Edited Taskfile.yml and .github/workflows/ci.yml. No new files, no new dependencies.

## Already Shipped (traceability only — NO tasks)

REL-01 and CFG-01 are listed in this plan's `requirements` frontmatter purely for phase-level requirements-coverage traceability. Both were verified already fully implemented and shipped on `main` during the phase-context session (see 01-CONTEXT.md D-00):
- **REL-01** (single-job goreleaser release model): shipped via PR #981; all three roadmap success criteria verified against v0.12.0's actual GitHub Release.
- **CFG-01** (koanf layered config loader): shipped in `internal/config/global.go` + `cmd/specgraph/serve.go`.

Do NOT create, re-plan, or re-verify any REL-01 or CFG-01 work in this plan. The only implementation work here is CFG-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-build-tooling/01-CONTEXT.md
@.planning/phases/01-release-build-tooling/01-RESEARCH.md
@.planning/phases/01-release-build-tooling/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin golangci-lint in Taskfile.yml and add the version-source leaf task</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml lines 1-20 (existing `vars:` block — see the flat `PROTOC_GEN_GO_VERSION` / `PROTOC_GEN_CONNECT_GO_VERSION` convention at lines 18-19)
    - Taskfile.yml lines 353-362 (the `tools:` task being edited)
    - .planning/phases/01-release-build-tooling/01-RESEARCH.md — "Architecture Patterns → Pattern: Taskfile-as-source-of-truth" and "Common Pitfalls → Pitfall 1" (why silent: true is mandatory)
    - .planning/phases/01-release-build-tooling/01-PATTERNS.md — "Taskfile.yml — add global var + new leaf task + edit tools: task" (Analog A + Analog B, exact line ranges)
    - .planning/phases/01-release-build-tooling/01-CONTEXT.md — decisions D-01, D-03, D-05
  </read_first>
  <action>
    Three edits to Taskfile.yml, all following in-file sibling conventions:

    1. In the global `vars:` block, immediately after the `PROTOC_GEN_CONNECT_GO_VERSION` entry (currently line 19), add a new flat static var whose key is `GOLANGCI_LINT_VERSION` and whose value is the string `v2.12.1` — same flat `KEY: value` shape as the two `PROTOC_GEN_*` siblings above it (NOT the dynamic `sh:` form used by `VERSION`/`COMMIT`). This is the single literal declaration of the pinned version for the whole repo (per D-03). Confirm at execution time that `v2.12.1` still matches ci.yml's current pin before writing (RESEARCH.md flags the version numbers may drift if consumed weeks later).

    2. In the `tools:` task (currently line 354-362): remove the token `golangci-lint` from the `brew install ...` line (line 357) only — leave every other brewed tool (`gofumpt lefthook actionlint goreleaser dprint cocogitto rumdl yamlfmt buf beads`) intact, per D-05 which scopes this fix to golangci-lint alone. Then add a new command line `go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@{{.GOLANGCI_LINT_VERSION}}`, placed alongside the two existing `protoc-gen-*` `go install` lines (lines 360-361), matching their exact `go install <module-path>@{{.<VAR>}}` templating shape.

    3. Add a new leaf task named `tools:golangci-lint-version`, placed adjacent to the `tools:` task. It MUST set `silent: true` at the task level and have a single command that echoes `{{.GOLANGCI_LINT_VERSION}}`. It MUST NOT declare any `deps:` — it stays a dependency-free leaf so CI can call it cheaply without triggering codegen or a web build (RESEARCH.md Anti-Patterns). `silent: true` is load-bearing, not cosmetic: without it Task prints a command-echo banner that contaminates the string CI captures via command substitution (RESEARCH.md Pitfall 1). Give it a `desc:` noting it is the single source of truth that ci.yml reads.
  </action>
  <verify>
    <automated>test "$(task tools:golangci-lint-version)" = "v2.12.1" && rg -qE '^\s*GOLANGCI_LINT_VERSION:\s*v2\.12\.1\s*$' Taskfile.yml && rg -q 'golangci-lint/v2/cmd/golangci-lint@\{\{\.GOLANGCI_LINT_VERSION\}\}' Taskfile.yml && ! rg -q 'brew install.*golangci-lint' Taskfile.yml</automated>
  </verify>
  <acceptance_criteria>
    - `task tools:golangci-lint-version` outputs exactly the single line `v2.12.1` with no `task:`/`[tools:...]` banner text (proves silent: true is set correctly).
    - The `brew install` line in the `tools:` task no longer contains `golangci-lint`; all other brewed tools remain.
    - The `tools:` task contains a `go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@{{.GOLANGCI_LINT_VERSION}}` line.
    - The new leaf task has `silent: true` and no `deps:` key.
  </acceptance_criteria>
  <done>
    Taskfile.yml declares GOLANGCI_LINT_VERSION once, installs golangci-lint via go install at that pinned version, and exposes a silent leaf task that prints the bare version string.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make ci.yml read the pinned version from Taskfile.yml instead of its own env var</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 24-37 (workflow-level `env:` block; the golangci-lint version entry is at line 27)
    - .github/workflows/ci.yml lines 63-80 (the `build-and-test` job's "Install Task" step at 63-67 that guarantees `task` is on PATH, and the "Install Go tools" step at 76-80 being edited)
    - .planning/phases/01-release-build-tooling/01-RESEARCH.md — "Architecture Patterns → Example — ci.yml", "Common Pitfalls → Pitfall 2" (edit only build-and-test, not e2e/e2e-agent) and "Pitfall 3" (Renovate is a confirmed non-issue)
    - .planning/phases/01-release-build-tooling/01-PATTERNS.md — ".github/workflows/ci.yml — remove independent env var, resolve version via Task"
    - .planning/phases/01-release-build-tooling/01-CONTEXT.md — decisions D-02, D-03
  </read_first>
  <action>
    Two edits, both inside the `build-and-test` job only. Do NOT touch the `e2e` or `e2e-agent` jobs — they never install golangci-lint (RESEARCH.md Pitfall 2).

    1. In the workflow-level `env:` block (lines 24-37), delete the entry that pins the golangci-lint version — the `GOLANGCI_LINT_VERSION` key currently at line 27. Leave every sibling entry (`GO_VERSION`, `NODE_VERSION`, `PROTOC_GEN_*`, `YAMLFMT_VERSION`, `ADDLICENSE_VERSION`, `RUMDL_*`, `DPRINT_*`) untouched — D-05 scopes this to golangci-lint only. Taskfile.yml is now the sole declaration.

    2. In the "Install Go tools" step (lines 76-80), replace the golangci-lint install line so the version is resolved by command substitution from the Taskfile leaf task instead of the deleted env var. The new line is `go install "github.com/golangci/golangci-lint/v2/cmd/golangci-lint@$(task tools:golangci-lint-version)"` (keep the surrounding double-quotes so the substituted `@version` is passed as one argument). Leave the `yamlfmt` and `addlicense` install lines in that same step exactly as they are — they still read `${{ env.YAMLFMT_VERSION }}` / `${{ env.ADDLICENSE_VERSION }}`. No new step is needed: the "Install Task" step (arduino/setup-task, lines 63-67) already put `task` on PATH earlier in this same job, before this step runs.
  </action>
  <verify>
    <automated>! rg -qE '^\s*GOLANGCI_LINT_VERSION:\s*v[0-9]' .github/workflows/ci.yml && rg -q 'golangci-lint@\$\(task tools:golangci-lint-version\)' .github/workflows/ci.yml && task lint</automated>
  </verify>
  <acceptance_criteria>
    - No line in .github/workflows/ci.yml declares a golangci-lint version literal (the env entry is gone).
    - The "Install Go tools" step installs golangci-lint via `@$(task tools:golangci-lint-version)` command substitution.
    - The `yamlfmt` and `addlicense` install lines are unchanged.
    - `e2e` and `e2e-agent` job definitions are unchanged (no diff outside `build-and-test`).
    - `task lint` (which runs actionlint over the workflow) passes on the edited ci.yml.
  </acceptance_criteria>
  <done>
    ci.yml's build-and-test job resolves the golangci-lint version from Taskfile.yml via `task tools:golangci-lint-version`; no independent version literal remains anywhere in ci.yml.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Artifact | File | Kind | Notes |
|----------|------|------|-------|
| `GOLANGCI_LINT_VERSION` var (value `v2.12.1`) | Taskfile.yml global `vars:` block | new Taskfile var | Single source of truth for the pinned golangci-lint version |
| `tools:golangci-lint-version` task | Taskfile.yml | new leaf task | `silent: true`, no `deps:`; echoes the version var; consumed by CI via command substitution |
| Edited `tools:` task | Taskfile.yml | modified task | golangci-lint removed from `brew install`, added as a `go install ...@{{.GOLANGCI_LINT_VERSION}}` line |
| Removed `GOLANGCI_LINT_VERSION` env entry | .github/workflows/ci.yml (`env:` block) | deletion | Eliminates the second, drift-prone declaration |
| Edited "Install Go tools" step | .github/workflows/ci.yml (`build-and-test` job) | modified step | golangci-lint install line resolves version via `$(task tools:golangci-lint-version)` |

No REL-01 / CFG-01 artifacts — both already shipped (traceability-only entries in this plan).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo write access -> build/CI config | Anyone who can commit to Taskfile.yml or ci.yml controls what the local `task tools` and the CI build-and-test job execute. This is the same boundary that already governs ci.yml today; this phase does not widen it. |
| Go module proxy / GOSUMDB -> installed golangci-lint binary | The pinned golangci-lint is fetched by `go install`, resolved and checksum-verified through the Go module proxy + checksum database — identical to the install command already running in CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering | `go install github.com/golangci/golangci-lint/v2/...` (Taskfile.yml + ci.yml install lines) | low | accept | Supply-chain trust model is unchanged by this phase — the binary is fetched and checksum-verified via the Go module proxy + GOSUMDB, exactly as CI's existing install already does. This phase only changes where the version string is declared, not how the binary is fetched or verified. golangci-lint is a well-established, already-vendored dependency (RESEARCH.md Package Legitimacy Audit: manual verdict OK, already running in production CI). |
| T-1-02 | Tampering | ci.yml `$(task tools:golangci-lint-version)` command substitution | low | accept | The substituted value is a static, repo-committed version string echoed by a dependency-free leaf task. Injecting a malicious value would require write access to Taskfile.yml — the same trust boundary as editing ci.yml directly, which an attacker with that access already controls. No new privilege or external input is introduced. The double-quoted `@$(...)` form keeps the resolved value as a single argument. |
| T-1-SC | Tampering | package-manager installs (npm/pip/cargo) | n/a | accept | Not applicable — this phase installs no new npm/pip/cargo package. The only install is a `go install` of an already-present, already-CI-installed Go module (no new dependency, no [ASSUMED]/[SUS]/[SLOP] package); Go modules are outside the automated package-legitimacy seam's scope and were manually verified OK in RESEARCH.md. No legitimacy checkpoint required. |
</threat_model>

<verification>
Phase-level checks (run after both tasks):
- `task tools:golangci-lint-version` prints exactly `v2.12.1` (bare, no banner).
- `rg -c 'GOLANGCI_LINT_VERSION' Taskfile.yml` == 1 declaration; `! rg -qE '^\s*GOLANGCI_LINT_VERSION:\s*v[0-9]' .github/workflows/ci.yml` (ci.yml no longer declares its own).
- `task lint` passes (actionlint validates the edited ci.yml syntax; yamlfmt/dprint validate formatting).
- Manual/CI confirmation: after the change lands, a `build-and-test` CI run's "Install Go tools" log shows golangci-lint installed at the version printed by the Taskfile task.
</verification>

<success_criteria>
- CFG-02 met: `task tools` installs golangci-lint via `go install` at the single pinned version declared once in Taskfile.yml; CI's build-and-test job resolves that same version from Taskfile.yml, so a clean local `task check` guarantees the same golangci-lint version CI runs. The brew-based drift vector (local v2.12.2 vs CI v2.12.1) is closed structurally.
- REL-01, CFG-01: no work — already shipped; present for traceability only.
</success_criteria>

<output>
Create `.planning/phases/01-release-build-tooling/01-01-SUMMARY.md` when done.
</output>
