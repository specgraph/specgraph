---
phase: 05-ui-project-selector-and-refinements
plan: 07
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/lib/components/LoginModal.svelte
  - web/src/lib/components/RevealKeyModal.svelte
autonomous: true
requirements: [D-12, D-13]
user_setup: []

must_haves:
  truths:
    - "LoginModal and RevealKeyModal render on shadcn Dialog/AlertDialog + Slate tokens with no scoped navy/blue CSS."
    - "The destructive revoke path uses AlertDialog with destructive Button; existing auth/reveal behavior preserved."
  artifacts:
    - "web/src/lib/components/LoginModal.svelte (Dialog + Input + Button)"
    - "web/src/lib/components/RevealKeyModal.svelte (Dialog reveal + AlertDialog revoke)"
  key_links:
    - "RevealKeyModal revoke → AlertDialog destructive confirm (UI-SPEC copy contract)"
---

<objective>
Migrate the two modal components — `LoginModal` and `RevealKeyModal` — to shadcn `Dialog`/`AlertDialog` primitives + Slate tokens, preserving auth and key-reveal behavior (D-12/D-13). The revoke path uses the destructive `AlertDialog` per the UI-SPEC copy contract.

Purpose: These gate auth and key management; a11y-correct shadcn dialogs (focus trap, ESC) replace the hand-rolled overlays.
Output: Two token-based modal components on bits-ui-backed primitives.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate LoginModal to shadcn Dialog + Input + Button</name>
  <files>web/src/lib/components/LoginModal.svelte</files>
  <read_first>
    - web/src/lib/components/LoginModal.svelte (overlay+card L38-64, button navy L99 — PATTERNS L299)
    - web/src/lib/components/ui/dialog, input, button
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (LoginModal → Dialog + Input + Button)
    - web/src/lib/auth.svelte (auth flow the modal drives — preserve callbacks like onSuccess)
  </read_first>
  <action>
    Re-implement `LoginModal.svelte` on shadcn `Dialog` with `Input` fields and a primary `Button` (retire the navy button and hand-rolled overlay). Preserve all props (e.g. `onSuccess`, `authError`) and the login behavior/wiring exactly. Delete the scoped `<style>`; use token utility classes. Keep bits-ui focus-trap/ESC behavior (do not re-add custom key handling). Keep Svelte 5 runes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/LoginModal.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Dd]ialog/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; uses shadcn `Dialog`/`Input`/`Button`; `onSuccess`/`authError` props + login behavior preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>LoginModal on shadcn Dialog with preserved auth flow.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate RevealKeyModal to Dialog (reveal) + AlertDialog (revoke)</name>
  <files>web/src/lib/components/RevealKeyModal.svelte</files>
  <read_first>
    - web/src/lib/components/RevealKeyModal.svelte (reveal + revoke behavior)
    - web/src/lib/components/ui/dialog, alert-dialog, button
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (RevealKeyModal → Dialog reveal; AlertDialog revoke), "Copywriting Contract" (Revoke AlertDialog: title "Revoke this key?", body, confirm "Revoke" variant=destructive, cancel "Cancel")
  </read_first>
  <action>
    Re-implement `RevealKeyModal.svelte` using shadcn `Dialog` for the key-reveal path and shadcn `AlertDialog` for the destructive revoke confirmation. The revoke confirm uses `Button variant="destructive"` labeled **Revoke**, cancel labeled **Cancel**, with the exact title/body copy from the UI-SPEC Copywriting Contract. Preserve the one-time-plaintext reveal behavior and all props (do not change the transient reveal-state handling). Delete the scoped `<style>`; use token utility classes; keep runes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/RevealKeyModal.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Aa]lert[Dd]ialog|alert-dialog/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; reveal uses `Dialog`, revoke uses `AlertDialog` with destructive `Button` and the UI-SPEC copy.
    - Reveal/revoke behavior and props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>RevealKeyModal on shadcn Dialog/AlertDialog with the destructive revoke contract.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

Modified files:
- `web/src/lib/components/LoginModal.svelte` — shadcn Dialog + Input + Button
- `web/src/lib/components/RevealKeyModal.svelte` — shadcn Dialog (reveal) + AlertDialog (revoke)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user credential input → auth flow | Login form fields |
| revealed plaintext key → DOM | One-time key material shown in reveal modal |
| revoke action → cookie-authed mutation | Destructive key revoke RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Information disclosure | one-time plaintext key in reveal Dialog | high | mitigate | Preserve existing transient reveal-state handling; the key stays in component-local reveal state only (not module state) — do not persist or log it; migration must not widen its lifetime. |
| T-05-11 | Spoofing/CSRF | revoke mutation | medium | accept | Unchanged — existing `csrfInterceptor` double-submit covers cookie-authed mutations; this migration adds no new mutation path. |
| T-05-12 | Tampering/XSS | error/key text rendered in modals | high | mitigate | Svelte `{text}` bindings (auto-escaped); no `{@html}`. |
</threat_model>

<verification>
- `pnpm -C web build` passes; no `<style>`/hex in either file.
- `task web:build && task build && task check` pass.
- Manual UAT: login dialog and reveal/revoke flows work with keyboard/focus-trap in both themes.
</verification>

<success_criteria>
- LoginModal and RevealKeyModal migrated to shadcn Dialog/AlertDialog with tokens, preserved auth/reveal/revoke behavior, and the destructive revoke copy; app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-07-SUMMARY.md` when done.
</output>
