---
phase: 05-ui-project-selector-and-refinements
plan: 06
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/lib/components/SearchFilter.svelte
  - web/src/lib/components/MetadataBar.svelte
  - web/src/lib/components/ChangelogTimeline.svelte
autonomous: true
requirements: [D-12, D-13]
user_setup: []

must_haves:
  truths:
    - "SearchFilter, MetadataBar, ChangelogTimeline render on shadcn primitives + Slate tokens with no scoped navy/blue CSS."
    - "SearchFilter focus ring uses --ring; existing props/behavior preserved."
    - "ChangelogTimeline uses Separator + Badge; MetadataBar uses Card/description list + Badge."
  artifacts:
    - "web/src/lib/components/SearchFilter.svelte (shadcn Input)"
    - "web/src/lib/components/MetadataBar.svelte (Card/desc list + Badge)"
    - "web/src/lib/components/ChangelogTimeline.svelte (timeline + Separator + Badge)"
  key_links:
    - "SearchFilter focus ring → --ring token (UI-SPEC accent rule #3)"
---

<objective>
Migrate `SearchFilter`, `MetadataBar`, and `ChangelogTimeline` to shadcn primitives + Slate tokens, preserving props and Svelte 5 runes (D-12/D-13).

Purpose: These support the spec-detail and dashboard views; migrating them cleanly enables Wave 3 restyle without contract changes.
Output: Three token-based components.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-RESEARCH.md
@.planning/phases/05-ui-project-selector-and-refinements/05-PATTERNS.md
@.planning/phases/05-ui-project-selector-and-refinements/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SearchFilter to shadcn Input</name>
  <files>web/src/lib/components/SearchFilter.svelte</files>
  <read_first>
    - web/src/lib/components/SearchFilter.svelte (input L11-18, drop focus-ring hex L37-40 — PATTERNS L298)
    - web/src/lib/components/ui/input
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (SearchFilter → Input), "Color → Accent reserved list" (focus rings use --ring)
  </read_first>
  <action>
    Re-implement `SearchFilter.svelte` on shadcn `Input` for the search field (and `Select`/`DropdownMenu` for any filter controls if present). Preserve props/behavior and Svelte 5 runes. Focus ring uses `--ring` (UI-SPEC accent rule #3). Delete the scoped `<style>`; use token utility classes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/SearchFilter.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Ii]nput/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; uses shadcn `Input`; focus ring via `--ring`; props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>SearchFilter on shadcn Input with token focus ring.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate MetadataBar to Card/description list + Badge</name>
  <files>web/src/lib/components/MetadataBar.svelte</files>
  <read_first>
    - web/src/lib/components/MetadataBar.svelte (current markup/props)
    - web/src/lib/components/ui/card, badge
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (MetadataBar → Card/description list with Badges)
  </read_first>
  <action>
    Re-implement `MetadataBar.svelte` as a shadcn `Card` / description-list layout with `Badge`s for meta values. Preserve props/behavior and Svelte 5 runes. Delete the scoped `<style>`; use token utility classes (`text-muted-foreground` for labels). Any categorical values that map to the shared badge palette should use `badge-variants`; otherwise neutral `Badge variant="secondary"`.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/MetadataBar.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1);if(!/[Cc]ard|[Bb]adge/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; uses shadcn `Card`/`Badge`; props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>MetadataBar on shadcn Card with token styling.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate ChangelogTimeline to token timeline + Separator + Badge</name>
  <files>web/src/lib/components/ChangelogTimeline.svelte</files>
  <read_first>
    - web/src/lib/components/ChangelogTimeline.svelte (current markup/props)
    - web/src/lib/components/ui/separator, badge
    - 05-UI-SPEC.md "Component Inventory & Migration Map" (ChangelogTimeline → custom timeline + Separator + Badge)
  </read_first>
  <action>
    Re-implement `ChangelogTimeline.svelte` as a token-styled timeline using shadcn `Separator` between entries and `Badge`s for change-type/labels. Preserve props/behavior and Svelte 5 runes. Delete the scoped `<style>`; use token utility classes.
  </action>
  <verify>
    <automated>cd web && node -e "const t=require('fs').readFileSync('src/lib/components/ChangelogTimeline.svelte','utf8');if(/<style/.test(t))process.exit(1);if(/#[0-9a-fA-F]{6}/.test(t))process.exit(1)" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `<style>`, no 6-digit hex; uses shadcn `Separator`/`Badge`; props preserved.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>ChangelogTimeline on token styling with Separator + Badge.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (this plan)

Modified files:
- `web/src/lib/components/SearchFilter.svelte` — shadcn Input
- `web/src/lib/components/MetadataBar.svelte` — Card/description list + Badge
- `web/src/lib/components/ChangelogTimeline.svelte` — timeline + Separator + Badge
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server metadata/changelog → rendered component | Meta values and changelog text rendered here |
| user search input → filter state | Free-text search string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-XSS-06 | Tampering/Info-disclosure | metadata/changelog text rendered | high | mitigate | Svelte `{text}` bindings (auto-escaped); no `{@html}` of server data. |
| T-05-09 | Input Validation | search free-text | low | accept | Search string used only for client-side filtering; never echoed into `innerHTML`. |
</threat_model>

<verification>
- `pnpm -C web build` passes; no `<style>`/hex in the three files.
- `task web:build && task build && task check` pass.
- Manual UAT: search input, metadata, and timeline render correctly in both themes.
</verification>

<success_criteria>
- SearchFilter/MetadataBar/ChangelogTimeline migrated to shadcn with tokens and preserved contracts; app builds.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-06-SUMMARY.md` when done.
</output>
