---
phase: 05-ui-project-selector-and-refinements
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/spec/[...slug]/+page.svelte
  - web/src/routes/decision/[...slug]/+page.svelte
autonomous: true
gap_closure: true
requirements: [D-12, D-13, D-14]
user_setup: []

must_haves:
  truths:
    - "In dark mode, the Spec detail page body content (h1/h2/h3/h4 headings, body text, meta labels, tables, blockquotes, banners) renders with readable contrast — no near-invisible navy-on-dark text (D-14)."
    - "In dark mode, the Decision detail page body content (heading, meta labels, section headings, body text, spec chips) renders with readable contrast (D-14)."
    - "Both detail pages' body content uses the same shadcn theme-token utilities the other four migrated pages use, so light and dark modes both work from one markup source (D-12/D-13)."
  artifacts:
    - "web/src/routes/spec/[...slug]/+page.svelte — body content on shadcn token utilities, old-palette hex removed."
    - "web/src/routes/decision/[...slug]/+page.svelte — body content on shadcn token utilities, old-palette hex removed."
  key_links:
    - "text-foreground / text-muted-foreground / bg-muted / bg-card / border-border utilities resolve against the shadcn Slate theme CSS variables, which flip under the `.dark` class set by mode-watcher (already wired in +layout.svelte) — so body content follows the theme toggle."
    - "Categorical/semantic accents (slice-status chips, lifecycle banners, conversation roles) use explicit light/dark Tailwind pairs (the badge-variants.ts convention) so they stay readable in both themes without hardcoded light-only hex."
---

<objective>
Close the single verified gap from 05-VERIFICATION.md (truth #3, D-12/D-13/D-14): the Spec detail
(`/spec/[slug]`) and Decision detail (`/decision/[slug]`) pages still carry pre-migration plain-CSS
`<style>` blocks built on the OLD navy `#1a1a2e` / blue `#2563eb` light-only palette with no `.dark`
override, so their body content (headings, body text, labels, tables, chips, banners) renders
near-invisible in dark mode. All other pages (dashboard, graph, keys, constitution) and both page
shells (Card/Badge/Skeleton/Retry) are already fully migrated to shadcn theme tokens; this pass makes
these two pages match.

Purpose: Restore the D-14 dark-mode deliverable on the two remaining project-scoped views and honor
D-13's replacement of the old plain-CSS palette with shadcn theme tokens.
Output: Two `.svelte` files whose body content is styled with Tailwind utility classes on shadcn
tokens (mirroring the already-migrated constitution page), with the old light-only palette removed.

This is a self-contained, mechanical presentation-only migration. It does NOT change data flow,
routing, `load()` functions, the project-selector architecture, or component logic — all of which are
fully verified. Only the visual `<style>`-block styling is converted to token utilities on the markup.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-project-selector-and-refinements/05-VERIFICATION.md
@.planning/phases/05-ui-project-selector-and-refinements/05-CONTEXT.md

# Canonical migration reference — a page already fully on shadcn tokens (zero <style> block):
@web/src/routes/constitution/+page.svelte
# The categorical/semantic light+dark class convention (the ONE color carve-out):
@web/src/lib/components/badge-variants.ts
</context>

<artifacts_this_phase_produces>
This gap-closure pass MODIFIES (does not create) two files:
- `web/src/routes/spec/[...slug]/+page.svelte` — body-content styling moved from the plain-CSS
  `<style>` block (L417-640) to Tailwind token utilities on the markup; old-palette hex removed.
- `web/src/routes/decision/[...slug]/+page.svelte` — body-content styling moved from the plain-CSS
  `<style>` block (L102-165) to Tailwind token utilities on the markup; old-palette hex removed.

No new symbols, components, or files. `badge-variants.ts` helpers (`stageBadgeClass`,
`statusBadgeClass`) are already imported and remain unchanged.
</artifacts_this_phase_produces>

<tasks>

<!-- Allowlist: these old-palette hex literals are named in <action>/acceptance so the
     executor knows exactly what to delete. They are being REMOVED from the target files,
     so they will not survive in the written output. Markers keep validate_plan's
     comment-text-discipline gate green. -->
<!-- planner-discipline-allow: #1a1a2e -->
<!-- planner-discipline-allow: #2563eb -->
<!-- planner-discipline-allow: #374151 -->
<!-- planner-discipline-allow: #475569 -->
<!-- planner-discipline-allow: #64748b -->
<!-- planner-discipline-allow: #f8fafc -->
<!-- planner-discipline-allow: #eff6ff -->
<!-- planner-discipline-allow: #dbeafe -->
<!-- planner-discipline-allow: #fef3c7 -->

<task type="auto">
  <name>Task 1: Migrate Spec detail page body content off the old plain-CSS palette to shadcn tokens</name>
  <files>web/src/routes/spec/[...slug]/+page.svelte</files>
  <read_first>
    - web/src/routes/spec/[...slug]/+page.svelte (the file being modified — the `<style>` block spans L417-640 and every selector it styles: h1, .meta/.label, .notes, blockquote, h3, h4, ul/li, .approach/.chosen, .chosen-badge, .tradeoffs, pre, .detail-table, code, .slice-card/.slice-label, .conversation-log/.exchange/.role, .lifecycle-banner and its variants, .load-changelog-btn, .decision-marker)
    - web/src/routes/constitution/+page.svelte (canonical reference — a fully-migrated page with ZERO `<style>` block; copy its token idioms: `text-foreground` headings, `text-muted-foreground` labels/secondary text, `bg-muted`/`border-border` table cells + headers, `bg-muted/50` + `border-l-2 border-primary` callout blocks, `border-collapse text-sm` tables)
    - web/src/lib/components/badge-variants.ts (the light/dark categorical/semantic class-pair convention for the small set of accents that must NOT collapse to a single theme token)
  </read_first>
  <action>
    Convert the body-content styling in `spec/[...slug]/+page.svelte` from its plain-CSS `<style>`
    block to Tailwind utility classes applied directly on the markup, mirroring the constitution page.
    Map each old selector to shadcn theme tokens so it responds to light/dark mode:

    - Headings (`h1`, `h3`, `h4`): `text-foreground` with the existing size/weight utilities
      (e.g. h1 → `text-xl font-semibold text-foreground mb-4`; h3/h4 → `text-sm font-semibold
      text-foreground`). Remove the old navy heading color.
    - Meta labels (`.meta .label`) and other secondary/muted text (`.tradeoffs`, `.slice-label`,
      blockquote text, table header text): `text-muted-foreground`.
    - Body prose (`.notes`): `text-foreground` (or leave default) with `whitespace-pre-wrap
      text-sm leading-relaxed`; remove the old slate body color.
    - Soft-surface blocks (`.approach`, `pre`, `.slice-card`, `code`, `.detail-table th`):
      `bg-muted` for the fill and `border-border` for borders/dividers; table cell dividers →
      `border-b border-border`. Convert `.approach.chosen` and its accent border to a theme accent
      such as `border-l-2 border-primary bg-muted/50` (do NOT reintroduce the old blue).
    - blockquote: `border-l-2 border-border pl-3 text-sm text-muted-foreground`.
    - The changelog trigger (`.load-changelog-btn`): replace with the shadcn `Button`
      (`variant="outline"` or `size="sm"`) already imported, or `bg-primary text-primary-foreground`
      utilities — drop the `var(--accent-color, ...)` fallback.
    - Lifecycle banners (`.superseded-banner`, `.supersedes-banner`), conversation roles
      (`.role.probe`, `.role.response`), and `.decision-marker` are SEMANTIC/CATEGORICAL accents:
      give each an explicit light/dark Tailwind class pair (the `badge-variants.ts` convention,
      e.g. `bg-amber-100 text-amber-900 dark:bg-amber-950 dark:text-amber-200`) so they read in
      both themes — never a single light-only hex.

    DELETE the old light-only palette hex entirely: `#1a1a2e`, `#2563eb`, `#374151`, `#475569`,
    `#64748b`, `#f8fafc`, `#eff6ff`, `#dbeafe`, `#fef3c7` (plus their neighbors `#e2e8f0`,
    `#f1f5f9`, `#f59e0b`, `#92400e`, `#3b82f6`, `#1e40af`, `#6366f1`, `#7c3aed`, `#059669`,
    `#b45309`). After conversion, remove the now-empty (or reduced) `<style>` block; keep only rules
    that cannot be expressed as utilities, if any.

    OUT OF SCOPE — DO NOT TOUCH: the slice-status chip categorical colors produced by
    `sliceStatusBadge()` (returns `#4b5563` / `#c2410c` / `#15803d`) and applied inline via
    `style="background:{badge.color}"` on `.slice-badge`. These are legitimate palette-neutral
    categorical data-viz colors (verification explicitly excludes them). Leave that function and the
    inline chip binding unchanged. Do not introduce any `{@html}`/`innerHTML`/`eval` — keep the page
    XSS-clean (it currently has none).
  </action>
  <acceptance_criteria>
    - Source: `web/src/routes/spec/[...slug]/+page.svelte` contains no `#1a1a2e` and no `#2563eb`
      (verify: `grep -c '#1a1a2e\|#2563eb' web/src/routes/spec/\[...slug\]/+page.svelte` returns 0).
    - Source: the old light-only palette hexes are gone —
      `grep -Eoc '#1a1a2e|#2563eb|#374151|#475569|#64748b|#f8fafc|#eff6ff|#dbeafe|#fef3c7'` on the
      file returns 0.
    - Source: body markup uses shadcn token utilities — the file contains at least one each of
      `text-foreground`, `text-muted-foreground`, `bg-muted`, and `border-border`.
    - Source: the slice-status chip categorical colors are preserved — `sliceStatusBadge` still
      returns its `#15803d`/`#c2410c`/`#4b5563` values and `.slice-badge` still binds
      `style="background:{badge.color}"` (untouched).
    - Source: no `{@html}`, `innerHTML`, or `eval` present (XSS-clean unchanged).
    - Behavior: `pnpm build` (from `web/`) exits 0.
    - Behavior: `pnpm test` (from `web/`) still passes (20/20).
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -c '#1a1a2e\|#2563eb' 'src/routes/spec/[...slug]/+page.svelte' | grep -qx 0 && pnpm build && pnpm test</automated>
    <human-check>Toggle dark mode and open a Spec detail page: h1, section headings, meta labels, tables, blockquotes, approaches, and lifecycle banners are all readable with proper contrast in BOTH light and dark mode.</human-check>
  </verify>
  <done>
    Spec detail body content is styled with shadcn token utilities, the old navy/blue light-only
    palette hex is gone, the slice-status chips remain their categorical colors, and `pnpm build` +
    `pnpm test` pass. Dark-mode contrast to be human-confirmed (see human_verification).
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Decision detail page body content off the old plain-CSS palette to shadcn tokens</name>
  <files>web/src/routes/decision/[...slug]/+page.svelte</files>
  <read_first>
    - web/src/routes/decision/[...slug]/+page.svelte (the file being modified — the `<style>` block spans L102-165: h1, .meta/.label, .section, h2, .body-text, .spec-chips, .spec-chip and :hover)
    - web/src/routes/constitution/+page.svelte (canonical fully-migrated reference — copy the same `text-foreground` / `text-muted-foreground` / `bg-muted` / `border-border` token idioms)
    - web/src/lib/components/badge-variants.ts (light/dark class-pair convention for the spec-chip accent)
  </read_first>
  <action>
    Convert `decision/[...slug]/+page.svelte` body styling from its plain-CSS `<style>` block to
    Tailwind utility classes on the markup, mirroring the constitution page:

    - Headings (`h1`, `h2`): `text-foreground` with the existing size/weight utilities
      (h1 → `text-xl font-semibold text-foreground mb-4`; h2 → `text-base font-semibold
      text-foreground mb-2`). Remove the old navy heading color.
    - Meta labels (`.meta .label`): `text-muted-foreground font-medium whitespace-nowrap min-w-32`.
    - Body prose (`.body-text`): `text-foreground whitespace-pre-wrap text-sm leading-relaxed`
      (remove the old slate color).
    - `.meta` table: `border-collapse text-sm` with cell padding utilities as today.
    - Spec chips (`.spec-chip` / `.spec-chip:hover`): this is a SEMANTIC link chip — give it an
      explicit light/dark Tailwind pair (badge-variants convention) so it reads in both themes, e.g.
      `bg-blue-100 text-blue-800 hover:bg-blue-200 dark:bg-blue-950 dark:text-blue-300
      dark:hover:bg-blue-900` (or use the shadcn `Badge`/`bg-muted text-foreground` tokens). Do NOT
      reintroduce the old light-only blue hex.

    DELETE the old light-only palette hex entirely from this file: `#1a1a2e`, `#2563eb`, `#374151`,
    `#64748b`, `#eff6ff`, `#dbeafe`. Remove the now-empty `<style>` block. Do not introduce any
    `{@html}`/`innerHTML`/`eval` — keep the page XSS-clean.

    OUT OF SCOPE: the decision-status Badge already uses `statusBadgeClass()` from `badge-variants.ts`
    (categorical, dark-aware) — leave that import and usage unchanged.
  </action>
  <acceptance_criteria>
    - Source: `web/src/routes/decision/[...slug]/+page.svelte` contains no `#1a1a2e` and no
      `#2563eb` (verify: `grep -c '#1a1a2e\|#2563eb'` on the file returns 0).
    - Source: the old light-only palette hexes are gone —
      `grep -Eoc '#1a1a2e|#2563eb|#374151|#64748b|#eff6ff|#dbeafe'` on the file returns 0.
    - Source: body markup uses shadcn token utilities — the file contains at least one each of
      `text-foreground`, `text-muted-foreground`.
    - Source: the decision-status Badge still calls `statusBadgeClass(...)` (unchanged).
    - Source: no `{@html}`, `innerHTML`, or `eval` present.
    - Behavior: `pnpm build` (from `web/`) exits 0.
    - Behavior: `pnpm test` (from `web/`) still passes (20/20).
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -c '#1a1a2e\|#2563eb' 'src/routes/decision/[...slug]/+page.svelte' | grep -qx 0 && pnpm build && pnpm test</automated>
    <human-check>Toggle dark mode and open a Decision detail page: title, section headings, meta labels, body text, and spec chips are all readable with proper contrast in BOTH light and dark mode.</human-check>
  </verify>
  <done>
    Decision detail body content is styled with shadcn token utilities, the old navy/blue light-only
    palette hex is gone, the status Badge remains categorical, and `pnpm build` + `pnpm test` pass.
    Dark-mode contrast to be human-confirmed (see human_verification).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change is a presentation-only CSS/color-token migration on static Svelte markup. It adds no new inputs, no data flow, no network surface, no auth/authz path, and no dynamic HTML rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14-XSS | Tampering (XSS) | spec/decision detail markup | low | accept | Conversion touches only class attributes and `<style>` removal. No `{@html}`/`innerHTML`/`eval` introduced (both pages are currently XSS-clean; acceptance criteria re-assert their absence). No new attack surface at or above `high` — no L1-relevant threats. |
</threat_model>

<verification>
- `cd web && grep -c '#1a1a2e\|#2563eb' 'src/routes/spec/[...slug]/+page.svelte'` → 0
- `cd web && grep -c '#1a1a2e\|#2563eb' 'src/routes/decision/[...slug]/+page.svelte'` → 0
- Both files contain `text-foreground`, `text-muted-foreground`, `bg-muted`/`border-border` token
  utilities in body markup.
- `cd web && pnpm build` exits 0 (adapter-static writes `build/`).
- `cd web && pnpm test` passes 20/20.
- No `{@html}`/`innerHTML`/`eval` anywhere in either file.
- (Human/UAT) Dark-mode toggle renders both detail pages' body content with proper contrast —
  carried forward as a human_verification note; cannot be asserted in code.
</verification>

<success_criteria>
The gap in 05-VERIFICATION.md (truth #3, D-12/D-13/D-14) is closed: the Spec detail and Decision
detail pages' body content is migrated off the old light-only navy/blue plain-CSS palette onto shadcn
theme-token utilities that respond to light/dark mode, matching the four already-migrated pages. The
old `#1a1a2e`/`#2563eb` (and neighboring light-only) hex is removed, the categorical slice-status
chips and decision-status badge are preserved, and `pnpm build` + `pnpm test` still pass. Dark-mode
readability confirmed by human UAT.
</success_criteria>

<output>
Create `.planning/phases/05-ui-project-selector-and-refinements/05-14-SUMMARY.md` when done.
</output>
