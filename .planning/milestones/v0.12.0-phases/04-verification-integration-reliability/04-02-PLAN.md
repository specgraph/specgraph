---
phase: 04-verification-integration-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/docs/concepts/drift.md
autonomous: true
requirements: [DRFT-01]
must_haves:
  truths:
    - "Drift status for any spec (or the full graph) is documented as queryable across all three surfaces — CLI, API (LifecycleService.CheckDrift / AcknowledgeDrift), and MCP (the `drift` tool) — satisfying SC#1's CLI/API/MCP naming (D-04)"
  artifacts:
    - "site/docs/concepts/drift.md — new `## Accessing Drift via API / MCP` section"
  key_links:
    - "Doc note names the same server-side content-hash detection reachable identically from CLI, API, and MCP"
    - "Existing `!!! info \"Planned\"` note about the `interfaces`/`verify` stub scopes stays intact (D-03)"
---

<objective>
Close the SC#1 documentation gap for DRFT-01: the drift concept doc is currently
CLI-centric, but SC#1 names "CLI/API/MCP." Add a short, accurate note that the same
drift check is reachable via `LifecycleService.CheckDrift`/`AcknowledgeDrift` (API) and
the MCP `drift` tool. Confirm-and-lightly-extend only — no large rewrite (D-04).

Purpose: Make the stable drift interface discoverable across all three surfaces without
changing any behavior, proto, or CLI reference generation.
Output: One new `##` section in `site/docs/concepts/drift.md`.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-verification-integration-reliability/04-CONTEXT.md
@.planning/phases/04-verification-integration-reliability/04-RESEARCH.md
@.planning/phases/04-verification-integration-reliability/04-PATTERNS.md

# Analog + ground-truth sources:
@site/docs/concepts/drift.md
@internal/server/lifecycle_handler.go
@internal/mcp/tools_lifecycle.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Accessing Drift via API / MCP" section to drift.md</name>
  <files>site/docs/concepts/drift.md</files>
  <read_first>
    - site/docs/concepts/drift.md lines 54-90 — the existing `## CLI Usage` section (heading level, fenced-block and `!!! info "Planned"` admonition idioms to mirror) and where `## Worked Example` (line 91) begins.
    - internal/server/lifecycle_handler.go — `CheckDrift` (single spec via `slug`, or all eligible specs when `slug` is empty) and `AcknowledgeDrift` (`upstream_slug` for one edge, `all: true` to re-baseline every drifted edge) — so the note is accurate.
    - internal/mcp/tools_lifecycle.go — the MCP `drift` tool exposing `check` and `acknowledge` actions.
    - 04-PATTERNS.md "add API / MCP access note" section (the recommended wording and placement).
  </read_first>
  <action>
    Insert a new sibling `## Accessing Drift via API / MCP` section into site/docs/concepts/drift.md, placed AFTER the `## CLI Usage` section and BEFORE `## Worked Example`. Keep it short (a few sentences + two bullets). State that the same drift check backing the CLI is reachable directly via: (1) API (ConnectRPC) — `LifecycleService.CheckDrift` (single spec via `slug`, or all eligible specs when `slug` is empty) and `LifecycleService.AcknowledgeDrift` (`upstream_slug` for one edge, or `all: true` to re-baseline every drifted edge); (2) MCP — the `drift` tool exposing the same operations via `check` and `acknowledge` actions. Close with a sentence that all three surfaces (CLI, API, MCP) call the identical server-side detection — content-hash comparison on `DEPENDS_ON` edges — so results are consistent regardless of entry point. Do NOT modify or remove the existing `!!! info "Planned"` note about the `--scope interfaces|verify` stub scopes (D-03). No `cli-reference.md` regeneration. Match the surrounding Markdown/MkDocs style (heading level `##`, admonition idiom if used). This is doc-only — no proto, no code, no scope-table change.
  </action>
  <verify>
    <automated>rg -q "^## Accessing Drift via API / MCP" site/docs/concepts/drift.md && rg -q "LifecycleService.CheckDrift" site/docs/concepts/drift.md && rg -q 'drift.*tool' site/docs/concepts/drift.md && rg -q 'info "Planned"' site/docs/concepts/drift.md</automated>
  </verify>
  <acceptance_criteria>
    - A new `## Accessing Drift via API / MCP` section exists in drift.md, positioned between `## CLI Usage` and `## Worked Example`.
    - The section names `LifecycleService.CheckDrift`, `LifecycleService.AcknowledgeDrift`, and the MCP `drift` tool with its `check`/`acknowledge` actions.
    - It states all three surfaces call the identical content-hash detection on `DEPENDS_ON` edges.
    - The existing `!!! info "Planned"` note about the `interfaces`/`verify` stub scopes is unchanged.
    - `task lint` (markdown lint) passes.
  </acceptance_criteria>
  <done>A `## Accessing Drift via API / MCP` section exists between `## CLI Usage` and `## Worked Example`, naming the API RPCs and MCP `drift` tool, with the `interfaces`/`verify` "Planned" note left intact and `task lint` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→reader | Documentation only; no runtime trust boundary crossed |

## STRIDE Threat Register

Documentation-only change over an existing feature — NO new attack surface, NO new
security controls, NO runtime behavior change.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-D1 | Information disclosure | drift.md content | low | accept | Note describes only already-public API/MCP surfaces; no secrets, endpoints, or credentials disclosed |
| T-04-SC | Tampering | npm/pip/cargo/go get installs | n/a | accept | No package installs (docs-only) |

No high-severity threats introduced.
</threat_model>

<verification>
- `rg -q "^## Accessing Drift via API / MCP" site/docs/concepts/drift.md` confirms the section exists.
- `task lint` passes (markdown lint over the edited doc).
- No code, proto, scope-table, or CLI-reference changes.
</verification>

<success_criteria>
- SC#1 documentation gap closed: drift interface is documented as queryable across CLI, API, and MCP.
- The `interfaces`/`verify` "Planned" stub note remains intact (D-03).
</success_criteria>

<artifacts_this_phase_produces>
Doc subsection anchor (no new source symbols):
- `site/docs/concepts/drift.md#accessing-drift-via-api--mcp` — new `## Accessing Drift via API / MCP` section.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-verification-integration-reliability/04-02-SUMMARY.md` when done
</output>
