---
phase: 02-api-key-lifecycle-self-service
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/auth/rolemin.go
  - internal/auth/rolemin_test.go
  - internal/auth/engine.go
  - internal/auth/policies/base.cedar
  - internal/auth/actions.go
  - internal/auth/actions_test.go
autonomous: true
requirements: [AUTH-03, AUTH-02]

must_haves:
  truths:
    - "auth.RoleMin returns the less-privileged of two roles and is fail-closed to RoleReader when either is unranked"
    - "The Cedar engine boots with a new apikey.self verb: \"self\" is in knownVerbs AND base.cedar permits apikey.self for any authenticated role"
    - "The four self procedures map to apikey.self and ResyncUserRole maps to user.manage; a drift test restricts the self verb to apikey.* actions"
  artifacts:
    - internal/auth/rolemin.go
    - internal/auth/actions.go
    - internal/auth/policies/base.cedar
  key_links:
    - "auth.RoleMin consumed by the self-mint handler's floor (Plan 05)"
    - "knownVerbs + base.cedar + actions.go must land together or NewCedarEngine fails → server won't boot"
---

<objective>
Land the auth-layer foundation for the new `apikey.self` authorization verb and the exported role floor, in the single commit the boot invariant requires. Adds: exported `auth.RoleMin` (fail-closed) next to the existing unexported ordering family; `"self"` in `knownVerbs`; the `apikey.self` permit in `base.cedar`; the five procedure→action map entries (four self → `apikey.self`, ResyncUserRole → `user.manage`); and the two hard-coded `actions_test.go` list updates plus the mandated `self`-only-on-`apikey.*` drift test.

Purpose: Provide the role-floor primitive (SC#2) and the authorization verb without breaking engine boot (Pitfall 1 & 2).
Output: `rolemin.go` + test; engine/cedar/actions changes with mirror + drift tests.
</objective>

<execution_context>
@~/.config/opencode/gsd-core/workflows/execute-plan.md
@~/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-key-lifecycle-self-service/02-CONTEXT.md
@.planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md
@.planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md
@docs/superpowers/specs/2026-06-16-spgr-g7st-self-service-api-keys-design.md
@docs/superpowers/specs/2026-06-04-spgr-rjrt-9-role-downgrade-failclosed-design.md
@internal/auth/actions.go
@internal/auth/actions_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exported auth.RoleMin (fail-closed) + table test</name>
  <files>internal/auth/rolemin.go, internal/auth/rolemin_test.go</files>
  <read_first>
    - internal/auth/identitystore.go (roleRank at :256, roleLessThan at :270, clampedRole at :289 — the unexported ordering family to reuse; do NOT introduce a second comparator per RESEARCH "Don't Hand-Roll")
    - docs/superpowers/specs/2026-06-04-spgr-rjrt-9-role-downgrade-failclosed-design.md (the fail-closed-to-reader guarantee RoleMin must mirror)
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md (section "internal/auth/rolemin.go — NEW"; Pattern 3)
  </read_first>
  <behavior>
    - RoleMin(admin, reader) == reader; RoleMin(writer, admin) == writer; RoleMin(reader, reader) == reader
    - RoleMin with any unranked/empty role on either side == RoleReader (fail-closed)
  </behavior>
  <action>
    Create `internal/auth/rolemin.go` (SPDX header, package doc comment already present in package) with exported `func RoleMin(a, b Role) Role` reusing the existing `roleRank` map: look up both ranks; if either is not found return `RoleReader` (fail-closed, per spgr-rjrt.9); otherwise return the role with the lower rank. Do NOT duplicate the ordering table — reference the existing `roleRank`. Create `internal/auth/rolemin_test.go` with a `testify/require` table test (`TestRoleMin`) covering the ranked min cases in both argument orders and the fail-closed cases (empty string, unknown role) resolving to RoleReader.
  </action>
  <verify>
    <automated>go test ./internal/auth/ -run TestRoleMin</automated>
  </verify>
  <acceptance_criteria>
    - internal/auth/rolemin.go declares exported `RoleMin(a, b Role) Role` reusing roleRank
    - `go test ./internal/auth/ -run TestRoleMin` exits 0
    - Table test asserts unranked/empty input on either side yields RoleReader
  </acceptance_criteria>
  <done>Exported fail-closed RoleMin exists and is proven by a table test covering ranked and unranked cases.</done>
</task>

<task type="auto">
  <name>Task 2: Register apikey.self verb (knownVerbs + base.cedar)</name>
  <files>internal/auth/engine.go, internal/auth/policies/base.cedar</files>
  <read_first>
    - internal/auth/engine.go (knownVerbs at :155; principalEntity role/id/email exposure at :212 — Cedar cannot see Source or the role floor)
    - internal/auth/policies/base.cedar (the "manage" verb permit at :42-48 to model the new permit on)
    - .planning/phases/02-api-key-lifecycle-self-service/02-RESEARCH.md (Pitfall 2: missing "self" in knownVerbs → NewCedarEngine fails → server won't boot; land in same commit as policy)
  </read_first>
  <action>
    In `internal/auth/engine.go`, add the `self` verb to the `knownVerbs` map so `actionVerb` parses `apikey.self` (mirror the existing map entries; add the key with value true). In `internal/auth/policies/base.cedar`, add a permit for `SpecGraph::Action::"apikey.self"` that admits ANY authenticated principal (do not gate on `principal.role == "admin"`), with a comment noting the handler further restricts it (rejects `Source == "apikey"`, floors role via RoleMin) because `principalEntity` exposes only role/id/email. Keep the existing `manage`/read/write/delete permits unchanged. These two edits + Task 3's actions.go entry MUST be in the same commit/plan or engine construction fails at boot.
  </action>
  <verify>
    <automated>go test ./internal/auth/ -run 'TestNewCedarEngine|TestEngine' && grep -c 'apikey.self' internal/auth/policies/base.cedar</automated>
  </verify>
  <acceptance_criteria>
    - internal/auth/engine.go knownVerbs contains the self verb (grep -v '^//' internal/auth/engine.go | grep -c '"self"' >= 1)
    - internal/auth/policies/base.cedar contains an apikey.self permit admitting any authenticated role
    - The Cedar engine constructs successfully (engine construction test exits 0 — server would boot)
  </acceptance_criteria>
  <done>The apikey.self verb is registered in both knownVerbs and the Cedar policy; the engine boots.</done>
</task>

<task type="auto">
  <name>Task 3: Procedure→action map + mirror/drift tests</name>
  <files>internal/auth/actions.go, internal/auth/actions_test.go</files>
  <read_first>
    - internal/auth/actions.go (procedureActions Identity block at :92-106 — the mapping style to mirror; references generated procedure constants from Plan 01)
    - internal/auth/actions_test.go (TestActionNames_AllParseToKnownVerb hard-coded verb list at :40; TestActionForProcedure_Identity mirror map at :50-71 — BOTH must be updated per RESEARCH Pitfall 1)
    - .planning/phases/02-api-key-lifecycle-self-service/02-PATTERNS.md (section "internal/auth/actions.go + actions_test.go — MODIFY")
  </read_first>
  <action>
    In `internal/auth/actions.go`, add five entries to `procedureActions`: `IdentityServiceCreateMyAPIKeyProcedure`, `IdentityServiceListMyAPIKeysProcedure`, `IdentityServiceRotateMyAPIKeyProcedure`, `IdentityServiceRevokeMyAPIKeyProcedure` → `"apikey.self"` (AUTH-03, per D-06); `IdentityServiceResyncUserRoleProcedure` → `"user.manage"` (AUTH-02 admin-only, per D-04). In `internal/auth/actions_test.go`: (a) add the self verb string to the hard-coded assertion slice in `TestActionNames_AllParseToKnownVerb` (the second list RESEARCH Pitfall 1 flagged); (b) add the five new procedures to the `TestActionForProcedure_Identity` mirror map; (c) add a new drift test asserting that the self verb appears ONLY on actions whose resource prefix is apikey (iterate procedureActions; any action ending in the self verb must have the apikey resource prefix). Use identifiers/constants, not literal magic strings where the codebase exposes constants.
  </action>
  <verify>
    <automated>go test ./internal/auth/ -run 'TestActionNames_AllParseToKnownVerb|TestActionForProcedure_Identity|TestActionName'</automated>
  </verify>
  <acceptance_criteria>
    - actions.go maps the four self procedures to apikey.self and ResyncUserRole to user.manage
    - Both previously hard-coded lists in actions_test.go are updated (verb list + identity mirror)
    - A drift test asserts the self verb is confined to apikey.* actions and passes
    - `go test ./internal/auth/...` exits 0
  </acceptance_criteria>
  <done>All five procedures are authorized; the two mirror lists and the self-only-on-apikey drift test pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cedar policy → handler | Cedar permits any authed role for apikey.self; the handler enforces source/floor Cedar cannot see |
| verb registry → engine boot | knownVerbs/policy/actions must agree or the engine refuses to construct |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | Role-cap laundering primitive | high | mitigate | Exported fail-closed `RoleMin`; unranked → RoleReader; consumed by the create/rotate floor in Plan 05 |
| T-02-12 | Denial of Service (availability) | Missing self in knownVerbs → engine boot failure | high | mitigate | knownVerbs + base.cedar + actions.go landed in one plan; engine-construction test gates it |
| T-02-13 | Elevation of Privilege | self verb leaking onto non-apikey actions | high | mitigate | Drift test restricts the self verb to apikey.* actions; ResyncUserRole mapped to admin-only user.manage |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Phase-wide inventory (this plan delivers the **bolded** items):

- Proto/RPCs: CreateMyAPIKey, ListMyAPIKeys, RotateMyAPIKey, RevokeMyAPIKey, ResyncUserRole + messages
- Auth: **exported `auth.RoleMin(a, b Role) Role`; Cedar verb `apikey.self` (+ `"self"` in `knownVerbs`); `apikey.self` permit in base.cedar; procedure→action map entries (four self → apikey.self, ResyncUserRole → user.manage); `self`-only-on-`apikey.*` drift test**
- Storage: `GetAPIKeyForUser`, `RevokeAPIKeyForUser`, `RotateAPIKeyForUser`, quota-safe `CreateAPIKeyForUser`; sentinel `ErrQuotaExceeded`
- Server: four self handlers; `ResyncUserRole` handler; `selfMintLimiter`; `clampExpiry`; CSRF double-submit middleware
- Config: self-service key-policy struct (default 90d / max 180d / quota 10 / rate-limit)
- CLI: `auth api-key create/list/rotate/revoke` self-variants; session-preferring resolver (Finding D); `auth user resync <user-id> --role <r> [--revoke-keys]`
- Web: `web/src/routes/keys/+page.svelte`, `web/src/lib/components/RevealKeyModal.svelte`, `web/src/lib/keys.svelte.ts`, `identityClient` + CSRF interceptor
</artifacts_produced>

<verification>
- `go test ./internal/auth/...` green (RoleMin table, engine boot, mirror lists, self-only drift test).
- knownVerbs, base.cedar, and actions.go all reference the self verb (single-commit boot invariant satisfied).
</verification>

<success_criteria>
The apikey.self authorization verb boots cleanly, RoleMin provides the fail-closed floor primitive, and ResyncUserRole is admin-gated — the auth foundation for Plans 05 and 06.
</success_criteria>

<output>
Create `.planning/phases/02-api-key-lifecycle-self-service/02-04-SUMMARY.md` when done.
</output>
