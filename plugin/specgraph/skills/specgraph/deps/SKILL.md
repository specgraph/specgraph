---
name: specgraph-deps
description: >
  Show dependency tree for a spec. Use when the user asks "what does this
  depend on?", "what blocks this?", "dependency tree", or "deps".
---

# SpecGraph Deps

````bash
specgraph deps <slug>
````

Present the dependency tree. Highlight blocked specs and circular dependencies if any.
