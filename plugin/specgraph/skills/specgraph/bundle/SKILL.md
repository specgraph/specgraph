---
name: specgraph-bundle
description: >
  Generate execution bundle and prime context for a spec. Use when the user
  wants to start implementing, says "generate bundle", "prime for",
  "execution context", or "bundle".
---

# SpecGraph Bundle

````bash
specgraph bundle <slug>
````

Present the bundle contents. Offer to inject context:

````bash
specgraph inject <slug> --tool=claude-code
````
