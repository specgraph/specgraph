# Release Single-Job GoReleaser Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the broken two-job git-cliff + GoReleaser release pipeline with a single-job model where cog derives the version + local tag and GoReleaser solely owns the GitHub Release and its notes, fixing empty release notes that have shipped on every release since v0.3.7.

**Architecture:** One `goreleaser` workflow job: cog computes the next semver and creates a local `v*` tag; GoReleaser then creates the remote tag (via `target_commitish`), generates native changelog notes, builds/signs all artifacts, and publishes a single atomic release. git-cliff and the committed CHANGELOG.md are retired.

**Tech Stack:** GitHub Actions, cocogitto (cog 7.x), GoReleaser v2, cosign, syft, Trivy, Taskfile.

**Design:** `docs/superpowers/specs/2026-06-05-release-single-job-goreleaser-design.md` (Bead: spgr-7r6g)

---

## File Structure

| File | Responsibility | Action |
|------|----------------|--------|
| `cog.toml` | cog as release engine (tag-only) + commit validation | Rewrite |
| `.goreleaser.yaml` | GoReleaser owns release + native changelog | Modify (`release:`, `changelog:`) |
| `.github/workflows/release.yml` | Single-job release pipeline | Rewrite |
| `cliff.toml` | git-cliff config | Delete |
| `CHANGELOG.md` | Frozen historical changelog | Add freeze header |
| `Taskfile.yml` | `release:cut` convenience target | Modify |

There is no automated test suite for release infrastructure; verification is via
`goreleaser check`, `cog bump --auto --dry-run`, `actionlint`, and a real
`workflow_dispatch` after merge. Each task below pairs concrete edits with the
exact verification command and expected output.

---

### Task 1: Rewrite `cog.toml` as the release engine

**Files:**

- Modify: `cog.toml`

- [ ] **Step 1: Replace the file contents**

Replace the entire `cog.toml` with:

```toml
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 Sean Brandt
#
# Cocogitto configuration — https://docs.cocogitto.io/
#
# cog is the release engine: it derives the next semver from conventional
# commits and creates a TAG ONLY (no bump commit, no in-repo CHANGELOG.md).
# GitHub Release notes are generated by GoReleaser (see .goreleaser.yaml).
# cog is also used for commit-message validation via the lefthook commit-msg
# hook (`cog verify`).
#
# cog 7.x note: these are top-level keys, NOT nested under a [settings] table
# (cog 7 rejects an unknown `settings` section). The commit-type allow-list is
# cog's built-in default (feat, fix, docs, style, refactor, perf, test, build,
# ci, chore, revert). Do NOT re-list defaults with empty tables (`feat = {}`):
# an empty entry DISABLES that type. Only add a [commit_types] entry with a
# NON-EMPTY table to introduce a new type.

# v-prefixed tags (v1.2.3) to match GoReleaser and the existing tag history.
tag_prefix = "v"

# Tag-only releases: no in-repo CHANGELOG.md (no commit to protected main).
disable_changelog = true

# Defense-in-depth: only allow a bump from main, so a stray dispatch from a
# feature branch cannot cut a release.
branch_whitelist = ["main"]

ignore_merge_commits = true
```

- [ ] **Step 2: Verify commit validation still works**

Run: `cog verify "feat(release): adopt single-job goreleaser model"`
Expected: prints the parsed commit (Type: feat) with no error.

- [ ] **Step 3: Verify a negative case is still rejected**

Run: `cog verify "not a conventional commit"`
Expected: non-zero exit with an "Error: missing commit type" style message.

- [ ] **Step 4: Verify version derivation is computable (dry-run, no tag created)**

Run: `cog bump --auto --dry-run`
Expected: prints a `v`-prefixed semver (e.g. `v0.8.0`) to stdout and exits 0.
If it errors with "Commit type ... not allowed", a default type was disabled —
re-check that no `[commit_types]` entry re-lists a default with `{}`.

- [ ] **Step 5: Commit**

```text
jj describe -m "ci(release): rewrite cog.toml as tag-only release engine

cog now derives version + local tag; drop [settings] wrapper and redundant
[commit_types] defaults per cog-7 semantics. disable_changelog + tag_prefix +
branch_whitelist mirror the holomush-proven config.

Refs: spgr-7r6g
Signed-off-by: Sean Brandt <claude2@sean.fzymgc.email>"
```

---

### Task 2: Point `.goreleaser.yaml` at GoReleaser-owned release + native notes

**Files:**

- Modify: `.goreleaser.yaml:80-83` (the `release:` and `changelog:` blocks)

- [ ] **Step 1: Replace the `release:` and `changelog:` blocks**

Replace the final two blocks (currently `release:\n  mode: append` and
`changelog:\n  disable: true`) with:

```yaml
release:
  github:
    owner: specgraph
    name: specgraph
  draft: false
  prerelease: auto
  # cog creates the v* tag LOCALLY (cog bump --disable-bump-commit) and does NOT
  # push it before GoReleaser runs. target_commitish tells the Releases API which
  # commit to create the tag from, so GoReleaser creates the remote tag as part
  # of publishing — atomically. Without this, `goreleaser release` against a
  # local-only tag fails at the API call.
  target_commitish: "{{ .Commit }}"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "^build:"
      - "Merge pull request"
      - "Merge branch"
```

- [ ] **Step 2: Verify the config parses**

Run: `goreleaser check`
Expected: `✓ config is valid` (no errors). Deprecation warnings, if any, are
acceptable but should be noted.

- [ ] **Step 3: Verify a snapshot build still works end-to-end (no publish)**

Run: `goreleaser build --snapshot --clean --single-target`
Expected: exits 0; a `specgraph` binary appears under `dist/`.

- [ ] **Step 4: Commit**

```text
jj describe -m "ci(release): GoReleaser owns the release + native changelog

Replace 'release.mode: append' + 'changelog.disable: true' with a
GoReleaser-owned release (target_commitish for atomic remote-tag creation from a
local tag) and native changelog generation with conventional-commit exclude
filters.

Refs: spgr-7r6g
Signed-off-by: Sean Brandt <claude2@sean.fzymgc.email>"
```

---

### Task 3: Rewrite `.github/workflows/release.yml` to a single job

**Files:**

- Modify: `.github/workflows/release.yml` (full rewrite)

- [ ] **Step 1: Replace the entire workflow**

Replace `.github/workflows/release.yml` with:

```yaml
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 Sean Brandt
name: Release
on:
  workflow_dispatch:
    inputs:
      expected_increment:
        description: "Guard: fail if cog computes a different bump (auto = no guard)"
        required: false
        default: "auto"
        type: choice
        options:
          - auto
          - major
          - minor
          - patch
permissions:
  contents: read
concurrency:
  group: release
  cancel-in-progress: false
jobs:
  goreleaser:
    name: Build, Sign, and Release
    runs-on: namespace-profile-linux-amd64-4x8
    permissions:
      contents: write
      packages: write
      id-token: write
      attestations: write
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Generate release app token
        id: app-token
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-packages: write
      - name: Install cog
        run: |
          set -euo pipefail
          COG_VERSION="7.0.0"
          COG_TARBALL="cocogitto-${COG_VERSION}-x86_64-unknown-linux-musl.tar.gz"
          COG_URL="https://github.com/cocogitto/cocogitto/releases/download/${COG_VERSION}/${COG_TARBALL}"
          COG_SHA256="e03938ff2c4c86d71c00c0f3284dbbe95c5ca76fe34a51f33e945c23010d59bb"
          curl -LsSfO "${COG_URL}"
          echo "${COG_SHA256}  ${COG_TARBALL}" | sha256sum -c -
          sudo tar xzf "${COG_TARBALL}" --overwrite --strip-components=1 \
            -C /usr/local/bin x86_64-unknown-linux-musl/cog
          rm "${COG_TARBALL}"
          cog --version
      - name: Configure git identity for tagging
        run: |
          git config user.name "specgraph-release-bot[bot]"
          git config user.email "specgraph-release-bot[bot]@users.noreply.github.com"
      - name: Preview + guard version bump
        env:
          EXPECTED: ${{ inputs.expected_increment }}
        run: |
          set -euo pipefail
          echo "Computed next version (dry-run):"
          cog bump --auto --dry-run
          if [ "${EXPECTED}" != "auto" ]; then
            CURRENT="$(git describe --tags --abbrev=0 2>/dev/null | sed 's/^v//' || true)"
            TARGET="$(cog bump --auto --dry-run 2>/dev/null | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | tail -n1 || true)"
            if [ -z "${CURRENT}" ] || [ -z "${TARGET}" ]; then
              echo "::error::could not determine version (current='${CURRENT}' target='${TARGET}'); cannot validate expected_increment=${EXPECTED}"
              exit 1
            fi
            IFS=. read -r cmaj cmin cpat <<< "${CURRENT}"
            IFS=. read -r tmaj tmin tpat <<< "${TARGET}"
            if   [ "${tmaj}" -gt "${cmaj}" ]; then KIND="major"
            elif [ "${tmin}" -gt "${cmin}" ]; then KIND="minor"
            elif [ "${tpat}" -gt "${cpat}" ]; then KIND="patch"
            else KIND="none"; fi
            if [ "${KIND}" != "${EXPECTED}" ]; then
              echo "::error::expected ${EXPECTED} bump but cog computed ${KIND} (${CURRENT} -> ${TARGET})"
              exit 1
            fi
          fi
      - name: Cut tag (cog bump, tag-only)
        run: |
          set -euo pipefail
          cog bump --auto --disable-bump-commit
          git describe --tags --abbrev=0
      - name: Get version
        id: version
        run: |
          set -euo pipefail
          TAG="$(git describe --tags --abbrev=0)"
          if ! printf '%s' "${TAG}" | grep -qE '^v[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "::error::resolved tag '${TAG}' is not a v-prefixed semver; refusing to build a malformed release"
            exit 1
          fi
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"
          echo "new_tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Building version: ${TAG#v}"
      - uses: sigstore/cosign-installer@ba7bc0a3fef59531c69a25acd34668d6d3fe6f22 # v4.1.0
      - uses: anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0
      - uses: namespacelabs/nscloud-setup-buildx-action@d059ed7184f0bc7c8b27e8810cea153d02bcc6dd # v0.0.23
      - uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
      - uses: pnpm/action-setup@02f6c237bd2518259fed6c71566509edfb3f2b74 # v4
        with:
          version: 10
          run_install: false
      - name: Build web UI
        working-directory: web
        run: pnpm install --frozen-lockfile && pnpm build
      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6
        with:
          go-version-file: go.mod
          cache: false
      - name: Configure Namespace cache
        uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9 # v1.4.2
        with:
          cache: go
      - uses: goreleaser/goreleaser-action@ec59f474b9834571250b370d4735c50f8e2d1e29 # v7
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
      - name: Scan Docker image
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # v0.35.0
        with:
          image-ref: "ghcr.io/specgraph/specgraph:${{ steps.version.outputs.version }}"
          format: table
          exit-code: "1"
          ignore-unfixed: true
          severity: CRITICAL,HIGH
      - name: Attest binary provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4
        with:
          subject-checksums: ./dist/checksums.txt
      - name: Attest Docker image provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4
        with:
          subject-checksums: ./dist/digests.txt
```

- [ ] **Step 2: Lint the workflow**

Run: `actionlint .github/workflows/release.yml`
Expected: no errors.

- [ ] **Step 3: Verify no dangling git-cliff / draft references remain**

Run: `rg -n "git-cliff|use_existing_draft|gh release create|--draft|mode: append" .github/workflows/release.yml`
Expected: no matches.

- [ ] **Step 4: Commit**

```text
jj describe -m "ci(release): single-job pipeline, GoReleaser owns the release

Collapse the release + goreleaser jobs into one. cog derives the version and
creates a local tag; GoReleaser creates the remote tag (target_commitish) and the
single published release with native notes. Remove git-cliff, CHANGELOG.md
commit/push, gh release create --draft, and gh release edit --draft=false. The
app token (contents+packages write) is passed to GoReleaser for tag + GHCR ops.

Fixes empty release notes since v0.3.7.
Refs: spgr-7r6g
Signed-off-by: Sean Brandt <claude2@sean.fzymgc.email>"
```

---

### Task 4: Delete `cliff.toml`

**Files:**

- Delete: `cliff.toml`

- [ ] **Step 1: Remove the file**

Run: `rm cliff.toml`

- [ ] **Step 2: Verify nothing else references it**

Run: `rg -l "cliff\.toml|git-cliff|git_cliff" --glob '!docs/**'`
Expected: no matches (the two `docs/superpowers/**/2026-03-26-*` files may still
reference it historically — those are superseded and left as-is).

- [ ] **Step 3: Commit**

```text
jj describe -m "ci(release): remove git-cliff config (cliff.toml)

git-cliff is no longer part of the release pipeline; cog derives versions and
GoReleaser generates notes.

Refs: spgr-7r6g
Signed-off-by: Sean Brandt <claude2@sean.fzymgc.email>"
```

---

### Task 5: Freeze `CHANGELOG.md`

**Files:**

- Modify: `CHANGELOG.md:1` (prepend a freeze notice)

- [ ] **Step 1: Prepend a freeze notice above the existing `# Changelog` heading**

Insert at the very top of the file, before the current first line:

```markdown
> **Note:** This changelog is frozen as of v0.7.0. Release notes for v0.8.0 and
> later are published on the [GitHub Releases](https://github.com/specgraph/specgraph/releases)
> page, generated by GoReleaser from conventional commits. See
> `docs/superpowers/specs/2026-06-05-release-single-job-goreleaser-design.md`.

```

Leave the rest of the file unchanged.

- [ ] **Step 2: Verify the notice renders and the heading is intact**

Run: `head -8 CHANGELOG.md`
Expected: the blockquote notice followed by a blank line and `# Changelog`.

- [ ] **Step 3: Commit**

```text
jj describe -m "docs(changelog): freeze CHANGELOG.md; notes move to GitHub Releases

The release pipeline no longer regenerates an in-repo changelog. Freeze the
existing file as a historical record and point readers to GitHub Releases.

Refs: spgr-7r6g
Signed-off-by: Sean Brandt <claude2@sean.fzymgc.email>"
```

---

### Task 6: Add `release:cut` Taskfile target

**Files:**

- Modify: `Taskfile.yml:371-378` (the `# Release` block)

- [ ] **Step 1: Add `release:cut` after `release:snapshot`**

Insert into the `# Release` task group, after the `release:snapshot` task:

```yaml
  release:cut:
    desc: "Cut a release via GitHub Actions (cog derives version + tag). Args: increment=auto|major|minor|patch"
    cmds:
      - gh workflow run release.yml -f expected_increment={{.increment | default "auto"}}
```

- [ ] **Step 2: Verify Taskfile parses and the target is listed**

Run: `task --list | rg "release:"`
Expected: `release:check`, `release:cut`, and `release:snapshot` all listed.

- [ ] **Step 3: Commit**

```text
jj describe -m "build(task): add release:cut to trigger the release workflow

task release:cut [increment=auto|major|minor|patch] dispatches the Release
workflow, mirroring the holomush convenience target.

Refs: spgr-7r6g
Signed-off-by: Sean Brandt <claude2@sean.fzymgc.email>"
```

---

### Task 7: Full local gate + PR

**Files:** none (verification only)

- [ ] **Step 1: Run the project quality gate**

Run: `task check`
Expected: fmt:check → license:check → lint → build → unit tests all pass. The
new `cog.toml`, `.goreleaser.yaml`, and workflow are covered by license-header
and lint checks.

- [ ] **Step 2: Re-validate the release tooling together**

Run: `goreleaser check && cog bump --auto --dry-run && actionlint`
Expected: config valid; a `v`-prefixed next version printed; no actionlint errors.

- [ ] **Step 3: Push the bookmark and open the PR**

```text
jj bookmark set release-single-job-goreleaser -r @
jj git push --bookmark release-single-job-goreleaser
gh pr create --title "infra(release): single-job GoReleaser-owns-release model (spgr-7r6g)" \
  --body "<summary + Fixes empty notes since v0.3.7; supersedes 2026-03-26 design>"
```

- [ ] **Step 4: Close the epic on merge**

After the PR merges and a test release is cut + verified (single published
release with populated notes + all assets), run: `bd close spgr-7r6g`.

---

## Out-of-band cleanup (not blocking; track separately)

- Backfill v0.7.0 published-release notes from its orphaned draft body.
- Delete orphaned draft releases for v0.3.7, v0.4.0, v0.5.0, v0.6.0, v0.7.0.

These are manual `gh release edit` / `gh release delete` operations and are
filed as a follow-up bead, not part of this plan's tasks.
